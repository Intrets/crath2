std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_T4_0_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_2997(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_T4_4<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_2998(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_T4_4<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_2999(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_T4_4_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3000(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_T4_4<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3001(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_T4_4<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3002(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_T4_4_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3003(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_fma_ec_T5_0<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3004(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_fma_ec_T5_0<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3005(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_fma_ec_T5_0_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3006(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_fma_ec_T5_5<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3007(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_fma_ec_T5_5<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3008(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_fma_ec_T5_5_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3009(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_fma_ec_T5_5<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3010(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_fma_ec_T5_5<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3011(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_fma_ec_T5_5_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3012(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_fma_T5_0<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3013(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_fma_T5_0<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3014(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_fma_T5_0_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3015(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_fma_T5_5<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3016(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_fma_T5_5<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3017(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_fma_T5_5_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3018(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_fma_T5_5<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3019(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_fma_T5_5<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3020(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_fma_T5_5_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3021(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_ec_T5_0<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3022(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_ec_T5_0<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3023(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_ec_T5_0_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3024(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_ec_T5_5<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3025(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_ec_T5_5<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3026(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_ec_T5_5_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3027(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_ec_T5_5<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3028(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_ec_T5_5<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3029(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_ec_T5_5_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3030(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_T5_0<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3031(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_T5_0<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3032(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_T5_0_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3033(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_T5_5<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3034(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_T5_5<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3035(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_T5_5_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3036(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_T5_5<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3037(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_T5_5<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3038(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_T5_5_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3039(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_fma_ec_T6_0<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3040(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_fma_ec_T6_0<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3041(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_fma_ec_T6_0_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3042(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_fma_ec_T6_6<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3043(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_fma_ec_T6_6<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3044(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_fma_ec_T6_6_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3045(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_fma_ec_T6_6<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3046(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_fma_ec_T6_6<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3047(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_fma_ec_T6_6_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3048(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_fma_T6_0<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3049(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_fma_T6_0<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3050(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_fma_T6_0_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3051(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_fma_T6_6<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3052(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_fma_T6_6<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3053(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_fma_T6_6_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3054(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_fma_T6_6<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3055(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_fma_T6_6<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3056(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_fma_T6_6_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3057(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_ec_T6_0<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3058(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_ec_T6_0<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3059(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_ec_T6_0_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3060(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_ec_T6_6<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3061(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_ec_T6_6<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3062(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_ec_T6_6_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3063(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_ec_T6_6<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3064(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_ec_T6_6<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3065(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_ec_T6_6_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3066(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_T6_0<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3067(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_T6_0<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3068(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_T6_0_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3069(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_T6_6<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3070(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_T6_6<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3071(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_T6_6_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3072(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_T6_6<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3073(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_T6_6<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3074(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_T6_6_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3075(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_fma_ec_T7_0<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3076(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_fma_ec_T7_0<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3077(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_fma_ec_T7_0_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3078(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_fma_ec_T7_7<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3079(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_fma_ec_T7_7<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3080(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_fma_ec_T7_7_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3081(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_fma_ec_T7_7<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3082(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_fma_ec_T7_7<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3083(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_fma_ec_T7_7_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3084(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_fma_T7_0<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3085(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_fma_T7_0<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3086(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_fma_T7_0_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3087(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_fma_T7_7<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3088(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_fma_T7_7<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3089(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_fma_T7_7_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3090(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_fma_T7_7<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3091(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_fma_T7_7<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3092(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_fma_T7_7_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3093(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_ec_T7_0<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3094(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_ec_T7_0<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3095(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_ec_T7_0_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3096(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_ec_T7_7<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3097(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_ec_T7_7<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3098(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_ec_T7_7_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3099(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_ec_T7_7<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3100(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_ec_T7_7<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3101(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_ec_T7_7_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3102(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_T7_0<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3103(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_T7_0<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3104(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_T7_0_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3105(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_T7_7<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3106(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_T7_7<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3107(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_T7_7_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3108(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_T7_7<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3109(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_T7_7<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3110(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_T7_7_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3111(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_fma_ec_T8_0<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3112(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_fma_ec_T8_0<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3113(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_fma_ec_T8_0_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3114(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_fma_ec_T8_8<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3115(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_fma_ec_T8_8<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3116(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_fma_ec_T8_8_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3117(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_fma_ec_T8_8<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3118(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_fma_ec_T8_8<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3119(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_fma_ec_T8_8_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3120(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_fma_T8_0<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3121(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_fma_T8_0<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3122(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_fma_T8_0_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3123(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_fma_T8_8<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3124(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_fma_T8_8<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3125(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_fma_T8_8_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3126(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_fma_T8_8<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3127(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_fma_T8_8<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3128(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_fma_T8_8_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3129(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_ec_T8_0<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3130(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_ec_T8_0<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3131(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_ec_T8_0_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3132(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_ec_T8_8<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3133(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_ec_T8_8<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3134(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_ec_T8_8_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3135(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_ec_T8_8<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3136(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_ec_T8_8<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3137(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_ec_T8_8_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3138(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_T8_0<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3139(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_T8_0<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3140(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_T8_0_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3141(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_T8_8<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3142(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_T8_8<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3143(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_T8_8_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3144(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_T8_8<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3145(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_T8_8<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3146(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_T8_8_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3147(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_fma_ec_T9_0<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3148(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_fma_ec_T9_0<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3149(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_fma_ec_T9_0_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3150(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_fma_ec_T9_9<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3151(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_fma_ec_T9_9<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3152(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_fma_ec_T9_9_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3153(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_fma_ec_T9_9<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3154(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_fma_ec_T9_9<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3155(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_fma_ec_T9_9_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3156(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_fma_T9_0<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3157(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_fma_T9_0<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3158(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_fma_T9_0_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3159(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_fma_T9_9<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3160(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_fma_T9_9<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3161(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_fma_T9_9_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3162(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_fma_T9_9<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3163(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_fma_T9_9<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3164(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_fma_T9_9_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3165(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_ec_T9_0<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3166(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_ec_T9_0<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3167(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_ec_T9_0_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3168(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_ec_T9_9<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3169(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_ec_T9_9<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3170(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_ec_T9_9_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3171(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_ec_T9_9<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3172(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_ec_T9_9<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3173(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_ec_T9_9_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3174(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_T9_0<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3175(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_T9_0<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3176(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_remez_abs_T9_0_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3177(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_T9_9<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3178(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_T9_9<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3179(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_remez_pade_abs_T9_9_float_simd(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3180(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_T9_9<float>(buffer[i]);
    }
    return value;
}

cr::simd::float2x4 msvc_bug_workaround_3181(std::vector<cr::simd::float2x4> const& buffer) {
    cr::simd::float2x4 value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_T9_9<cr::simd::float2x4>(buffer[i]);
    }
    return value;
}

float msvc_bug_workaround_3182(std::vector<float> const& buffer) {
    float value{};
    #if defined(COMPILER_CLANGCL) || defined(COMPILER_CLANG)
    #pragma clang loop vectorize(disable)
    #elif defined(COMPILER_MSVC)
    #pragma loop(no_vector)
    #else
    #error "TODO: disable vectorization for loop"
    #endif
    for (size_t i = 0; i < buffer.size(); i++ ) {
        value += tan_T9_9_float_simd(buffer[i]);
    }
    return value;
}

void addAll(TestResult& testResult) {
{
auto& entry = testResult.make();
entry.tags = { "noop", "noop", "float", "scalar", "reference",  };
entry.subName = "noop";
entry.accuracy_test<float>([](auto const& x) { return x; }, [](auto const& x) { return x; }, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_1, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "std::sinf", "float", "scalar", "reference",  };
entry.subName = "std::sinf";
entry.accuracy_test<float>(std::sinf, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_2, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "remez", "float", "scalar",  };
entry.subName = "sin_remez_fma_ec_T3_0<float>";
entry.accuracy_test<float>(sin_remez_fma_ec_T3_0<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_3, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "remez", "float2x4", "simd",  };
entry.subName = "sin_remez_fma_ec_T3_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_fma_ec_T3_0<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_4, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "remez", "float", "simd",  };
entry.subName = "sin_remez_fma_ec_T3_0_float_simd";
entry.accuracy_test<float>(sin_remez_fma_ec_T3_0_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_5, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "remez_pade", "float", "scalar",  };
entry.subName = "sin_remez_pade_fma_ec_T3_3<float>";
entry.accuracy_test<float>(sin_remez_pade_fma_ec_T3_3<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_6, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "remez_pade", "float2x4", "simd",  };
entry.subName = "sin_remez_pade_fma_ec_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_pade_fma_ec_T3_3<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_7, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "remez_pade", "float", "simd",  };
entry.subName = "sin_remez_pade_fma_ec_T3_3_float_simd";
entry.accuracy_test<float>(sin_remez_pade_fma_ec_T3_3_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_8, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "float", "scalar",  };
entry.subName = "sin_quart_fma_ec_T3_3<float>";
entry.accuracy_test<float>(sin_quart_fma_ec_T3_3<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_9, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "float2x4", "simd",  };
entry.subName = "sin_quart_fma_ec_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_quart_fma_ec_T3_3<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_10, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "float", "simd",  };
entry.subName = "sin_quart_fma_ec_T3_3_float_simd";
entry.accuracy_test<float>(sin_quart_fma_ec_T3_3_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_11, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "remez", "float", "scalar",  };
entry.subName = "sin_remez_fma_T3_0<float>";
entry.accuracy_test<float>(sin_remez_fma_T3_0<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_12, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "remez", "float2x4", "simd",  };
entry.subName = "sin_remez_fma_T3_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_fma_T3_0<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_13, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "remez", "float", "simd",  };
entry.subName = "sin_remez_fma_T3_0_float_simd";
entry.accuracy_test<float>(sin_remez_fma_T3_0_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_14, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "remez_pade", "float", "scalar",  };
entry.subName = "sin_remez_pade_fma_T3_3<float>";
entry.accuracy_test<float>(sin_remez_pade_fma_T3_3<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_15, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "remez_pade", "float2x4", "simd",  };
entry.subName = "sin_remez_pade_fma_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_pade_fma_T3_3<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_16, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "remez_pade", "float", "simd",  };
entry.subName = "sin_remez_pade_fma_T3_3_float_simd";
entry.accuracy_test<float>(sin_remez_pade_fma_T3_3_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_17, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "float", "scalar",  };
entry.subName = "sin_quart_fma_T3_3<float>";
entry.accuracy_test<float>(sin_quart_fma_T3_3<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_18, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "float2x4", "simd",  };
entry.subName = "sin_quart_fma_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_quart_fma_T3_3<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_19, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "float", "simd",  };
entry.subName = "sin_quart_fma_T3_3_float_simd";
entry.accuracy_test<float>(sin_quart_fma_T3_3_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_20, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "remez", "float", "scalar",  };
entry.subName = "sin_remez_ec_T3_0<float>";
entry.accuracy_test<float>(sin_remez_ec_T3_0<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_21, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "remez", "float2x4", "simd",  };
entry.subName = "sin_remez_ec_T3_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_ec_T3_0<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_22, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "remez", "float", "simd",  };
entry.subName = "sin_remez_ec_T3_0_float_simd";
entry.accuracy_test<float>(sin_remez_ec_T3_0_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_23, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "remez_pade", "float", "scalar",  };
entry.subName = "sin_remez_pade_ec_T3_3<float>";
entry.accuracy_test<float>(sin_remez_pade_ec_T3_3<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_24, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "remez_pade", "float2x4", "simd",  };
entry.subName = "sin_remez_pade_ec_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_pade_ec_T3_3<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_25, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "remez_pade", "float", "simd",  };
entry.subName = "sin_remez_pade_ec_T3_3_float_simd";
entry.accuracy_test<float>(sin_remez_pade_ec_T3_3_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_26, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "float", "scalar",  };
entry.subName = "sin_quart_ec_T3_3<float>";
entry.accuracy_test<float>(sin_quart_ec_T3_3<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_27, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "float2x4", "simd",  };
entry.subName = "sin_quart_ec_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_quart_ec_T3_3<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_28, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "float", "simd",  };
entry.subName = "sin_quart_ec_T3_3_float_simd";
entry.accuracy_test<float>(sin_quart_ec_T3_3_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_29, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "remez", "float", "scalar",  };
entry.subName = "sin_remez_T3_0<float>";
entry.accuracy_test<float>(sin_remez_T3_0<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_30, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "remez", "float2x4", "simd",  };
entry.subName = "sin_remez_T3_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_T3_0<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_31, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "remez", "float", "simd",  };
entry.subName = "sin_remez_T3_0_float_simd";
entry.accuracy_test<float>(sin_remez_T3_0_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_32, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "remez_pade", "float", "scalar",  };
entry.subName = "sin_remez_pade_T3_3<float>";
entry.accuracy_test<float>(sin_remez_pade_T3_3<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_33, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "remez_pade", "float2x4", "simd",  };
entry.subName = "sin_remez_pade_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_pade_T3_3<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_34, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "remez_pade", "float", "simd",  };
entry.subName = "sin_remez_pade_T3_3_float_simd";
entry.accuracy_test<float>(sin_remez_pade_T3_3_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_35, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "float", "scalar",  };
entry.subName = "sin_quart_T3_3<float>";
entry.accuracy_test<float>(sin_quart_T3_3<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_36, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "float2x4", "simd",  };
entry.subName = "sin_quart_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_quart_T3_3<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_37, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "float", "simd",  };
entry.subName = "sin_quart_T3_3_float_simd";
entry.accuracy_test<float>(sin_quart_T3_3_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_38, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "remez", "float", "scalar",  };
entry.subName = "sin_remez_fma_ec_T4_0<float>";
entry.accuracy_test<float>(sin_remez_fma_ec_T4_0<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_39, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "remez", "float2x4", "simd",  };
entry.subName = "sin_remez_fma_ec_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_fma_ec_T4_0<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_40, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "remez", "float", "simd",  };
entry.subName = "sin_remez_fma_ec_T4_0_float_simd";
entry.accuracy_test<float>(sin_remez_fma_ec_T4_0_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_41, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "remez_pade", "float", "scalar",  };
entry.subName = "sin_remez_pade_fma_ec_T4_4<float>";
entry.accuracy_test<float>(sin_remez_pade_fma_ec_T4_4<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_42, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "remez_pade", "float2x4", "simd",  };
entry.subName = "sin_remez_pade_fma_ec_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_pade_fma_ec_T4_4<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_43, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "remez_pade", "float", "simd",  };
entry.subName = "sin_remez_pade_fma_ec_T4_4_float_simd";
entry.accuracy_test<float>(sin_remez_pade_fma_ec_T4_4_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_44, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "float", "scalar",  };
entry.subName = "sin_quart_fma_ec_T4_4<float>";
entry.accuracy_test<float>(sin_quart_fma_ec_T4_4<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_45, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "float2x4", "simd",  };
entry.subName = "sin_quart_fma_ec_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_quart_fma_ec_T4_4<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_46, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "float", "simd",  };
entry.subName = "sin_quart_fma_ec_T4_4_float_simd";
entry.accuracy_test<float>(sin_quart_fma_ec_T4_4_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_47, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "remez", "float", "scalar",  };
entry.subName = "sin_remez_fma_T4_0<float>";
entry.accuracy_test<float>(sin_remez_fma_T4_0<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_48, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "remez", "float2x4", "simd",  };
entry.subName = "sin_remez_fma_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_fma_T4_0<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_49, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "remez", "float", "simd",  };
entry.subName = "sin_remez_fma_T4_0_float_simd";
entry.accuracy_test<float>(sin_remez_fma_T4_0_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_50, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "remez_pade", "float", "scalar",  };
entry.subName = "sin_remez_pade_fma_T4_4<float>";
entry.accuracy_test<float>(sin_remez_pade_fma_T4_4<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_51, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "remez_pade", "float2x4", "simd",  };
entry.subName = "sin_remez_pade_fma_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_pade_fma_T4_4<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_52, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "remez_pade", "float", "simd",  };
entry.subName = "sin_remez_pade_fma_T4_4_float_simd";
entry.accuracy_test<float>(sin_remez_pade_fma_T4_4_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_53, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "float", "scalar",  };
entry.subName = "sin_quart_fma_T4_4<float>";
entry.accuracy_test<float>(sin_quart_fma_T4_4<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_54, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "float2x4", "simd",  };
entry.subName = "sin_quart_fma_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_quart_fma_T4_4<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_55, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "float", "simd",  };
entry.subName = "sin_quart_fma_T4_4_float_simd";
entry.accuracy_test<float>(sin_quart_fma_T4_4_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_56, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "remez", "float", "scalar",  };
entry.subName = "sin_remez_ec_T4_0<float>";
entry.accuracy_test<float>(sin_remez_ec_T4_0<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_57, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "remez", "float2x4", "simd",  };
entry.subName = "sin_remez_ec_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_ec_T4_0<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_58, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "remez", "float", "simd",  };
entry.subName = "sin_remez_ec_T4_0_float_simd";
entry.accuracy_test<float>(sin_remez_ec_T4_0_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_59, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "remez_pade", "float", "scalar",  };
entry.subName = "sin_remez_pade_ec_T4_4<float>";
entry.accuracy_test<float>(sin_remez_pade_ec_T4_4<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_60, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "remez_pade", "float2x4", "simd",  };
entry.subName = "sin_remez_pade_ec_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_pade_ec_T4_4<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_61, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "remez_pade", "float", "simd",  };
entry.subName = "sin_remez_pade_ec_T4_4_float_simd";
entry.accuracy_test<float>(sin_remez_pade_ec_T4_4_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_62, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "float", "scalar",  };
entry.subName = "sin_quart_ec_T4_4<float>";
entry.accuracy_test<float>(sin_quart_ec_T4_4<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_63, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "float2x4", "simd",  };
entry.subName = "sin_quart_ec_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_quart_ec_T4_4<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_64, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "float", "simd",  };
entry.subName = "sin_quart_ec_T4_4_float_simd";
entry.accuracy_test<float>(sin_quart_ec_T4_4_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_65, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "remez", "float", "scalar",  };
entry.subName = "sin_remez_T4_0<float>";
entry.accuracy_test<float>(sin_remez_T4_0<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_66, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "remez", "float2x4", "simd",  };
entry.subName = "sin_remez_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_T4_0<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_67, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "remez", "float", "simd",  };
entry.subName = "sin_remez_T4_0_float_simd";
entry.accuracy_test<float>(sin_remez_T4_0_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_68, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "remez_pade", "float", "scalar",  };
entry.subName = "sin_remez_pade_T4_4<float>";
entry.accuracy_test<float>(sin_remez_pade_T4_4<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_69, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "remez_pade", "float2x4", "simd",  };
entry.subName = "sin_remez_pade_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_pade_T4_4<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_70, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "remez_pade", "float", "simd",  };
entry.subName = "sin_remez_pade_T4_4_float_simd";
entry.accuracy_test<float>(sin_remez_pade_T4_4_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_71, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "float", "scalar",  };
entry.subName = "sin_quart_T4_4<float>";
entry.accuracy_test<float>(sin_quart_T4_4<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_72, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "float2x4", "simd",  };
entry.subName = "sin_quart_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_quart_T4_4<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_73, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "float", "simd",  };
entry.subName = "sin_quart_T4_4_float_simd";
entry.accuracy_test<float>(sin_quart_T4_4_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_74, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "remez", "float", "scalar",  };
entry.subName = "sin_remez_fma_ec_T5_0<float>";
entry.accuracy_test<float>(sin_remez_fma_ec_T5_0<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_75, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "remez", "float2x4", "simd",  };
entry.subName = "sin_remez_fma_ec_T5_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_fma_ec_T5_0<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_76, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "remez", "float", "simd",  };
entry.subName = "sin_remez_fma_ec_T5_0_float_simd";
entry.accuracy_test<float>(sin_remez_fma_ec_T5_0_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_77, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "remez_pade", "float", "scalar",  };
entry.subName = "sin_remez_pade_fma_ec_T5_5<float>";
entry.accuracy_test<float>(sin_remez_pade_fma_ec_T5_5<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_78, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "remez_pade", "float2x4", "simd",  };
entry.subName = "sin_remez_pade_fma_ec_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_pade_fma_ec_T5_5<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_79, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "remez_pade", "float", "simd",  };
entry.subName = "sin_remez_pade_fma_ec_T5_5_float_simd";
entry.accuracy_test<float>(sin_remez_pade_fma_ec_T5_5_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_80, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "float", "scalar",  };
entry.subName = "sin_quart_fma_ec_T5_5<float>";
entry.accuracy_test<float>(sin_quart_fma_ec_T5_5<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_81, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "float2x4", "simd",  };
entry.subName = "sin_quart_fma_ec_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_quart_fma_ec_T5_5<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_82, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "float", "simd",  };
entry.subName = "sin_quart_fma_ec_T5_5_float_simd";
entry.accuracy_test<float>(sin_quart_fma_ec_T5_5_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_83, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "remez", "float", "scalar",  };
entry.subName = "sin_remez_fma_T5_0<float>";
entry.accuracy_test<float>(sin_remez_fma_T5_0<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_84, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "remez", "float2x4", "simd",  };
entry.subName = "sin_remez_fma_T5_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_fma_T5_0<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_85, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "remez", "float", "simd",  };
entry.subName = "sin_remez_fma_T5_0_float_simd";
entry.accuracy_test<float>(sin_remez_fma_T5_0_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_86, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "remez_pade", "float", "scalar",  };
entry.subName = "sin_remez_pade_fma_T5_5<float>";
entry.accuracy_test<float>(sin_remez_pade_fma_T5_5<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_87, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "remez_pade", "float2x4", "simd",  };
entry.subName = "sin_remez_pade_fma_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_pade_fma_T5_5<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_88, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "remez_pade", "float", "simd",  };
entry.subName = "sin_remez_pade_fma_T5_5_float_simd";
entry.accuracy_test<float>(sin_remez_pade_fma_T5_5_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_89, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "float", "scalar",  };
entry.subName = "sin_quart_fma_T5_5<float>";
entry.accuracy_test<float>(sin_quart_fma_T5_5<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_90, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "float2x4", "simd",  };
entry.subName = "sin_quart_fma_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_quart_fma_T5_5<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_91, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "float", "simd",  };
entry.subName = "sin_quart_fma_T5_5_float_simd";
entry.accuracy_test<float>(sin_quart_fma_T5_5_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_92, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "remez", "float", "scalar",  };
entry.subName = "sin_remez_ec_T5_0<float>";
entry.accuracy_test<float>(sin_remez_ec_T5_0<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_93, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "remez", "float2x4", "simd",  };
entry.subName = "sin_remez_ec_T5_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_ec_T5_0<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_94, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "remez", "float", "simd",  };
entry.subName = "sin_remez_ec_T5_0_float_simd";
entry.accuracy_test<float>(sin_remez_ec_T5_0_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_95, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "remez_pade", "float", "scalar",  };
entry.subName = "sin_remez_pade_ec_T5_5<float>";
entry.accuracy_test<float>(sin_remez_pade_ec_T5_5<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_96, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "remez_pade", "float2x4", "simd",  };
entry.subName = "sin_remez_pade_ec_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_pade_ec_T5_5<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_97, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "remez_pade", "float", "simd",  };
entry.subName = "sin_remez_pade_ec_T5_5_float_simd";
entry.accuracy_test<float>(sin_remez_pade_ec_T5_5_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_98, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "float", "scalar",  };
entry.subName = "sin_quart_ec_T5_5<float>";
entry.accuracy_test<float>(sin_quart_ec_T5_5<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_99, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "float2x4", "simd",  };
entry.subName = "sin_quart_ec_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_quart_ec_T5_5<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_100, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "float", "simd",  };
entry.subName = "sin_quart_ec_T5_5_float_simd";
entry.accuracy_test<float>(sin_quart_ec_T5_5_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_101, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "remez", "float", "scalar",  };
entry.subName = "sin_remez_T5_0<float>";
entry.accuracy_test<float>(sin_remez_T5_0<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_102, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "remez", "float2x4", "simd",  };
entry.subName = "sin_remez_T5_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_T5_0<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_103, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "remez", "float", "simd",  };
entry.subName = "sin_remez_T5_0_float_simd";
entry.accuracy_test<float>(sin_remez_T5_0_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_104, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "remez_pade", "float", "scalar",  };
entry.subName = "sin_remez_pade_T5_5<float>";
entry.accuracy_test<float>(sin_remez_pade_T5_5<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_105, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "remez_pade", "float2x4", "simd",  };
entry.subName = "sin_remez_pade_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_pade_T5_5<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_106, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "remez_pade", "float", "simd",  };
entry.subName = "sin_remez_pade_T5_5_float_simd";
entry.accuracy_test<float>(sin_remez_pade_T5_5_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_107, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "float", "scalar",  };
entry.subName = "sin_quart_T5_5<float>";
entry.accuracy_test<float>(sin_quart_T5_5<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_108, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "float2x4", "simd",  };
entry.subName = "sin_quart_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_quart_T5_5<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_109, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "float", "simd",  };
entry.subName = "sin_quart_T5_5_float_simd";
entry.accuracy_test<float>(sin_quart_T5_5_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_110, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "remez", "float", "scalar",  };
entry.subName = "sin_remez_fma_ec_T6_0<float>";
entry.accuracy_test<float>(sin_remez_fma_ec_T6_0<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_111, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "remez", "float2x4", "simd",  };
entry.subName = "sin_remez_fma_ec_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_fma_ec_T6_0<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_112, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "remez", "float", "simd",  };
entry.subName = "sin_remez_fma_ec_T6_0_float_simd";
entry.accuracy_test<float>(sin_remez_fma_ec_T6_0_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_113, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "remez_pade", "float", "scalar",  };
entry.subName = "sin_remez_pade_fma_ec_T6_6<float>";
entry.accuracy_test<float>(sin_remez_pade_fma_ec_T6_6<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_114, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "remez_pade", "float2x4", "simd",  };
entry.subName = "sin_remez_pade_fma_ec_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_pade_fma_ec_T6_6<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_115, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "remez_pade", "float", "simd",  };
entry.subName = "sin_remez_pade_fma_ec_T6_6_float_simd";
entry.accuracy_test<float>(sin_remez_pade_fma_ec_T6_6_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_116, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "float", "scalar",  };
entry.subName = "sin_quart_fma_ec_T6_6<float>";
entry.accuracy_test<float>(sin_quart_fma_ec_T6_6<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_117, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "float2x4", "simd",  };
entry.subName = "sin_quart_fma_ec_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_quart_fma_ec_T6_6<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_118, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "float", "simd",  };
entry.subName = "sin_quart_fma_ec_T6_6_float_simd";
entry.accuracy_test<float>(sin_quart_fma_ec_T6_6_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_119, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "remez", "float", "scalar",  };
entry.subName = "sin_remez_fma_T6_0<float>";
entry.accuracy_test<float>(sin_remez_fma_T6_0<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_120, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "remez", "float2x4", "simd",  };
entry.subName = "sin_remez_fma_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_fma_T6_0<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_121, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "remez", "float", "simd",  };
entry.subName = "sin_remez_fma_T6_0_float_simd";
entry.accuracy_test<float>(sin_remez_fma_T6_0_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_122, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "remez_pade", "float", "scalar",  };
entry.subName = "sin_remez_pade_fma_T6_6<float>";
entry.accuracy_test<float>(sin_remez_pade_fma_T6_6<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_123, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "remez_pade", "float2x4", "simd",  };
entry.subName = "sin_remez_pade_fma_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_pade_fma_T6_6<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_124, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "remez_pade", "float", "simd",  };
entry.subName = "sin_remez_pade_fma_T6_6_float_simd";
entry.accuracy_test<float>(sin_remez_pade_fma_T6_6_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_125, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "float", "scalar",  };
entry.subName = "sin_quart_fma_T6_6<float>";
entry.accuracy_test<float>(sin_quart_fma_T6_6<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_126, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "float2x4", "simd",  };
entry.subName = "sin_quart_fma_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_quart_fma_T6_6<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_127, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "float", "simd",  };
entry.subName = "sin_quart_fma_T6_6_float_simd";
entry.accuracy_test<float>(sin_quart_fma_T6_6_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_128, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "remez", "float", "scalar",  };
entry.subName = "sin_remez_ec_T6_0<float>";
entry.accuracy_test<float>(sin_remez_ec_T6_0<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_129, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "remez", "float2x4", "simd",  };
entry.subName = "sin_remez_ec_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_ec_T6_0<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_130, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "remez", "float", "simd",  };
entry.subName = "sin_remez_ec_T6_0_float_simd";
entry.accuracy_test<float>(sin_remez_ec_T6_0_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_131, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "remez_pade", "float", "scalar",  };
entry.subName = "sin_remez_pade_ec_T6_6<float>";
entry.accuracy_test<float>(sin_remez_pade_ec_T6_6<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_132, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "remez_pade", "float2x4", "simd",  };
entry.subName = "sin_remez_pade_ec_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_pade_ec_T6_6<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_133, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "remez_pade", "float", "simd",  };
entry.subName = "sin_remez_pade_ec_T6_6_float_simd";
entry.accuracy_test<float>(sin_remez_pade_ec_T6_6_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_134, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "float", "scalar",  };
entry.subName = "sin_quart_ec_T6_6<float>";
entry.accuracy_test<float>(sin_quart_ec_T6_6<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_135, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "float2x4", "simd",  };
entry.subName = "sin_quart_ec_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_quart_ec_T6_6<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_136, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "float", "simd",  };
entry.subName = "sin_quart_ec_T6_6_float_simd";
entry.accuracy_test<float>(sin_quart_ec_T6_6_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_137, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "remez", "float", "scalar",  };
entry.subName = "sin_remez_T6_0<float>";
entry.accuracy_test<float>(sin_remez_T6_0<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_138, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "remez", "float2x4", "simd",  };
entry.subName = "sin_remez_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_T6_0<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_139, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "remez", "float", "simd",  };
entry.subName = "sin_remez_T6_0_float_simd";
entry.accuracy_test<float>(sin_remez_T6_0_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_140, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "remez_pade", "float", "scalar",  };
entry.subName = "sin_remez_pade_T6_6<float>";
entry.accuracy_test<float>(sin_remez_pade_T6_6<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_141, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "remez_pade", "float2x4", "simd",  };
entry.subName = "sin_remez_pade_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_pade_T6_6<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_142, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "remez_pade", "float", "simd",  };
entry.subName = "sin_remez_pade_T6_6_float_simd";
entry.accuracy_test<float>(sin_remez_pade_T6_6_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_143, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "float", "scalar",  };
entry.subName = "sin_quart_T6_6<float>";
entry.accuracy_test<float>(sin_quart_T6_6<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_144, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "float2x4", "simd",  };
entry.subName = "sin_quart_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_quart_T6_6<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_145, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "float", "simd",  };
entry.subName = "sin_quart_T6_6_float_simd";
entry.accuracy_test<float>(sin_quart_T6_6_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_146, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "remez", "float", "scalar",  };
entry.subName = "sin_remez_fma_ec_T7_0<float>";
entry.accuracy_test<float>(sin_remez_fma_ec_T7_0<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_147, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "remez", "float2x4", "simd",  };
entry.subName = "sin_remez_fma_ec_T7_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_fma_ec_T7_0<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_148, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "remez", "float", "simd",  };
entry.subName = "sin_remez_fma_ec_T7_0_float_simd";
entry.accuracy_test<float>(sin_remez_fma_ec_T7_0_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_149, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "remez_pade", "float", "scalar",  };
entry.subName = "sin_remez_pade_fma_ec_T7_7<float>";
entry.accuracy_test<float>(sin_remez_pade_fma_ec_T7_7<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_150, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "remez_pade", "float2x4", "simd",  };
entry.subName = "sin_remez_pade_fma_ec_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_pade_fma_ec_T7_7<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_151, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "remez_pade", "float", "simd",  };
entry.subName = "sin_remez_pade_fma_ec_T7_7_float_simd";
entry.accuracy_test<float>(sin_remez_pade_fma_ec_T7_7_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_152, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "float", "scalar",  };
entry.subName = "sin_quart_fma_ec_T7_7<float>";
entry.accuracy_test<float>(sin_quart_fma_ec_T7_7<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_153, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "float2x4", "simd",  };
entry.subName = "sin_quart_fma_ec_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_quart_fma_ec_T7_7<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_154, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "float", "simd",  };
entry.subName = "sin_quart_fma_ec_T7_7_float_simd";
entry.accuracy_test<float>(sin_quart_fma_ec_T7_7_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_155, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "remez", "float", "scalar",  };
entry.subName = "sin_remez_fma_T7_0<float>";
entry.accuracy_test<float>(sin_remez_fma_T7_0<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_156, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "remez", "float2x4", "simd",  };
entry.subName = "sin_remez_fma_T7_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_fma_T7_0<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_157, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "remez", "float", "simd",  };
entry.subName = "sin_remez_fma_T7_0_float_simd";
entry.accuracy_test<float>(sin_remez_fma_T7_0_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_158, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "remez_pade", "float", "scalar",  };
entry.subName = "sin_remez_pade_fma_T7_7<float>";
entry.accuracy_test<float>(sin_remez_pade_fma_T7_7<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_159, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "remez_pade", "float2x4", "simd",  };
entry.subName = "sin_remez_pade_fma_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_pade_fma_T7_7<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_160, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "remez_pade", "float", "simd",  };
entry.subName = "sin_remez_pade_fma_T7_7_float_simd";
entry.accuracy_test<float>(sin_remez_pade_fma_T7_7_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_161, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "float", "scalar",  };
entry.subName = "sin_quart_fma_T7_7<float>";
entry.accuracy_test<float>(sin_quart_fma_T7_7<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_162, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "float2x4", "simd",  };
entry.subName = "sin_quart_fma_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_quart_fma_T7_7<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_163, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "float", "simd",  };
entry.subName = "sin_quart_fma_T7_7_float_simd";
entry.accuracy_test<float>(sin_quart_fma_T7_7_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_164, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "remez", "float", "scalar",  };
entry.subName = "sin_remez_ec_T7_0<float>";
entry.accuracy_test<float>(sin_remez_ec_T7_0<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_165, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "remez", "float2x4", "simd",  };
entry.subName = "sin_remez_ec_T7_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_ec_T7_0<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_166, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "remez", "float", "simd",  };
entry.subName = "sin_remez_ec_T7_0_float_simd";
entry.accuracy_test<float>(sin_remez_ec_T7_0_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_167, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "remez_pade", "float", "scalar",  };
entry.subName = "sin_remez_pade_ec_T7_7<float>";
entry.accuracy_test<float>(sin_remez_pade_ec_T7_7<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_168, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "remez_pade", "float2x4", "simd",  };
entry.subName = "sin_remez_pade_ec_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_pade_ec_T7_7<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_169, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "remez_pade", "float", "simd",  };
entry.subName = "sin_remez_pade_ec_T7_7_float_simd";
entry.accuracy_test<float>(sin_remez_pade_ec_T7_7_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_170, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "float", "scalar",  };
entry.subName = "sin_quart_ec_T7_7<float>";
entry.accuracy_test<float>(sin_quart_ec_T7_7<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_171, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "float2x4", "simd",  };
entry.subName = "sin_quart_ec_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_quart_ec_T7_7<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_172, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "float", "simd",  };
entry.subName = "sin_quart_ec_T7_7_float_simd";
entry.accuracy_test<float>(sin_quart_ec_T7_7_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_173, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "remez", "float", "scalar",  };
entry.subName = "sin_remez_T7_0<float>";
entry.accuracy_test<float>(sin_remez_T7_0<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_174, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "remez", "float2x4", "simd",  };
entry.subName = "sin_remez_T7_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_T7_0<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_175, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "remez", "float", "simd",  };
entry.subName = "sin_remez_T7_0_float_simd";
entry.accuracy_test<float>(sin_remez_T7_0_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_176, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "remez_pade", "float", "scalar",  };
entry.subName = "sin_remez_pade_T7_7<float>";
entry.accuracy_test<float>(sin_remez_pade_T7_7<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_177, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "remez_pade", "float2x4", "simd",  };
entry.subName = "sin_remez_pade_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_pade_T7_7<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_178, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "remez_pade", "float", "simd",  };
entry.subName = "sin_remez_pade_T7_7_float_simd";
entry.accuracy_test<float>(sin_remez_pade_T7_7_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_179, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "float", "scalar",  };
entry.subName = "sin_quart_T7_7<float>";
entry.accuracy_test<float>(sin_quart_T7_7<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_180, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "float2x4", "simd",  };
entry.subName = "sin_quart_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_quart_T7_7<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_181, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "float", "simd",  };
entry.subName = "sin_quart_T7_7_float_simd";
entry.accuracy_test<float>(sin_quart_T7_7_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_182, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "remez", "float", "scalar",  };
entry.subName = "sin_remez_fma_ec_T8_0<float>";
entry.accuracy_test<float>(sin_remez_fma_ec_T8_0<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_183, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "remez", "float2x4", "simd",  };
entry.subName = "sin_remez_fma_ec_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_fma_ec_T8_0<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_184, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "remez", "float", "simd",  };
entry.subName = "sin_remez_fma_ec_T8_0_float_simd";
entry.accuracy_test<float>(sin_remez_fma_ec_T8_0_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_185, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "remez_pade", "float", "scalar",  };
entry.subName = "sin_remez_pade_fma_ec_T8_8<float>";
entry.accuracy_test<float>(sin_remez_pade_fma_ec_T8_8<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_186, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "remez_pade", "float2x4", "simd",  };
entry.subName = "sin_remez_pade_fma_ec_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_pade_fma_ec_T8_8<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_187, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "remez_pade", "float", "simd",  };
entry.subName = "sin_remez_pade_fma_ec_T8_8_float_simd";
entry.accuracy_test<float>(sin_remez_pade_fma_ec_T8_8_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_188, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "float", "scalar",  };
entry.subName = "sin_quart_fma_ec_T8_8<float>";
entry.accuracy_test<float>(sin_quart_fma_ec_T8_8<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_189, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "float2x4", "simd",  };
entry.subName = "sin_quart_fma_ec_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_quart_fma_ec_T8_8<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_190, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "float", "simd",  };
entry.subName = "sin_quart_fma_ec_T8_8_float_simd";
entry.accuracy_test<float>(sin_quart_fma_ec_T8_8_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_191, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "remez", "float", "scalar",  };
entry.subName = "sin_remez_fma_T8_0<float>";
entry.accuracy_test<float>(sin_remez_fma_T8_0<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_192, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "remez", "float2x4", "simd",  };
entry.subName = "sin_remez_fma_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_fma_T8_0<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_193, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "remez", "float", "simd",  };
entry.subName = "sin_remez_fma_T8_0_float_simd";
entry.accuracy_test<float>(sin_remez_fma_T8_0_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_194, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "remez_pade", "float", "scalar",  };
entry.subName = "sin_remez_pade_fma_T8_8<float>";
entry.accuracy_test<float>(sin_remez_pade_fma_T8_8<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_195, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "remez_pade", "float2x4", "simd",  };
entry.subName = "sin_remez_pade_fma_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_pade_fma_T8_8<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_196, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "remez_pade", "float", "simd",  };
entry.subName = "sin_remez_pade_fma_T8_8_float_simd";
entry.accuracy_test<float>(sin_remez_pade_fma_T8_8_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_197, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "float", "scalar",  };
entry.subName = "sin_quart_fma_T8_8<float>";
entry.accuracy_test<float>(sin_quart_fma_T8_8<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_198, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "float2x4", "simd",  };
entry.subName = "sin_quart_fma_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_quart_fma_T8_8<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_199, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "float", "simd",  };
entry.subName = "sin_quart_fma_T8_8_float_simd";
entry.accuracy_test<float>(sin_quart_fma_T8_8_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_200, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "remez", "float", "scalar",  };
entry.subName = "sin_remez_ec_T8_0<float>";
entry.accuracy_test<float>(sin_remez_ec_T8_0<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_201, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "remez", "float2x4", "simd",  };
entry.subName = "sin_remez_ec_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_ec_T8_0<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_202, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "remez", "float", "simd",  };
entry.subName = "sin_remez_ec_T8_0_float_simd";
entry.accuracy_test<float>(sin_remez_ec_T8_0_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_203, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "remez_pade", "float", "scalar",  };
entry.subName = "sin_remez_pade_ec_T8_8<float>";
entry.accuracy_test<float>(sin_remez_pade_ec_T8_8<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_204, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "remez_pade", "float2x4", "simd",  };
entry.subName = "sin_remez_pade_ec_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_pade_ec_T8_8<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_205, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "remez_pade", "float", "simd",  };
entry.subName = "sin_remez_pade_ec_T8_8_float_simd";
entry.accuracy_test<float>(sin_remez_pade_ec_T8_8_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_206, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "float", "scalar",  };
entry.subName = "sin_quart_ec_T8_8<float>";
entry.accuracy_test<float>(sin_quart_ec_T8_8<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_207, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "float2x4", "simd",  };
entry.subName = "sin_quart_ec_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_quart_ec_T8_8<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_208, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "float", "simd",  };
entry.subName = "sin_quart_ec_T8_8_float_simd";
entry.accuracy_test<float>(sin_quart_ec_T8_8_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_209, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "remez", "float", "scalar",  };
entry.subName = "sin_remez_T8_0<float>";
entry.accuracy_test<float>(sin_remez_T8_0<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_210, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "remez", "float2x4", "simd",  };
entry.subName = "sin_remez_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_T8_0<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_211, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "remez", "float", "simd",  };
entry.subName = "sin_remez_T8_0_float_simd";
entry.accuracy_test<float>(sin_remez_T8_0_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_212, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "remez_pade", "float", "scalar",  };
entry.subName = "sin_remez_pade_T8_8<float>";
entry.accuracy_test<float>(sin_remez_pade_T8_8<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_213, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "remez_pade", "float2x4", "simd",  };
entry.subName = "sin_remez_pade_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_pade_T8_8<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_214, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "remez_pade", "float", "simd",  };
entry.subName = "sin_remez_pade_T8_8_float_simd";
entry.accuracy_test<float>(sin_remez_pade_T8_8_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_215, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "float", "scalar",  };
entry.subName = "sin_quart_T8_8<float>";
entry.accuracy_test<float>(sin_quart_T8_8<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_216, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "float2x4", "simd",  };
entry.subName = "sin_quart_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_quart_T8_8<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_217, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "float", "simd",  };
entry.subName = "sin_quart_T8_8_float_simd";
entry.accuracy_test<float>(sin_quart_T8_8_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_218, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "remez", "float", "scalar",  };
entry.subName = "sin_remez_fma_ec_T9_0<float>";
entry.accuracy_test<float>(sin_remez_fma_ec_T9_0<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_219, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "remez", "float2x4", "simd",  };
entry.subName = "sin_remez_fma_ec_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_fma_ec_T9_0<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_220, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "remez", "float", "simd",  };
entry.subName = "sin_remez_fma_ec_T9_0_float_simd";
entry.accuracy_test<float>(sin_remez_fma_ec_T9_0_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_221, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "remez_pade", "float", "scalar",  };
entry.subName = "sin_remez_pade_fma_ec_T9_9<float>";
entry.accuracy_test<float>(sin_remez_pade_fma_ec_T9_9<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_222, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "remez_pade", "float2x4", "simd",  };
entry.subName = "sin_remez_pade_fma_ec_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_pade_fma_ec_T9_9<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_223, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "remez_pade", "float", "simd",  };
entry.subName = "sin_remez_pade_fma_ec_T9_9_float_simd";
entry.accuracy_test<float>(sin_remez_pade_fma_ec_T9_9_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_224, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "float", "scalar",  };
entry.subName = "sin_quart_fma_ec_T9_9<float>";
entry.accuracy_test<float>(sin_quart_fma_ec_T9_9<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_225, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "float2x4", "simd",  };
entry.subName = "sin_quart_fma_ec_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_quart_fma_ec_T9_9<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_226, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin", "float", "simd",  };
entry.subName = "sin_quart_fma_ec_T9_9_float_simd";
entry.accuracy_test<float>(sin_quart_fma_ec_T9_9_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_227, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "remez", "float", "scalar",  };
entry.subName = "sin_remez_fma_T9_0<float>";
entry.accuracy_test<float>(sin_remez_fma_T9_0<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_228, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "remez", "float2x4", "simd",  };
entry.subName = "sin_remez_fma_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_fma_T9_0<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_229, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "remez", "float", "simd",  };
entry.subName = "sin_remez_fma_T9_0_float_simd";
entry.accuracy_test<float>(sin_remez_fma_T9_0_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_230, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "remez_pade", "float", "scalar",  };
entry.subName = "sin_remez_pade_fma_T9_9<float>";
entry.accuracy_test<float>(sin_remez_pade_fma_T9_9<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_231, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "remez_pade", "float2x4", "simd",  };
entry.subName = "sin_remez_pade_fma_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_pade_fma_T9_9<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_232, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "remez_pade", "float", "simd",  };
entry.subName = "sin_remez_pade_fma_T9_9_float_simd";
entry.accuracy_test<float>(sin_remez_pade_fma_T9_9_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_233, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "float", "scalar",  };
entry.subName = "sin_quart_fma_T9_9<float>";
entry.accuracy_test<float>(sin_quart_fma_T9_9<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_234, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "float2x4", "simd",  };
entry.subName = "sin_quart_fma_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_quart_fma_T9_9<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_235, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin", "float", "simd",  };
entry.subName = "sin_quart_fma_T9_9_float_simd";
entry.accuracy_test<float>(sin_quart_fma_T9_9_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_236, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "remez", "float", "scalar",  };
entry.subName = "sin_remez_ec_T9_0<float>";
entry.accuracy_test<float>(sin_remez_ec_T9_0<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_237, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "remez", "float2x4", "simd",  };
entry.subName = "sin_remez_ec_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_ec_T9_0<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_238, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "remez", "float", "simd",  };
entry.subName = "sin_remez_ec_T9_0_float_simd";
entry.accuracy_test<float>(sin_remez_ec_T9_0_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_239, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "remez_pade", "float", "scalar",  };
entry.subName = "sin_remez_pade_ec_T9_9<float>";
entry.accuracy_test<float>(sin_remez_pade_ec_T9_9<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_240, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "remez_pade", "float2x4", "simd",  };
entry.subName = "sin_remez_pade_ec_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_pade_ec_T9_9<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_241, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "remez_pade", "float", "simd",  };
entry.subName = "sin_remez_pade_ec_T9_9_float_simd";
entry.accuracy_test<float>(sin_remez_pade_ec_T9_9_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_242, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "float", "scalar",  };
entry.subName = "sin_quart_ec_T9_9<float>";
entry.accuracy_test<float>(sin_quart_ec_T9_9<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_243, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "float2x4", "simd",  };
entry.subName = "sin_quart_ec_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_quart_ec_T9_9<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_244, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin", "float", "simd",  };
entry.subName = "sin_quart_ec_T9_9_float_simd";
entry.accuracy_test<float>(sin_quart_ec_T9_9_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_245, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "remez", "float", "scalar",  };
entry.subName = "sin_remez_T9_0<float>";
entry.accuracy_test<float>(sin_remez_T9_0<float>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_246, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "remez", "float2x4", "simd",  };
entry.subName = "sin_remez_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_remez_T9_0<cr::simd::float2x4>, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_247, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "remez", "float", "simd",  };
entry.subName = "sin_remez_T9_0_float_simd";
entry.accuracy_test<float>(sin_remez_T9_0_float_simd, std::sinf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_248, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin", "remez_pade", "
T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_unit1_remez_fma_T9_0<cr::simd::float2x4>, std::sinf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_697, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin_unit1", "remez", "float", "simd",  };
entry.subName = "sin_unit1_remez_fma_T9_0_float_simd";
entry.accuracy_test<float>(sin_unit1_remez_fma_T9_0_float_simd, std::sinf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_698, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin_unit1", "remez_pade", "float", "scalar",  };
entry.subName = "sin_unit1_remez_pade_fma_T9_9<float>";
entry.accuracy_test<float>(sin_unit1_remez_pade_fma_T9_9<float>, std::sinf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_699, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin_unit1", "remez_pade", "float2x4", "simd",  };
entry.subName = "sin_unit1_remez_pade_fma_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_unit1_remez_pade_fma_T9_9<cr::simd::float2x4>, std::sinf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_700, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin_unit1", "remez_pade", "float", "simd",  };
entry.subName = "sin_unit1_remez_pade_fma_T9_9_float_simd";
entry.accuracy_test<float>(sin_unit1_remez_pade_fma_T9_9_float_simd, std::sinf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_701, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin_unit1", "float", "scalar",  };
entry.subName = "sin_unit1_quart_fma_T9_9<float>";
entry.accuracy_test<float>(sin_unit1_quart_fma_T9_9<float>, std::sinf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_702, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin_unit1", "float2x4", "simd",  };
entry.subName = "sin_unit1_quart_fma_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_unit1_quart_fma_T9_9<cr::simd::float2x4>, std::sinf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_703, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin_unit1", "float", "simd",  };
entry.subName = "sin_unit1_quart_fma_T9_9_float_simd";
entry.accuracy_test<float>(sin_unit1_quart_fma_T9_9_float_simd, std::sinf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_704, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin_unit1", "remez", "float", "scalar",  };
entry.subName = "sin_unit1_remez_ec_T9_0<float>";
entry.accuracy_test<float>(sin_unit1_remez_ec_T9_0<float>, std::sinf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_705, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin_unit1", "remez", "float2x4", "simd",  };
entry.subName = "sin_unit1_remez_ec_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_unit1_remez_ec_T9_0<cr::simd::float2x4>, std::sinf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_706, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin_unit1", "remez", "float", "simd",  };
entry.subName = "sin_unit1_remez_ec_T9_0_float_simd";
entry.accuracy_test<float>(sin_unit1_remez_ec_T9_0_float_simd, std::sinf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_707, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin_unit1", "remez_pade", "float", "scalar",  };
entry.subName = "sin_unit1_remez_pade_ec_T9_9<float>";
entry.accuracy_test<float>(sin_unit1_remez_pade_ec_T9_9<float>, std::sinf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_708, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin_unit1", "remez_pade", "float2x4", "simd",  };
entry.subName = "sin_unit1_remez_pade_ec_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_unit1_remez_pade_ec_T9_9<cr::simd::float2x4>, std::sinf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_709, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin_unit1", "remez_pade", "float", "simd",  };
entry.subName = "sin_unit1_remez_pade_ec_T9_9_float_simd";
entry.accuracy_test<float>(sin_unit1_remez_pade_ec_T9_9_float_simd, std::sinf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_710, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin_unit1", "float", "scalar",  };
entry.subName = "sin_unit1_quart_ec_T9_9<float>";
entry.accuracy_test<float>(sin_unit1_quart_ec_T9_9<float>, std::sinf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_711, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin_unit1", "float2x4", "simd",  };
entry.subName = "sin_unit1_quart_ec_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_unit1_quart_ec_T9_9<cr::simd::float2x4>, std::sinf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_712, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin_unit1", "float", "simd",  };
entry.subName = "sin_unit1_quart_ec_T9_9_float_simd";
entry.accuracy_test<float>(sin_unit1_quart_ec_T9_9_float_simd, std::sinf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_713, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin_unit1", "remez", "float", "scalar",  };
entry.subName = "sin_unit1_remez_T9_0<float>";
entry.accuracy_test<float>(sin_unit1_remez_T9_0<float>, std::sinf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_714, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin_unit1", "remez", "float2x4", "simd",  };
entry.subName = "sin_unit1_remez_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_unit1_remez_T9_0<cr::simd::float2x4>, std::sinf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_715, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin_unit1", "remez", "float", "simd",  };
entry.subName = "sin_unit1_remez_T9_0_float_simd";
entry.accuracy_test<float>(sin_unit1_remez_T9_0_float_simd, std::sinf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_716, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin_unit1", "remez_pade", "float", "scalar",  };
entry.subName = "sin_unit1_remez_pade_T9_9<float>";
entry.accuracy_test<float>(sin_unit1_remez_pade_T9_9<float>, std::sinf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_717, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin_unit1", "remez_pade", "float2x4", "simd",  };
entry.subName = "sin_unit1_remez_pade_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_unit1_remez_pade_T9_9<cr::simd::float2x4>, std::sinf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_718, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin_unit1", "remez_pade", "float", "simd",  };
entry.subName = "sin_unit1_remez_pade_T9_9_float_simd";
entry.accuracy_test<float>(sin_unit1_remez_pade_T9_9_float_simd, std::sinf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_719, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin_unit1", "float", "scalar",  };
entry.subName = "sin_unit1_quart_T9_9<float>";
entry.accuracy_test<float>(sin_unit1_quart_T9_9<float>, std::sinf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_720, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin_unit1", "float2x4", "simd",  };
entry.subName = "sin_unit1_quart_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_unit1_quart_T9_9<cr::simd::float2x4>, std::sinf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_721, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin_unit1", "float", "simd",  };
entry.subName = "sin_unit1_quart_T9_9_float_simd";
entry.accuracy_test<float>(sin_unit1_quart_T9_9_float_simd, std::sinf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_722, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin_unit2", "remez", "float", "scalar",  };
entry.subName = "sin_unit2_remez_fma_ec_T9_0<float>";
entry.accuracy_test<float>(sin_unit2_remez_fma_ec_T9_0<float>, std::sinf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_723, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin_unit2", "remez", "float2x4", "simd",  };
entry.subName = "sin_unit2_remez_fma_ec_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_unit2_remez_fma_ec_T9_0<cr::simd::float2x4>, std::sinf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_724, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin_unit2", "remez", "float", "simd",  };
entry.subName = "sin_unit2_remez_fma_ec_T9_0_float_simd";
entry.accuracy_test<float>(sin_unit2_remez_fma_ec_T9_0_float_simd, std::sinf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_725, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin_unit2", "remez_pade", "float", "scalar",  };
entry.subName = "sin_unit2_remez_pade_fma_ec_T9_9<float>";
entry.accuracy_test<float>(sin_unit2_remez_pade_fma_ec_T9_9<float>, std::sinf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_726, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin_unit2", "remez_pade", "float2x4", "simd",  };
entry.subName = "sin_unit2_remez_pade_fma_ec_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_unit2_remez_pade_fma_ec_T9_9<cr::simd::float2x4>, std::sinf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_727, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin_unit2", "remez_pade", "float", "simd",  };
entry.subName = "sin_unit2_remez_pade_fma_ec_T9_9_float_simd";
entry.accuracy_test<float>(sin_unit2_remez_pade_fma_ec_T9_9_float_simd, std::sinf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_728, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin_unit2", "float", "scalar",  };
entry.subName = "sin_unit2_quart_fma_ec_T9_9<float>";
entry.accuracy_test<float>(sin_unit2_quart_fma_ec_T9_9<float>, std::sinf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_729, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin_unit2", "float2x4", "simd",  };
entry.subName = "sin_unit2_quart_fma_ec_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_unit2_quart_fma_ec_T9_9<cr::simd::float2x4>, std::sinf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_730, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "sin_unit2", "float", "simd",  };
entry.subName = "sin_unit2_quart_fma_ec_T9_9_float_simd";
entry.accuracy_test<float>(sin_unit2_quart_fma_ec_T9_9_float_simd, std::sinf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_731, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin_unit2", "remez", "float", "scalar",  };
entry.subName = "sin_unit2_remez_fma_T9_0<float>";
entry.accuracy_test<float>(sin_unit2_remez_fma_T9_0<float>, std::sinf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_732, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin_unit2", "remez", "float2x4", "simd",  };
entry.subName = "sin_unit2_remez_fma_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_unit2_remez_fma_T9_0<cr::simd::float2x4>, std::sinf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_733, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin_unit2", "remez", "float", "simd",  };
entry.subName = "sin_unit2_remez_fma_T9_0_float_simd";
entry.accuracy_test<float>(sin_unit2_remez_fma_T9_0_float_simd, std::sinf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_734, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin_unit2", "remez_pade", "float", "scalar",  };
entry.subName = "sin_unit2_remez_pade_fma_T9_9<float>";
entry.accuracy_test<float>(sin_unit2_remez_pade_fma_T9_9<float>, std::sinf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_735, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin_unit2", "remez_pade", "float2x4", "simd",  };
entry.subName = "sin_unit2_remez_pade_fma_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_unit2_remez_pade_fma_T9_9<cr::simd::float2x4>, std::sinf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_736, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin_unit2", "remez_pade", "float", "simd",  };
entry.subName = "sin_unit2_remez_pade_fma_T9_9_float_simd";
entry.accuracy_test<float>(sin_unit2_remez_pade_fma_T9_9_float_simd, std::sinf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_737, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin_unit2", "float", "scalar",  };
entry.subName = "sin_unit2_quart_fma_T9_9<float>";
entry.accuracy_test<float>(sin_unit2_quart_fma_T9_9<float>, std::sinf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_738, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin_unit2", "float2x4", "simd",  };
entry.subName = "sin_unit2_quart_fma_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_unit2_quart_fma_T9_9<cr::simd::float2x4>, std::sinf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_739, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "sin_unit2", "float", "simd",  };
entry.subName = "sin_unit2_quart_fma_T9_9_float_simd";
entry.accuracy_test<float>(sin_unit2_quart_fma_T9_9_float_simd, std::sinf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_740, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin_unit2", "remez", "float", "scalar",  };
entry.subName = "sin_unit2_remez_ec_T9_0<float>";
entry.accuracy_test<float>(sin_unit2_remez_ec_T9_0<float>, std::sinf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_741, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin_unit2", "remez", "float2x4", "simd",  };
entry.subName = "sin_unit2_remez_ec_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_unit2_remez_ec_T9_0<cr::simd::float2x4>, std::sinf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_742, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin_unit2", "remez", "float", "simd",  };
entry.subName = "sin_unit2_remez_ec_T9_0_float_simd";
entry.accuracy_test<float>(sin_unit2_remez_ec_T9_0_float_simd, std::sinf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_743, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin_unit2", "remez_pade", "float", "scalar",  };
entry.subName = "sin_unit2_remez_pade_ec_T9_9<float>";
entry.accuracy_test<float>(sin_unit2_remez_pade_ec_T9_9<float>, std::sinf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_744, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin_unit2", "remez_pade", "float2x4", "simd",  };
entry.subName = "sin_unit2_remez_pade_ec_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_unit2_remez_pade_ec_T9_9<cr::simd::float2x4>, std::sinf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_745, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin_unit2", "remez_pade", "float", "simd",  };
entry.subName = "sin_unit2_remez_pade_ec_T9_9_float_simd";
entry.accuracy_test<float>(sin_unit2_remez_pade_ec_T9_9_float_simd, std::sinf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_746, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin_unit2", "float", "scalar",  };
entry.subName = "sin_unit2_quart_ec_T9_9<float>";
entry.accuracy_test<float>(sin_unit2_quart_ec_T9_9<float>, std::sinf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_747, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin_unit2", "float2x4", "simd",  };
entry.subName = "sin_unit2_quart_ec_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_unit2_quart_ec_T9_9<cr::simd::float2x4>, std::sinf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_748, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "sin_unit2", "float", "simd",  };
entry.subName = "sin_unit2_quart_ec_T9_9_float_simd";
entry.accuracy_test<float>(sin_unit2_quart_ec_T9_9_float_simd, std::sinf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_749, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin_unit2", "remez", "float", "scalar",  };
entry.subName = "sin_unit2_remez_T9_0<float>";
entry.accuracy_test<float>(sin_unit2_remez_T9_0<float>, std::sinf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_750, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin_unit2", "remez", "float2x4", "simd",  };
entry.subName = "sin_unit2_remez_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(sin_unit2_remez_T9_0<cr::simd::float2x4>, std::sinf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_751, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin_unit2", "remez", "float", "simd",  };
entry.subName = "sin_unit2_remez_T9_0_float_simd";
entry.accuracy_test<float>(sin_unit2_remez_T9_0_float_simd, std::sinf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_752, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "sin_unit2", "remez_pade", "float", "scalar",  };
entry.subName =
 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "remez", "float2x4", "simd",  };
entry.subName = "cos_unit2_remez_ec_T3_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_remez_ec_T3_0<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_815, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "remez", "float", "simd",  };
entry.subName = "cos_unit2_remez_ec_T3_0_float_simd";
entry.accuracy_test<float>(cos_unit2_remez_ec_T3_0_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_816, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "remez_pade", "float", "scalar",  };
entry.subName = "cos_unit2_remez_pade_ec_T3_0<float>";
entry.accuracy_test<float>(cos_unit2_remez_pade_ec_T3_0<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_817, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_unit2_remez_pade_ec_T3_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_remez_pade_ec_T3_0<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_818, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "remez_pade", "float", "simd",  };
entry.subName = "cos_unit2_remez_pade_ec_T3_0_float_simd";
entry.accuracy_test<float>(cos_unit2_remez_pade_ec_T3_0_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_819, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "float", "scalar",  };
entry.subName = "cos_unit2_quart_ec_T3_3<float>";
entry.accuracy_test<float>(cos_unit2_quart_ec_T3_3<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_820, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "float2x4", "simd",  };
entry.subName = "cos_unit2_quart_ec_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_quart_ec_T3_3<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_821, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "float", "simd",  };
entry.subName = "cos_unit2_quart_ec_T3_3_float_simd";
entry.accuracy_test<float>(cos_unit2_quart_ec_T3_3_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_822, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "remez", "float", "scalar",  };
entry.subName = "cos_unit2_remez_T3_0<float>";
entry.accuracy_test<float>(cos_unit2_remez_T3_0<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_823, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "remez", "float2x4", "simd",  };
entry.subName = "cos_unit2_remez_T3_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_remez_T3_0<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_824, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "remez", "float", "simd",  };
entry.subName = "cos_unit2_remez_T3_0_float_simd";
entry.accuracy_test<float>(cos_unit2_remez_T3_0_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_825, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "remez_pade", "float", "scalar",  };
entry.subName = "cos_unit2_remez_pade_T3_0<float>";
entry.accuracy_test<float>(cos_unit2_remez_pade_T3_0<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_826, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_unit2_remez_pade_T3_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_remez_pade_T3_0<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_827, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "remez_pade", "float", "simd",  };
entry.subName = "cos_unit2_remez_pade_T3_0_float_simd";
entry.accuracy_test<float>(cos_unit2_remez_pade_T3_0_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_828, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "float", "scalar",  };
entry.subName = "cos_unit2_quart_T3_3<float>";
entry.accuracy_test<float>(cos_unit2_quart_T3_3<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_829, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "float2x4", "simd",  };
entry.subName = "cos_unit2_quart_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_quart_T3_3<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_830, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "float", "simd",  };
entry.subName = "cos_unit2_quart_T3_3_float_simd";
entry.accuracy_test<float>(cos_unit2_quart_T3_3_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_831, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "remez", "float", "scalar",  };
entry.subName = "cos_remez_fma_ec_T3_0<float>";
entry.accuracy_test<float>(cos_remez_fma_ec_T3_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_832, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "remez", "float2x4", "simd",  };
entry.subName = "cos_remez_fma_ec_T3_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_fma_ec_T3_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_833, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "remez", "float", "simd",  };
entry.subName = "cos_remez_fma_ec_T3_0_float_simd";
entry.accuracy_test<float>(cos_remez_fma_ec_T3_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_834, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "remez_pade", "float", "scalar",  };
entry.subName = "cos_remez_pade_fma_ec_T3_0<float>";
entry.accuracy_test<float>(cos_remez_pade_fma_ec_T3_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_835, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_remez_pade_fma_ec_T3_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_pade_fma_ec_T3_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_836, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "remez_pade", "float", "simd",  };
entry.subName = "cos_remez_pade_fma_ec_T3_0_float_simd";
entry.accuracy_test<float>(cos_remez_pade_fma_ec_T3_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_837, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "float", "scalar",  };
entry.subName = "cos_quart_fma_ec_T3_3<float>";
entry.accuracy_test<float>(cos_quart_fma_ec_T3_3<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_838, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "float2x4", "simd",  };
entry.subName = "cos_quart_fma_ec_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_quart_fma_ec_T3_3<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_839, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "float", "simd",  };
entry.subName = "cos_quart_fma_ec_T3_3_float_simd";
entry.accuracy_test<float>(cos_quart_fma_ec_T3_3_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_840, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "remez", "float", "scalar",  };
entry.subName = "cos_remez_fma_T3_0<float>";
entry.accuracy_test<float>(cos_remez_fma_T3_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_841, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "remez", "float2x4", "simd",  };
entry.subName = "cos_remez_fma_T3_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_fma_T3_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_842, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "remez", "float", "simd",  };
entry.subName = "cos_remez_fma_T3_0_float_simd";
entry.accuracy_test<float>(cos_remez_fma_T3_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_843, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "remez_pade", "float", "scalar",  };
entry.subName = "cos_remez_pade_fma_T3_0<float>";
entry.accuracy_test<float>(cos_remez_pade_fma_T3_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_844, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_remez_pade_fma_T3_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_pade_fma_T3_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_845, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "remez_pade", "float", "simd",  };
entry.subName = "cos_remez_pade_fma_T3_0_float_simd";
entry.accuracy_test<float>(cos_remez_pade_fma_T3_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_846, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "float", "scalar",  };
entry.subName = "cos_quart_fma_T3_3<float>";
entry.accuracy_test<float>(cos_quart_fma_T3_3<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_847, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "float2x4", "simd",  };
entry.subName = "cos_quart_fma_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_quart_fma_T3_3<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_848, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "float", "simd",  };
entry.subName = "cos_quart_fma_T3_3_float_simd";
entry.accuracy_test<float>(cos_quart_fma_T3_3_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_849, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "remez", "float", "scalar",  };
entry.subName = "cos_remez_ec_T3_0<float>";
entry.accuracy_test<float>(cos_remez_ec_T3_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_850, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "remez", "float2x4", "simd",  };
entry.subName = "cos_remez_ec_T3_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_ec_T3_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_851, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "remez", "float", "simd",  };
entry.subName = "cos_remez_ec_T3_0_float_simd";
entry.accuracy_test<float>(cos_remez_ec_T3_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_852, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "remez_pade", "float", "scalar",  };
entry.subName = "cos_remez_pade_ec_T3_0<float>";
entry.accuracy_test<float>(cos_remez_pade_ec_T3_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_853, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_remez_pade_ec_T3_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_pade_ec_T3_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_854, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "remez_pade", "float", "simd",  };
entry.subName = "cos_remez_pade_ec_T3_0_float_simd";
entry.accuracy_test<float>(cos_remez_pade_ec_T3_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_855, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "float", "scalar",  };
entry.subName = "cos_quart_ec_T3_3<float>";
entry.accuracy_test<float>(cos_quart_ec_T3_3<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_856, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "float2x4", "simd",  };
entry.subName = "cos_quart_ec_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_quart_ec_T3_3<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_857, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "float", "simd",  };
entry.subName = "cos_quart_ec_T3_3_float_simd";
entry.accuracy_test<float>(cos_quart_ec_T3_3_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_858, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "remez", "float", "scalar",  };
entry.subName = "cos_remez_T3_0<float>";
entry.accuracy_test<float>(cos_remez_T3_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_859, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "remez", "float2x4", "simd",  };
entry.subName = "cos_remez_T3_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_T3_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_860, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "remez", "float", "simd",  };
entry.subName = "cos_remez_T3_0_float_simd";
entry.accuracy_test<float>(cos_remez_T3_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_861, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "remez_pade", "float", "scalar",  };
entry.subName = "cos_remez_pade_T3_0<float>";
entry.accuracy_test<float>(cos_remez_pade_T3_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_862, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_remez_pade_T3_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_pade_T3_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_863, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "remez_pade", "float", "simd",  };
entry.subName = "cos_remez_pade_T3_0_float_simd";
entry.accuracy_test<float>(cos_remez_pade_T3_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_864, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "float", "scalar",  };
entry.subName = "cos_quart_T3_3<float>";
entry.accuracy_test<float>(cos_quart_T3_3<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_865, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "float2x4", "simd",  };
entry.subName = "cos_quart_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_quart_T3_3<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_866, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "float", "simd",  };
entry.subName = "cos_quart_T3_3_float_simd";
entry.accuracy_test<float>(cos_quart_T3_3_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_867, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "remez", "float", "scalar",  };
entry.subName = "cos_unit1_remez_fma_ec_T4_0<float>";
entry.accuracy_test<float>(cos_unit1_remez_fma_ec_T4_0<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_868, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "remez", "float2x4", "simd",  };
entry.subName = "cos_unit1_remez_fma_ec_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_remez_fma_ec_T4_0<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_869, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "remez", "float", "simd",  };
entry.subName = "cos_unit1_remez_fma_ec_T4_0_float_simd";
entry.accuracy_test<float>(cos_unit1_remez
float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_936, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit2", "float", "scalar",  };
entry.subName = "cos_unit2_half_fma_T4_4<float>";
entry.accuracy_test<float>(cos_unit2_half_fma_T4_4<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_937, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit2", "float2x4", "simd",  };
entry.subName = "cos_unit2_half_fma_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_half_fma_T4_4<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_938, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit2", "float", "simd",  };
entry.subName = "cos_unit2_half_fma_T4_4_float_simd";
entry.accuracy_test<float>(cos_unit2_half_fma_T4_4_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_939, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "remez", "float", "scalar",  };
entry.subName = "cos_unit2_remez_ec_T4_0<float>";
entry.accuracy_test<float>(cos_unit2_remez_ec_T4_0<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_940, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "remez", "float2x4", "simd",  };
entry.subName = "cos_unit2_remez_ec_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_remez_ec_T4_0<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_941, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "remez", "float", "simd",  };
entry.subName = "cos_unit2_remez_ec_T4_0_float_simd";
entry.accuracy_test<float>(cos_unit2_remez_ec_T4_0_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_942, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "remez_pade", "float", "scalar",  };
entry.subName = "cos_unit2_remez_pade_ec_T4_0<float>";
entry.accuracy_test<float>(cos_unit2_remez_pade_ec_T4_0<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_943, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_unit2_remez_pade_ec_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_remez_pade_ec_T4_0<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_944, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "remez_pade", "float", "simd",  };
entry.subName = "cos_unit2_remez_pade_ec_T4_0_float_simd";
entry.accuracy_test<float>(cos_unit2_remez_pade_ec_T4_0_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_945, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "float", "scalar",  };
entry.subName = "cos_unit2_quart_ec_T4_4<float>";
entry.accuracy_test<float>(cos_unit2_quart_ec_T4_4<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_946, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "float2x4", "simd",  };
entry.subName = "cos_unit2_quart_ec_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_quart_ec_T4_4<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_947, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "float", "simd",  };
entry.subName = "cos_unit2_quart_ec_T4_4_float_simd";
entry.accuracy_test<float>(cos_unit2_quart_ec_T4_4_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_948, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "float", "scalar",  };
entry.subName = "cos_unit2_half_ec_T4_4<float>";
entry.accuracy_test<float>(cos_unit2_half_ec_T4_4<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_949, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "float2x4", "simd",  };
entry.subName = "cos_unit2_half_ec_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_half_ec_T4_4<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_950, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "float", "simd",  };
entry.subName = "cos_unit2_half_ec_T4_4_float_simd";
entry.accuracy_test<float>(cos_unit2_half_ec_T4_4_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_951, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "remez", "float", "scalar",  };
entry.subName = "cos_unit2_remez_T4_0<float>";
entry.accuracy_test<float>(cos_unit2_remez_T4_0<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_952, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "remez", "float2x4", "simd",  };
entry.subName = "cos_unit2_remez_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_remez_T4_0<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_953, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "remez", "float", "simd",  };
entry.subName = "cos_unit2_remez_T4_0_float_simd";
entry.accuracy_test<float>(cos_unit2_remez_T4_0_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_954, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "remez_pade", "float", "scalar",  };
entry.subName = "cos_unit2_remez_pade_T4_0<float>";
entry.accuracy_test<float>(cos_unit2_remez_pade_T4_0<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_955, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_unit2_remez_pade_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_remez_pade_T4_0<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_956, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "remez_pade", "float", "simd",  };
entry.subName = "cos_unit2_remez_pade_T4_0_float_simd";
entry.accuracy_test<float>(cos_unit2_remez_pade_T4_0_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_957, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "float", "scalar",  };
entry.subName = "cos_unit2_quart_T4_4<float>";
entry.accuracy_test<float>(cos_unit2_quart_T4_4<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_958, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "float2x4", "simd",  };
entry.subName = "cos_unit2_quart_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_quart_T4_4<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_959, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "float", "simd",  };
entry.subName = "cos_unit2_quart_T4_4_float_simd";
entry.accuracy_test<float>(cos_unit2_quart_T4_4_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_960, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "float", "scalar",  };
entry.subName = "cos_unit2_half_T4_4<float>";
entry.accuracy_test<float>(cos_unit2_half_T4_4<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_961, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "float2x4", "simd",  };
entry.subName = "cos_unit2_half_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_half_T4_4<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_962, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "float", "simd",  };
entry.subName = "cos_unit2_half_T4_4_float_simd";
entry.accuracy_test<float>(cos_unit2_half_T4_4_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_963, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "remez", "float", "scalar",  };
entry.subName = "cos_remez_fma_ec_T4_0<float>";
entry.accuracy_test<float>(cos_remez_fma_ec_T4_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_964, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "remez", "float2x4", "simd",  };
entry.subName = "cos_remez_fma_ec_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_fma_ec_T4_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_965, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "remez", "float", "simd",  };
entry.subName = "cos_remez_fma_ec_T4_0_float_simd";
entry.accuracy_test<float>(cos_remez_fma_ec_T4_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_966, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "remez_pade", "float", "scalar",  };
entry.subName = "cos_remez_pade_fma_ec_T4_0<float>";
entry.accuracy_test<float>(cos_remez_pade_fma_ec_T4_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_967, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_remez_pade_fma_ec_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_pade_fma_ec_T4_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_968, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "remez_pade", "float", "simd",  };
entry.subName = "cos_remez_pade_fma_ec_T4_0_float_simd";
entry.accuracy_test<float>(cos_remez_pade_fma_ec_T4_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_969, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "float", "scalar",  };
entry.subName = "cos_quart_fma_ec_T4_4<float>";
entry.accuracy_test<float>(cos_quart_fma_ec_T4_4<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_970, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "float2x4", "simd",  };
entry.subName = "cos_quart_fma_ec_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_quart_fma_ec_T4_4<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_971, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "float", "simd",  };
entry.subName = "cos_quart_fma_ec_T4_4_float_simd";
entry.accuracy_test<float>(cos_quart_fma_ec_T4_4_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_972, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "float", "scalar",  };
entry.subName = "cos_half_fma_ec_T4_4<float>";
entry.accuracy_test<float>(cos_half_fma_ec_T4_4<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_973, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "float2x4", "simd",  };
entry.subName = "cos_half_fma_ec_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_half_fma_ec_T4_4<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_974, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "float", "simd",  };
entry.subName = "cos_half_fma_ec_T4_4_float_simd";
entry.accuracy_test<float>(cos_half_fma_ec_T4_4_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_975, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "remez", "float", "scalar",  };
entry.subName = "cos_remez_fma_T4_0<float>";
entry.accuracy_test<float>(cos_remez_fma_T4_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_976, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "remez", "float2x4", "simd",  };
entry.subName = "cos_remez_fma_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_fma_T4_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_977, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "remez", "float", "simd",  };
entry.subName = "cos_remez_fma_T4_0_float_simd";
entry.accuracy_test<float>(cos_remez_fma_T4_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_978, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "remez_pade", "float", "scalar",  };
entry.subName = "cos_remez_pade_fma_T4_0<float>";
entry.accuracy_test<float>(cos_remez_pade_fma_T4_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_979, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_remez_pade_fma_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_pade_fma_T4_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_980, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "remez_pade", "float", "simd",  };
entry.subName = "cos_remez_pade_fma_T4_0_float_simd";
entry.accuracy_test<float>(cos_remez_pade_fma_T4_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_981, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "float", "scalar",  };
entry.subName = "cos_quart_fma_T4_4<float>";
entry.accuracy_test<float>(cos_quart_fma_T4_4<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_982, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "float2x4", "simd",  };
entry.subName = "cos_quart_fma_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_quart_fma_T4_4<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_983, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "float", "simd",  };
entry.subName = "cos_quart_fma_T4_4_float_simd";
entry.accuracy_test<float>(cos_quart_fma_T4_4_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_984, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "float", "scalar",  };
entry.subName = "cos_half_fma_T4_4<float>";
entry.accuracy_test<float>(cos_half_fma_T4_4<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_985, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "float2x4", "simd",  };
entry.subName = "cos_half_fma_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_half_fma_T4_4<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_986, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "float", "simd",  };
entry.subName = "cos_half_fma_T4_4_float_simd";
entry.accuracy_test<float>(cos_half_fma_T4_4_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_987, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "remez", "float", "scalar",  };
entry.subName = "cos_remez_ec_T4_0<float>";
entry.accuracy_test<float>(cos_remez_ec_T4_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_988, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "remez", "float2x4", "simd",  };
entry.subName = "cos_remez_ec_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_ec_T4_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_989, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "remez", "float", "simd",  };
entry.subName = "cos_remez_ec_T4_0_float_simd";
entry.accuracy_test<float>(cos_remez_ec_T4_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_990, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "remez_pade", "float", "scalar",  };
entry.subName = "cos_remez_pade_ec_T4_0<float>";
entry.accuracy_test<float>(cos_remez_pade_ec_T4_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_991, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_remez_pade_ec_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_pade_ec_T4_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_992, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "remez_pade", "float", "simd",  };
entry.subName = "cos_remez_pade_ec_T4_0_float_simd";
entry.accuracy_test<float>(cos_remez_pade_ec_T4_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_993, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "float", "scalar",  };
entry.subName = "cos_quart_ec_T4_4<float>";
entry.accuracy_test<float>(cos_quart_ec_T4_4<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_994, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "float2x4", "simd",  };
entry.subName = "cos_quart_ec_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_quart_ec_T4_4<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_995, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "float", "simd",  };
entry.subName = "cos_quart_ec_T4_4_float_simd";
entry.accuracy_test<float>(cos_quart_ec_T4_4_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_996, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "float", "scalar",  };
entry.subName = "cos_half_ec_T4_4<float>";
entry.accuracy_test<float>(cos_half_ec_T4_4<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_997, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "float2x4", "simd",  };
entry.subName = "cos_half_ec_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_half_ec_T4_4<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_998, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "float", "simd",  };
entry.subName = "cos_half_ec_T4_4_float_simd";
entry.accuracy_test<float>(cos_half_ec_T4_4_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_999, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "remez", "float", "scalar",  };
entry.subName = "cos_remez_T4_0<float>";
entry.accuracy_test<float>(cos_remez_T4_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1000, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "remez", "float2x4", "simd",  };
entry.subName = "cos_remez_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_T4_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1001, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "remez", "float", "simd",  };
entry.subName = "cos_remez_T4_0_float_simd";
entry.accuracy_test<float>(cos_remez_T4_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1002, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "remez_pade", "float", "scalar",  };
entry.subName = "cos_remez_pade_T4_0<float>";
entry.accuracy_test<float>(cos_remez_pade_T4_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1003, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_remez_pade_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_pade_T4_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1004, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "remez_pade", "float", "simd",  };
entry.subName = "cos_remez_pade_T4_0_float_simd";
entry.accuracy_test<float>(cos_remez_pade_T4_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1005, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "float", "scalar",  };
entry.subName = "cos_quart_T4_4<float>";
entry.accuracy_test<float>(cos_quart_T4_4<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1006, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "float2x4", "simd",  };
entry.subName = "cos_quart_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_quart_T4_4<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1007, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "float", "simd",  };
entry.subName = "cos_quart_T4_4_float_simd";
entry.accuracy_test<float>(cos_quart_T4_4_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1008, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "float", "scalar",  };
entry.subName = "cos_half_T4_4<float>";
entry.accuracy_test<float>(cos_half_T4_4<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1009, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "float2x4", "simd",  };
entry.subName = "cos_half_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_half_T4_4<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1010, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "float", "simd",  };
entry.subName = "cos_half_T4_4_float_simd";
entry.accuracy_test<float>(cos_half_T4_4_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1011, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "remez", "float", "scalar",  };
entry.subName = "cos_unit1_remez_fma_ec_T5_0<float>";
entry.accuracy_test<float>(cos_unit1_remez_fma_ec_T5_0<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1012, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "remez", "float2x4", "simd",  };
entry.subName = "cos_unit1_remez_fma_ec_T5_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_remez_fma_ec_T5_0<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1013, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "remez", "float", "simd",  };
entry.subName = "cos_unit1_remez_fma_ec_T5_0_float_simd";
entry.accuracy_test<float>(cos_unit1_remez_fma_ec_T5_0_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1014, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "remez_pade", "float", "scalar",  };
entry.subName = "cos_unit1_remez_pade_fma_ec_T5_0<float>";
entry.accuracy_test<float>(cos_unit1_remez_pade_fma_ec_T5_0<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1015, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_unit1_remez_pade_fma_ec_T5_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_remez_pade_fma_ec_T5_0<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1016, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "remez_pade", "float", "simd",  };
entry.subName = "cos_unit1_remez_pade_fma_ec_T5_0_float_simd";
entry.accuracy_test<float>(cos_unit1_remez_pade_fma_ec_T5_0_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1017, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "float", "scalar",  };
entry.subName = "cos_unit1_quart_fma_ec_T5_5<float>";
entry.accuracy_test<float>(cos_unit1_quart_fma_ec_T5_5<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1018, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "float2x4", "simd",  };
entry.subName = "cos_unit1_quart_fma_ec_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_quart_fma_ec_T5_5<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1019, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "float", "simd",  };
entry.subName = "cos_unit1_quart_fma_ec_T5_5_float_simd";
entry.accuracy_test<float>(cos_unit1_quart_fma_ec_T5_5_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1020, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "remez", "float", "scalar",  };
entry.subName = "cos_unit1_remez_fma_T5_0<float>";
entry.accuracy_test<float>(cos_unit1_remez_fma_T5_0<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1021, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "remez", "float2x4", "simd",  };
entry.subName = "cos_unit1_remez_fma_T5_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_remez_fma_T5_0<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1022, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "remez", "float", "simd",  };
entry.subName = "cos_unit1_remez_fma_T5_0_float_simd";
entry.accuracy_test<float>(cos_unit1_remez_fma_T5_0_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1023, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "remez_pade", "float", "scalar",  };
entry.subName = "cos_unit1_remez_pade_fma_T5_0<float>";
entry.accuracy_test<float>(cos_unit1_remez_pade_fma_T5_0<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1024, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_unit1_remez_pade_fma_T5_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_remez_pade_fma_T5_0<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1025, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "remez_pade", "float", "simd",  };
entry.subName = "cos_unit1_remez_pade_fma_T5_0_float_simd";
entry.accuracy_test<float>(cos_unit1_remez_pade_fma_T5_0_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1026, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "float", "scalar",  };
entry.subName = "cos_unit1_quart_fma_T5_5<float>";
entry.accuracy_test<float>(cos_unit1_quart_fma_T5_5<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1027, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "float2x4", "simd",  };
entry.subName = "cos_unit1_quart_fma_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_quart_fma_T5_5<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1028, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "float", "simd",  };
entry.subName = "cos_unit1_quart_fma_T5_5_float_simd";
entry.accuracy_test<float>(cos_unit1_quart_fma_T5_5_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1029, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit1", "remez", "float", "scalar",  };
entry.subName = "cos_unit1_remez_ec_T5_0<float>";
entry.accuracy_test<float>(cos_unit1_remez_ec_T5_0<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1030, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit1", "remez", "float2x4", "simd",  };
entry.subName = "cos_unit1_remez_ec_T5_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_remez_ec_T5_0<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1031, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit1", "remez", "float", "simd",  };
entry.subName = "cos_unit1_remez_ec_T5_0_float_simd";
entry.accuracy_test<float>(cos_unit1_remez_ec_T5_0_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1032, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit1", "remez_pade", "float", "scalar",  };
entry.subName = "cos_unit1_remez_pade_ec_T5_0<float>";
entry.accuracy_test<float>(cos_unit1_remez_pade_ec_T5_0<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1033, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit1", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_unit1_remez_pade_ec_T5_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_remez_pade_ec_T5_0<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1034, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit1", "remez_pade", "float", "simd",  };
entry.subName = "cos_unit1_remez_pade_ec_T5_0_float_simd";
entry.accuracy_test<float>(cos_unit1_remez_pade_ec_T5_0_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1035, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit1", "float", "scalar",  };
entry.subName = "cos_unit1_quart_ec_T5_5<float>";
entry.accuracy_test<float>(cos_unit1_quart_ec_T5_5<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1036, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit1", "float2x4", "simd",  };
entry.subName = "cos_unit1_quart_ec_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_quart_ec_T5_5<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1037, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit1", "float", "simd",  };
entry.subName = "cos_unit1_quart_ec_T5_5_float_simd";
entry.accuracy_test<float>(cos_unit1_quart_ec_T5_5_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1038, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit1", "remez", "float", "scalar",  };
entry.subName = "cos_unit1_remez_T5_0<float>";
entry.accuracy_test<float>(cos_unit1_remez_T5_0<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1039, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit1", "remez", "float2x4", "simd",  };
entry.subName = "cos_unit1_remez_T5_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_remez_T5_0<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1040, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit1", "remez", "float", "simd",  };
entry.subName = "cos_unit1_remez_T5_0_float_simd";
entry.accuracy_test<float>(cos_unit1_remez_T5_0_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1041, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit1", "remez_pade", "float", "scalar",  };
entry.subName = "cos_unit1_remez_pade_T5_0<float>";
entry.accuracy_test<float>(cos_unit1_remez_pade_T5_0<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1042, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit1", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_unit1_remez_pade_T5_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_remez_pade_T5_0<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1043, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit1", "remez_pade", "float", "simd",  };
entry.subName = "cos_unit1_remez_pade_T5_0_float_simd";
entry.accuracy_test<float>(cos_unit1_remez_pade_T5_0_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1044, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit1", "float", "scalar",  };
entry.subName = "cos_unit1_quart_T5_5<float>";
entry.accuracy_test<float>(cos_unit1_quart_T5_5<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1045, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit1", "float2x4", "simd",  };
entry.subName = "cos_unit1_quart_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_quart_T5_5<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1046, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit1", "float", "simd",  };
entry.subName = "cos_unit1_quart_T5_5_float_simd";
entry.accuracy_test<float>(cos_unit1_quart_T5_5_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1047, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit2", "remez", "float", "scalar",  };
entry.subName = "cos_unit2_remez_fma_ec_T5_0<float>";
entry.accuracy_test<float>(cos_unit2_remez_fma_ec_T5_0<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1048, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit2", "remez", "float2x4", "simd",  };
entry.subName = "cos_unit2_remez_fma_ec_T5_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_remez_fma_ec_T5_0<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_
 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "float", "simd",  };
entry.subName = "cos_quart_ec_T5_5_float_simd";
entry.accuracy_test<float>(cos_quart_ec_T5_5_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1110, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "remez", "float", "scalar",  };
entry.subName = "cos_remez_T5_0<float>";
entry.accuracy_test<float>(cos_remez_T5_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1111, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "remez", "float2x4", "simd",  };
entry.subName = "cos_remez_T5_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_T5_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1112, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "remez", "float", "simd",  };
entry.subName = "cos_remez_T5_0_float_simd";
entry.accuracy_test<float>(cos_remez_T5_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1113, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "remez_pade", "float", "scalar",  };
entry.subName = "cos_remez_pade_T5_0<float>";
entry.accuracy_test<float>(cos_remez_pade_T5_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1114, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_remez_pade_T5_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_pade_T5_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1115, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "remez_pade", "float", "simd",  };
entry.subName = "cos_remez_pade_T5_0_float_simd";
entry.accuracy_test<float>(cos_remez_pade_T5_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1116, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "float", "scalar",  };
entry.subName = "cos_quart_T5_5<float>";
entry.accuracy_test<float>(cos_quart_T5_5<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1117, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "float2x4", "simd",  };
entry.subName = "cos_quart_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_quart_T5_5<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1118, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "float", "simd",  };
entry.subName = "cos_quart_T5_5_float_simd";
entry.accuracy_test<float>(cos_quart_T5_5_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1119, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "remez", "float", "scalar",  };
entry.subName = "cos_unit1_remez_fma_ec_T6_0<float>";
entry.accuracy_test<float>(cos_unit1_remez_fma_ec_T6_0<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1120, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "remez", "float2x4", "simd",  };
entry.subName = "cos_unit1_remez_fma_ec_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_remez_fma_ec_T6_0<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1121, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "remez", "float", "simd",  };
entry.subName = "cos_unit1_remez_fma_ec_T6_0_float_simd";
entry.accuracy_test<float>(cos_unit1_remez_fma_ec_T6_0_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1122, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "remez_pade", "float", "scalar",  };
entry.subName = "cos_unit1_remez_pade_fma_ec_T6_0<float>";
entry.accuracy_test<float>(cos_unit1_remez_pade_fma_ec_T6_0<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1123, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_unit1_remez_pade_fma_ec_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_remez_pade_fma_ec_T6_0<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1124, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "remez_pade", "float", "simd",  };
entry.subName = "cos_unit1_remez_pade_fma_ec_T6_0_float_simd";
entry.accuracy_test<float>(cos_unit1_remez_pade_fma_ec_T6_0_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1125, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "float", "scalar",  };
entry.subName = "cos_unit1_quart_fma_ec_T6_6<float>";
entry.accuracy_test<float>(cos_unit1_quart_fma_ec_T6_6<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1126, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "float2x4", "simd",  };
entry.subName = "cos_unit1_quart_fma_ec_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_quart_fma_ec_T6_6<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1127, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "float", "simd",  };
entry.subName = "cos_unit1_quart_fma_ec_T6_6_float_simd";
entry.accuracy_test<float>(cos_unit1_quart_fma_ec_T6_6_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1128, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "float", "scalar",  };
entry.subName = "cos_unit1_half_fma_ec_T6_6<float>";
entry.accuracy_test<float>(cos_unit1_half_fma_ec_T6_6<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1129, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "float2x4", "simd",  };
entry.subName = "cos_unit1_half_fma_ec_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_half_fma_ec_T6_6<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1130, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "float", "simd",  };
entry.subName = "cos_unit1_half_fma_ec_T6_6_float_simd";
entry.accuracy_test<float>(cos_unit1_half_fma_ec_T6_6_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1131, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "remez", "float", "scalar",  };
entry.subName = "cos_unit1_remez_fma_T6_0<float>";
entry.accuracy_test<float>(cos_unit1_remez_fma_T6_0<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1132, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "remez", "float2x4", "simd",  };
entry.subName = "cos_unit1_remez_fma_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_remez_fma_T6_0<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1133, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "remez", "float", "simd",  };
entry.subName = "cos_unit1_remez_fma_T6_0_float_simd";
entry.accuracy_test<float>(cos_unit1_remez_fma_T6_0_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1134, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "remez_pade", "float", "scalar",  };
entry.subName = "cos_unit1_remez_pade_fma_T6_0<float>";
entry.accuracy_test<float>(cos_unit1_remez_pade_fma_T6_0<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1135, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_unit1_remez_pade_fma_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_remez_pade_fma_T6_0<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1136, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "remez_pade", "float", "simd",  };
entry.subName = "cos_unit1_remez_pade_fma_T6_0_float_simd";
entry.accuracy_test<float>(cos_unit1_remez_pade_fma_T6_0_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1137, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "float", "scalar",  };
entry.subName = "cos_unit1_quart_fma_T6_6<float>";
entry.accuracy_test<float>(cos_unit1_quart_fma_T6_6<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1138, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "float2x4", "simd",  };
entry.subName = "cos_unit1_quart_fma_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_quart_fma_T6_6<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1139, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "float", "simd",  };
entry.subName = "cos_unit1_quart_fma_T6_6_float_simd";
entry.accuracy_test<float>(cos_unit1_quart_fma_T6_6_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1140, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "float", "scalar",  };
entry.subName = "cos_unit1_half_fma_T6_6<float>";
entry.accuracy_test<float>(cos_unit1_half_fma_T6_6<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1141, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "float2x4", "simd",  };
entry.subName = "cos_unit1_half_fma_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_half_fma_T6_6<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1142, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "float", "simd",  };
entry.subName = "cos_unit1_half_fma_T6_6_float_simd";
entry.accuracy_test<float>(cos_unit1_half_fma_T6_6_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1143, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit1", "remez", "float", "scalar",  };
entry.subName = "cos_unit1_remez_ec_T6_0<float>";
entry.accuracy_test<float>(cos_unit1_remez_ec_T6_0<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1144, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit1", "remez", "float2x4", "simd",  };
entry.subName = "cos_unit1_remez_ec_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_remez_ec_T6_0<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1145, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit1", "remez", "float", "simd",  };
entry.subName = "cos_unit1_remez_ec_T6_0_float_simd";
entry.accuracy_test<float>(cos_unit1_remez_ec_T6_0_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1146, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit1", "remez_pade", "float", "scalar",  };
entry.subName = "cos_unit1_remez_pade_ec_T6_0<float>";
entry.accuracy_test<float>(cos_unit1_remez_pade_ec_T6_0<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1147, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit1", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_unit1_remez_pade_ec_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_remez_pade_ec_T6_0<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1148, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit1", "remez_pade", "float", "simd",  };
entry.subName = "cos_unit1_remez_pade_ec_T6_0_float_simd";
entry.accuracy_test<float>(cos_unit1_remez_pade_ec_T6_0_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1149, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit1", "float", "scalar",  };
entry.subName = "cos_unit1_quart_ec_T6_6<float>";
entry.accuracy_test<float>(cos_unit1_quart_ec_T6_6<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1150, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit1", "float2x4", "simd",  };
entry.subName = "cos_unit1_quart_ec_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_quart_ec_T6_6<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1151, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit1", "float", "simd",  };
entry.subName = "cos_unit1_quart_ec_T6_6_float_simd";
entry.accuracy_test<float>(cos_unit1_quart_ec_T6_6_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1152, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit1", "float", "scalar",  };
entry.subName = "cos_unit1_half_ec_T6_6<float>";
entry.accuracy_test<float>(cos_unit1_half_ec_T6_6<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1153, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit1", "float2x4", "simd",  };
entry.subName = "cos_unit1_half_ec_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_half_ec_T6_6<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1154, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit1", "float", "simd",  };
entry.subName = "cos_unit1_half_ec_T6_6_float_simd";
entry.accuracy_test<float>(cos_unit1_half_ec_T6_6_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1155, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit1", "remez", "float", "scalar",  };
entry.subName = "cos_unit1_remez_T6_0<float>";
entry.accuracy_test<float>(cos_unit1_remez_T6_0<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1156, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit1", "remez", "float2x4", "simd",  };
entry.subName = "cos_unit1_remez_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_remez_T6_0<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1157, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit1", "remez", "float", "simd",  };
entry.subName = "cos_unit1_remez_T6_0_float_simd";
entry.accuracy_test<float>(cos_unit1_remez_T6_0_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1158, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit1", "remez_pade", "float", "scalar",  };
entry.subName = "cos_unit1_remez_pade_T6_0<float>";
entry.accuracy_test<float>(cos_unit1_remez_pade_T6_0<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1159, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit1", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_unit1_remez_pade_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_remez_pade_T6_0<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1160, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit1", "remez_pade", "float", "simd",  };
entry.subName = "cos_unit1_remez_pade_T6_0_float_simd";
entry.accuracy_test<float>(cos_unit1_remez_pade_T6_0_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1161, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit1", "float", "scalar",  };
entry.subName = "cos_unit1_quart_T6_6<float>";
entry.accuracy_test<float>(cos_unit1_quart_T6_6<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1162, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit1", "float2x4", "simd",  };
entry.subName = "cos_unit1_quart_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_quart_T6_6<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1163, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit1", "float", "simd",  };
entry.subName = "cos_unit1_quart_T6_6_float_simd";
entry.accuracy_test<float>(cos_unit1_quart_T6_6_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1164, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit1", "float", "scalar",  };
entry.subName = "cos_unit1_half_T6_6<float>";
entry.accuracy_test<float>(cos_unit1_half_T6_6<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1165, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit1", "float2x4", "simd",  };
entry.subName = "cos_unit1_half_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_half_T6_6<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1166, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit1", "float", "simd",  };
entry.subName = "cos_unit1_half_T6_6_float_simd";
entry.accuracy_test<float>(cos_unit1_half_T6_6_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1167, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit2", "remez", "float", "scalar",  };
entry.subName = "cos_unit2_remez_fma_ec_T6_0<float>";
entry.accuracy_test<float>(cos_unit2_remez_fma_ec_T6_0<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1168, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit2", "remez", "float2x4", "simd",  };
entry.subName = "cos_unit2_remez_fma_ec_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_remez_fma_ec_T6_0<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1169, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit2", "remez", "float", "simd",  };
entry.subName = "cos_unit2_remez_fma_ec_T6_0_float_simd";
entry.accuracy_test<float>(cos_unit2_remez_fma_ec_T6_0_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1170, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit2", "remez_pade", "float", "scalar",  };
entry.subName = "cos_unit2_remez_pade_fma_ec_T6_0<float>";
entry.accuracy_test<float>(cos_unit2_remez_pade_fma_ec_T6_0<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1171, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit2", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_unit2_remez_pade_fma_ec_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_remez_pade_fma_ec_T6_0<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1172, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit2", "remez_pade", "float", "simd",  };
entry.subName = "cos_unit2_remez_pade_fma_ec_T6_0_float_simd";
entry.accuracy_test<float>(cos_unit2_remez_pade_fma_ec_T6_0_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1173, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit2", "float", "scalar",  };
entry.subName = "cos_unit2_quart_fma_ec_T6_6<float>";
entry.accuracy_test<float>(cos_unit2_quart_fma_ec_T6_6<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1174, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit2", "float2x4", "simd",  };
entry.subName = "cos_unit2_quart_fma_ec_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_quart_fma_ec_T6_6<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1175, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit2", "float", "simd",  };
entry.subName = "cos_unit2_quart_fma_ec_T6_6_float_simd";
entry.accuracy_test<float>(cos_unit2_quart_fma_ec_T6_6_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1176, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit2", "float", "scalar",  };
entry.subName = "cos_unit2_half_fma_ec_T6_6<float>";
entry.accuracy_test<float>(cos_unit2_half_fma_ec_T6_6<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1177, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit2", "float2x4", "simd",  };
entry.subName = "cos_unit2_half_fma_ec_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_half_fma_ec_T6_6<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1178, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit2", "float", "simd",  };
entry.subName = "cos_unit2_half_fma_ec_T6_6_float_simd";
entry.accuracy_test<float>(cos_unit2_half_fma_ec_T6_6_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1179, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit2", "remez", "float", "scalar",  };
entry.subName = "cos_unit2_remez_fma_T6_0<float>";
entry.accuracy_test<float>(cos_unit2_remez_fma_T6_0<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1180, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit2", "remez", "float2x4", "simd",  };
entry.subName = "cos_unit2_remez_fma_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_remez_fma_T6_0<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1181, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit2", "remez", "float", "simd",  };
entry.subName = "cos_unit2_remez_fma_T6_0_float_simd";
entry.accuracy_test<float>(cos_unit2_remez_fma_T6_0_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1182, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit2", "remez_pade", "float", "scalar",  };
entry.subName = "cos_unit2_remez_pade_fma_T6_0<float>";
entry.accuracy_test<float>(cos_unit2_remez_pade_fma_T6_0<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1183, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit2", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_unit2_remez_pade_fma_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_remez_pade_fma_T6_0<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1184, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit2", "remez_pade", "float", "simd",  };
entry.subName = "cos_unit2_remez_pade_fma_T6_0_float_simd";
entry.accuracy_test<float>(cos_unit2_remez_pade_fma_T6_0_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1185, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit2", "float", "scalar",  };
entry.subName = "cos_unit2_quart_fma_T6_6<float>";
entry.accuracy_test<float>(cos_unit2_quart_fma_T6_6<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1186, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit2", "float2x4", "simd",  };
entry.subName = "cos_unit2_quart_fma_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_quart_fma_T6_6<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1187, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit2", "float", "simd",  };
entry.subName = "cos_unit2_quart_fma_T6_6_float_simd";
entry.accuracy_test<float>(cos_unit2_quart_fma_T6_6_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1188, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit2", "float", "scalar",  };
entry.subName = "cos_unit2_half_fma_T6_6<float>";
entry.accuracy_test<float>(cos_unit2_half_fma_T6_6<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1189, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit2", "float2x4", "simd",  };
entry.subName = "cos_unit2_half_fma_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_half_fma_T6_6<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1190, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit2", "float", "simd",  };
entry.subName = "cos_unit2_half_fma_T6_6_float_simd";
entry.accuracy_test<float>(cos_unit2_half_fma_T6_6_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1191, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "remez", "float", "scalar",  };
entry.subName = "cos_unit2_remez_ec_T6_0<float>";
entry.accuracy_test<float>(cos_unit2_remez_ec_T6_0<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1192, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "remez", "float2x4", "simd",  };
entry.subName = "cos_unit2_remez_ec_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_remez_ec_T6_0<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1193, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "remez", "float", "simd",  };
entry.subName = "cos_unit2_remez_ec_T6_0_float_simd";
entry.accuracy_test<float>(cos_unit2_remez_ec_T6_0_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1194, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "remez_pade", "float", "scalar",  };
entry.subName = "cos_unit2_remez_pade_ec_T6_0<float>";
entry.accuracy_test<float>(cos_unit2_remez_pade_ec_T6_0<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1195, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_unit2_remez_pade_ec_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_remez_pade_ec_T6_0<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1196, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "remez_pade", "float", "simd",  };
entry.subName = "cos_unit2_remez_pade_ec_T6_0_float_simd";
entry.accuracy_test<float>(cos_unit2_remez_pade_ec_T6_0_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1197, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "float", "scalar",  };
entry.subName = "cos_unit2_quart_ec_T6_6<float>";
entry.accuracy_test<float>(cos_unit2_quart_ec_T6_6<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1198, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "float2x4", "simd",  };
entry.subName = "cos_unit2_quart_ec_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_quart_ec_T6_6<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1199, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "float", "simd",  };
entry.subName = "cos_unit2_quart_ec_T6_6_float_simd";
entry.accuracy_test<float>(cos_unit2_quart_ec_T6_6_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1200, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "float", "scalar",  };
entry.subName = "cos_unit2_half_ec_T6_6<float>";
entry.accuracy_test<float>(cos_unit2_half_ec_T6_6<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1201, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "float2x4", "simd",  };
entry.subName = "cos_unit2_half_ec_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_half_ec_T6_6<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1202, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "float", "simd",  };
entry.subName = "cos_unit2_half_ec_T6_6_float_simd";
entry.accuracy_test<float>(cos_unit2_half_ec_T6_6_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1203, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "remez", "float", "scalar",  };
entry.subName = "cos_unit2_remez_T6_0<float>";
entry.accuracy_test<float>(cos_unit2_remez_T6_0<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1204, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "remez", "float2x4", "simd",  };
entry.subName = "cos_unit2_remez_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_remez_T6_0<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1205, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "remez", "float", "simd",  };
entry.subName = "cos_unit2_remez_T6_0_float_simd";
entry.accuracy_test<float>(cos_unit2_remez_T6_0_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1206, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "remez_pade", "float", "scalar",  };
entry.subName = "cos_unit2_remez_pade_T6_0<float>";
entry.accuracy_test<float>(cos_unit2_remez_pade_T6_0<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1207, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_unit2_remez_pade_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_remez_pade_T6_0<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1208, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "remez_pade", "float", "simd",  };
entry.subName = "cos_unit2_remez_pade_T6_0_float_simd";
entry.accuracy_test<float>(cos_unit2_remez_pade_T6_0_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1209, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "float", "scalar",  };
entry.subName = "cos_unit2_quart_T6_6<float>";
entry.accuracy_test<float>(cos_unit2_quart_T6_6<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1210, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "float2x4", "simd",  };
entry.subName = "cos_unit2_quart_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_quart_T6_6<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1211, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "float", "simd",  };
entry.subName = "cos_unit2_quart_T6_6_float_simd";
entry.accuracy_test<float>(cos_unit2_quart_T6_6_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1212, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "float", "scalar",  };
entry.subName = "cos_unit2_half_T6_6<float>";
entry.accuracy_test<float>(cos_unit2_half_T6_6<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1213, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "float2x4", "simd",  };
entry.subName = "cos_unit2_half_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_half_T6_6<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1214, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "float", "simd",  };
entry.subName = "cos_unit2_half_T6_6_float_simd";
entry.accuracy_test<float>(cos_unit2_half_T6_6_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1215, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "remez", "float", "scalar",  };
entry.subName = "cos_remez_fma_ec_T6_0<float>";
entry.accuracy_test<float>(cos_remez_fma_ec_T6_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1216, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "remez", "float2x4", "simd",  };
entry.subName = "cos_remez_fma_ec_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_fma_ec_T6_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1217, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "remez", "float", "simd",  };
entry.subName = "cos_remez_fma_ec_T6_0_float_simd";
entry.accuracy_test<float>(cos_remez_fma_ec_T6_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1218, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "remez_pade", "float", "scalar",  };
entry.subName = "cos_remez_pade_fma_ec_T6_0<float>";
entry.accuracy_test<float>(cos_remez_pade_fma_ec_T6_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1219, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_remez_pade_fma_ec_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_pade_fma_ec_T6_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1220, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "remez_pade", "float", "simd",  };
entry.subName = "cos_remez_pade_fma_ec_T6_0_float_simd";
entry.accuracy_test<float>(cos_remez_pade_fma_ec_T6_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1221, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "float", "scalar",  };
entry.subName = "cos_quart_fma_ec_T6_6<float>";
entry.accuracy_test<float>(cos_quart_fma_ec_T6_6<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1222, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "float2x4", "simd",  };
entry.subName = "cos_quart_fma_ec_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_quart_fma_ec_T6_6<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1223, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "float", "simd",  };
entry.subName = "cos_quart_fma_ec_T6_6_float_simd";
entry.accuracy_test<float>(cos_quart_fma_ec_T6_6_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1224, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "float", "scalar",  };
entry.subName = "cos_half_fma_ec_T6_6<float>";
entry.accuracy_test<float>(cos_half_fma_ec_T6_6<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1225, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "float2x4", "simd",  };
entry.subName = "cos_half_fma_ec_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_half_fma_ec_T6_6<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1226, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "float", "simd",  };
entry.subName = "cos_half_fma_ec_T6_6_float_simd";
entry.accuracy_test<float>(cos_half_fma_ec_T6_6_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1227, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "remez", "float", "scalar",  };
entry.subName = "cos_remez_fma_T6_0<float>";
entry.accuracy_test<float>(cos_remez_fma_T6_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1228, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "remez", "float2x4", "simd",  };
entry.subName = "cos_remez_fma_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_fma_T6_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1229, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "remez", "float", "simd",  };
entry.subName = "cos_remez_fma_T6_0_float_simd";
entry.accuracy_test<float>(cos_remez_fma_T6_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1230, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "remez_pade", "float", "scalar",  };
entry.subName = "cos_remez_pade_fma_T6_0<float>";
entry.accuracy_test<float>(cos_remez_pade_fma_T6_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1231, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_remez_pade_fma_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_pade_fma_T6_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1232, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "remez_pade", "float", "simd",  };
entry.subName = "cos_remez_pade_fma_T6_0_float_simd";
entry.accuracy_test<float>(cos_remez_pade_fma_T6_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1233, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "float", "scalar",  };
entry.subName = "cos_quart_fma_T6_6<float>";
entry.accuracy_test<float>(cos_quart_fma_T6_6<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1234, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "float2x4", "simd",  };
entry.subName = "cos_quart_fma_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_quart_fma_T6_6<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1235, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "float", "simd",  };
entry.subName = "cos_quart_fma_T6_6_float_simd";
entry.accuracy_test<float>(cos_quart_fma_T6_6_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1236, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "float", "scalar",  };
entry.subName = "cos_half_fma_T6_6<float>";
entry.accuracy_test<float>(cos_half_fma_T6_6<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1237, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "float2x4", "simd",  };
entry.subName = "cos_half_fma_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_half_fma_T6_6<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1238, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "float", "simd",  };
entry.subName = "cos_half_fma_T6_6_float_simd";
entry.accuracy_test<float>(cos_half_fma_T6_6_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1239, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "remez", "float", "scalar",  };
entry.subName = "cos_remez_ec_T6_0<float>";
entry.accuracy_test<float>(cos_remez_ec_T6_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1240, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "remez", "float2x4", "simd",  };
entry.subName = "cos_remez_ec_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_ec_T6_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1241, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "remez", "float", "simd",  };
entry.subName = "cos_remez_ec_T6_0_float_simd";
entry.accuracy_test<float>(cos_remez_ec_T6_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1242, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "remez_pade", "float", "scalar",  };
entry.subName = "cos_remez_pade_ec_T6_0<float>";
entry.accuracy_test<float>(cos_remez_pade_ec_T6_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1243, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_remez_pade_ec_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_pade_ec_T6_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1244, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "remez_pade", "float", "simd",  };
entry.subName = "cos_remez_pade_ec_T6_0_float_simd";
entry.accuracy_test<float>(cos_remez_pade_ec_T6_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1245, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "float", "scalar",  };
entry.subName = "cos_quart_ec_T6_6<float>";
entry.accuracy_test<float>(cos_quart_ec_T6_6<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1246, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "float2x4", "simd",  };
entry.subName = "cos_quart_ec_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_quart_ec_T6_6<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1247, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "float", "simd",  };
entry.subName = "cos_quart_ec_T6_6_float_simd";
entry.accuracy_test<float>(cos_quart_ec_T6_6_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1248, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "float", "scalar",  };
entry.subName = "cos_half_ec_T6_6<float>";
entry.accuracy_test<float>(cos_half_ec_T6_6<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1249, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "float2x4", "simd",  };
entry.subName = "cos_half_ec_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_half_ec_T6_6<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1250, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "float", "simd",  };
entry.subName = "cos_half_ec_T6_6_float_simd";
entry.accuracy_test<float>(cos_half_ec_T6_6_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1251, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "remez", "float", "scalar",  };
entry.subName = "cos_remez_T6_0<float>";
entry.accuracy_test<float>(cos_remez_T6_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1252, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "remez", "float2x4", "simd",  };
entry.subName = "cos_remez_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_T6_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1253, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "remez", "float", "simd",  };
entry.subName = "cos_remez_T6_0_float_simd";
entry.accuracy_test<float>(cos_remez_T6_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1254, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "remez_pade", "float", "scalar",  };
entry.subName = "cos_remez_pade_T6_0<float>";
entry.accuracy_test<float>(cos_remez_pade_T6_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1255, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_remez_pade_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_pade_T6_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1256, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "remez_pade", "float", "simd",  };
entry.subName = "cos_remez_pade_T6_0_float_simd";
entry.accuracy_test<float>(cos_remez_pade_T6_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1257, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "float", "scalar",  };
entry.subName = "cos_quart_T6_6<float>";
entry.accuracy_test<float>(cos_quart_T6_6<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1258, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "float2x4", "simd",  };
entry.subName = "cos_quart_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_quart_T6_6<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1259, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "float", "simd",  };
entry.subName = "cos_quart_T6_6_float_simd";
entry.accuracy_test<float>(cos_quart_T6_6_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1260, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "float", "scalar",  };
entry.subName = "cos_half_T6_6<float>";
entry.accuracy_test<float>(cos_half_T6_6<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1261, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "float2x4", "simd",  };
entry.subName = "cos_half_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_half_T6_6<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1262, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "float", "simd",  };
entry.subName = "cos_half_T6_6_float_simd";
entry.accuracy_test<float>(cos_half_T6_6_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1263, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "remez", "float", "scalar",  };
entry.subName = "cos_unit1_remez_fma_ec_T7_0<float>";
entry.accuracy_test<float>(cos_unit1_remez_fma_ec_T7_0<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1264, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "remez", "float2x4", "simd",  };
entry.subName = "cos_unit1_remez_fma_ec_T7_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_remez_fma_ec_T7_0<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1265, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "remez", "float", "simd",  };
entry.subName = "cos_unit1_remez_fma_ec_T7_0_float_simd";
entry.accuracy_test<float>(cos_unit1_remez_fma_ec_T7_0_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1266, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "remez_pade", "float", "scalar",  };
entry.subName = "cos_unit1_remez_pade_fma_ec_T7_0<float>";
entry.accuracy_test<float>(cos_unit1_remez_pade_fma_ec_T7_0<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1267, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_unit1_remez_pade_fma_ec_T7_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_remez_pade_fma_ec_T7_0<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1268, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "remez_pade", "float", "simd",  };
entry.subName = "cos_unit1_remez_pade_fma_ec_T7_0_float_simd";
entry.accuracy_test<float>(cos_unit1_remez_pade_fma_ec_T7_0_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1269, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "float", "scalar",  };
entry.subName = "cos_unit1_quart_fma_ec_T7_7<float>";
entry.accuracy_test<float>(cos_unit1_quart_fma_ec_T7_7<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1270, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "float2x4", "simd",  };
entry.subName = "cos_unit1_quart_fma_ec_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_quart_fma_ec_T7_7<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1271, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "float", "simd",  };
entry.subName = "cos_unit1_quart_fma_ec_T7_7_float_simd";
entry.accuracy_test<float>(cos_unit1_quart_fma_ec_T7_7_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1272, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "remez", "float", "scalar",  };
entry.subName = "cos_unit1_remez_fma_T7_0<float>";
entry.accuracy_test<float>(cos_unit1_remez_fma_T7_0<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1273, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "remez", "float2x4", "simd",  };
entry.subName = "cos_unit1_remez_fma_T7_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_remez_fma_T7_0<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1274, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "remez", "float", "simd",  };
entry.subName = "cos_unit1_remez_fma_T7_0_float_simd";
entry.accuracy_test<float>(cos_unit1_remez_fma_T7_0_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1275, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "remez_pade", "float", "scalar",  };
entry.subName = "cos_unit1_remez_pade_fma_T7_0<float>";
entry.accuracy_test<float>(cos_unit1_remez_pade_fma_T7_0<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1276, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_unit1_remez_pade_fma_T7_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_remez_pade_fma_T7_0<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1277, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "remez_pade", "float", "simd",  };
entry.subName = "cos_unit1_remez_pade_fma_T7_0_float_simd";
entry.accuracy_test<float>(cos_unit1_remez_pade_fma_T7_0_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1278, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "float", "scalar",  };
entry.subName = "cos_unit1_quart_fma_T7_7<float>";
entry.accuracy_test<float>(cos_unit1_quart_fma_T7_7<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1279, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "float2x4", "simd",  };
entry.subName = "cos_unit1_quart_fma_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_quart_fma_T7_7<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1280, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "float", "simd",  };
entry.subName = "cos_unit1_quart_fma_T7_7_float_simd";
entry.accuracy_test<float>(cos_unit1_quart
 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "float", "scalar",  };
entry.subName = "cos_quart_fma_T7_7<float>";
entry.accuracy_test<float>(cos_quart_fma_T7_7<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1351, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "float2x4", "simd",  };
entry.subName = "cos_quart_fma_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_quart_fma_T7_7<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1352, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "float", "simd",  };
entry.subName = "cos_quart_fma_T7_7_float_simd";
entry.accuracy_test<float>(cos_quart_fma_T7_7_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1353, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "remez", "float", "scalar",  };
entry.subName = "cos_remez_ec_T7_0<float>";
entry.accuracy_test<float>(cos_remez_ec_T7_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1354, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "remez", "float2x4", "simd",  };
entry.subName = "cos_remez_ec_T7_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_ec_T7_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1355, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "remez", "float", "simd",  };
entry.subName = "cos_remez_ec_T7_0_float_simd";
entry.accuracy_test<float>(cos_remez_ec_T7_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1356, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "remez_pade", "float", "scalar",  };
entry.subName = "cos_remez_pade_ec_T7_0<float>";
entry.accuracy_test<float>(cos_remez_pade_ec_T7_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1357, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_remez_pade_ec_T7_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_pade_ec_T7_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1358, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "remez_pade", "float", "simd",  };
entry.subName = "cos_remez_pade_ec_T7_0_float_simd";
entry.accuracy_test<float>(cos_remez_pade_ec_T7_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1359, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "float", "scalar",  };
entry.subName = "cos_quart_ec_T7_7<float>";
entry.accuracy_test<float>(cos_quart_ec_T7_7<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1360, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "float2x4", "simd",  };
entry.subName = "cos_quart_ec_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_quart_ec_T7_7<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1361, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "float", "simd",  };
entry.subName = "cos_quart_ec_T7_7_float_simd";
entry.accuracy_test<float>(cos_quart_ec_T7_7_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1362, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "remez", "float", "scalar",  };
entry.subName = "cos_remez_T7_0<float>";
entry.accuracy_test<float>(cos_remez_T7_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1363, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "remez", "float2x4", "simd",  };
entry.subName = "cos_remez_T7_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_T7_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1364, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "remez", "float", "simd",  };
entry.subName = "cos_remez_T7_0_float_simd";
entry.accuracy_test<float>(cos_remez_T7_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1365, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "remez_pade", "float", "scalar",  };
entry.subName = "cos_remez_pade_T7_0<float>";
entry.accuracy_test<float>(cos_remez_pade_T7_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1366, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_remez_pade_T7_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_pade_T7_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1367, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "remez_pade", "float", "simd",  };
entry.subName = "cos_remez_pade_T7_0_float_simd";
entry.accuracy_test<float>(cos_remez_pade_T7_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1368, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "float", "scalar",  };
entry.subName = "cos_quart_T7_7<float>";
entry.accuracy_test<float>(cos_quart_T7_7<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1369, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "float2x4", "simd",  };
entry.subName = "cos_quart_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_quart_T7_7<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1370, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "float", "simd",  };
entry.subName = "cos_quart_T7_7_float_simd";
entry.accuracy_test<float>(cos_quart_T7_7_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1371, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "remez", "float", "scalar",  };
entry.subName = "cos_unit1_remez_fma_ec_T8_0<float>";
entry.accuracy_test<float>(cos_unit1_remez_fma_ec_T8_0<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1372, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "remez", "float2x4", "simd",  };
entry.subName = "cos_unit1_remez_fma_ec_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_remez_fma_ec_T8_0<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1373, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "remez", "float", "simd",  };
entry.subName = "cos_unit1_remez_fma_ec_T8_0_float_simd";
entry.accuracy_test<float>(cos_unit1_remez_fma_ec_T8_0_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1374, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "remez_pade", "float", "scalar",  };
entry.subName = "cos_unit1_remez_pade_fma_ec_T8_0<float>";
entry.accuracy_test<float>(cos_unit1_remez_pade_fma_ec_T8_0<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1375, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_unit1_remez_pade_fma_ec_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_remez_pade_fma_ec_T8_0<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1376, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "remez_pade", "float", "simd",  };
entry.subName = "cos_unit1_remez_pade_fma_ec_T8_0_float_simd";
entry.accuracy_test<float>(cos_unit1_remez_pade_fma_ec_T8_0_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1377, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "float", "scalar",  };
entry.subName = "cos_unit1_quart_fma_ec_T8_8<float>";
entry.accuracy_test<float>(cos_unit1_quart_fma_ec_T8_8<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1378, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "float2x4", "simd",  };
entry.subName = "cos_unit1_quart_fma_ec_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_quart_fma_ec_T8_8<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1379, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "float", "simd",  };
entry.subName = "cos_unit1_quart_fma_ec_T8_8_float_simd";
entry.accuracy_test<float>(cos_unit1_quart_fma_ec_T8_8_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1380, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "float", "scalar",  };
entry.subName = "cos_unit1_half_fma_ec_T8_8<float>";
entry.accuracy_test<float>(cos_unit1_half_fma_ec_T8_8<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1381, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "float2x4", "simd",  };
entry.subName = "cos_unit1_half_fma_ec_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_half_fma_ec_T8_8<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1382, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "float", "simd",  };
entry.subName = "cos_unit1_half_fma_ec_T8_8_float_simd";
entry.accuracy_test<float>(cos_unit1_half_fma_ec_T8_8_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1383, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "remez", "float", "scalar",  };
entry.subName = "cos_unit1_remez_fma_T8_0<float>";
entry.accuracy_test<float>(cos_unit1_remez_fma_T8_0<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1384, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "remez", "float2x4", "simd",  };
entry.subName = "cos_unit1_remez_fma_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_remez_fma_T8_0<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1385, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "remez", "float", "simd",  };
entry.subName = "cos_unit1_remez_fma_T8_0_float_simd";
entry.accuracy_test<float>(cos_unit1_remez_fma_T8_0_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1386, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "remez_pade", "float", "scalar",  };
entry.subName = "cos_unit1_remez_pade_fma_T8_0<float>";
entry.accuracy_test<float>(cos_unit1_remez_pade_fma_T8_0<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1387, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_unit1_remez_pade_fma_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_remez_pade_fma_T8_0<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1388, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "remez_pade", "float", "simd",  };
entry.subName = "cos_unit1_remez_pade_fma_T8_0_float_simd";
entry.accuracy_test<float>(cos_unit1_remez_pade_fma_T8_0_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1389, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "float", "scalar",  };
entry.subName = "cos_unit1_quart_fma_T8_8<float>";
entry.accuracy_test<float>(cos_unit1_quart_fma_T8_8<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1390, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "float2x4", "simd",  };
entry.subName = "cos_unit1_quart_fma_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_quart_fma_T8_8<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1391, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "float", "simd",  };
entry.subName = "cos_unit1_quart_fma_T8_8_float_simd";
entry.accuracy_test<float>(cos_unit1_quart_fma_T8_8_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1392, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "float", "scalar",  };
entry.subName = "cos_unit1_half_fma_T8_8<float>";
entry.accuracy_test<float>(cos_unit1_half_fma_T8_8<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1393, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "float2x4", "simd",  };
entry.subName = "cos_unit1_half_fma_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_half_fma_T8_8<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1394, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "float", "simd",  };
entry.subName = "cos_unit1_half_fma_T8_8_float_simd";
entry.accuracy_test<float>(cos_unit1_half_fma_T8_8_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1395, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit1", "remez", "float", "scalar",  };
entry.subName = "cos_unit1_remez_ec_T8_0<float>";
entry.accuracy_test<float>(cos_unit1_remez_ec_T8_0<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1396, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit1", "remez", "float2x4", "simd",  };
entry.subName = "cos_unit1_remez_ec_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_remez_ec_T8_0<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1397, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit1", "remez", "float", "simd",  };
entry.subName = "cos_unit1_remez_ec_T8_0_float_simd";
entry.accuracy_test<float>(cos_unit1_remez_ec_T8_0_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1398, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit1", "remez_pade", "float", "scalar",  };
entry.subName = "cos_unit1_remez_pade_ec_T8_0<float>";
entry.accuracy_test<float>(cos_unit1_remez_pade_ec_T8_0<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1399, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit1", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_unit1_remez_pade_ec_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_remez_pade_ec_T8_0<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1400, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit1", "remez_pade", "float", "simd",  };
entry.subName = "cos_unit1_remez_pade_ec_T8_0_float_simd";
entry.accuracy_test<float>(cos_unit1_remez_pade_ec_T8_0_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1401, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit1", "float", "scalar",  };
entry.subName = "cos_unit1_quart_ec_T8_8<float>";
entry.accuracy_test<float>(cos_unit1_quart_ec_T8_8<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1402, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit1", "float2x4", "simd",  };
entry.subName = "cos_unit1_quart_ec_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_quart_ec_T8_8<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1403, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit1", "float", "simd",  };
entry.subName = "cos_unit1_quart_ec_T8_8_float_simd";
entry.accuracy_test<float>(cos_unit1_quart_ec_T8_8_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1404, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit1", "float", "scalar",  };
entry.subName = "cos_unit1_half_ec_T8_8<float>";
entry.accuracy_test<float>(cos_unit1_half_ec_T8_8<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1405, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit1", "float2x4", "simd",  };
entry.subName = "cos_unit1_half_ec_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_half_ec_T8_8<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1406, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit1", "float", "simd",  };
entry.subName = "cos_unit1_half_ec_T8_8_float_simd";
entry.accuracy_test<float>(cos_unit1_half_ec_T8_8_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1407, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit1", "remez", "float", "scalar",  };
entry.subName = "cos_unit1_remez_T8_0<float>";
entry.accuracy_test<float>(cos_unit1_remez_T8_0<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1408, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit1", "remez", "float2x4", "simd",  };
entry.subName = "cos_unit1_remez_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_remez_T8_0<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1409, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit1", "remez", "float", "simd",  };
entry.subName = "cos_unit1_remez_T8_0_float_simd";
entry.accuracy_test<float>(cos_unit1_remez_T8_0_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1410, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit1", "remez_pade", "float", "scalar",  };
entry.subName = "cos_unit1_remez_pade_T8_0<float>";
entry.accuracy_test<float>(cos_unit1_remez_pade_T8_0<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1411, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit1", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_unit1_remez_pade_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_remez_pade_T8_0<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1412, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit1", "remez_pade", "float", "simd",  };
entry.subName = "cos_unit1_remez_pade_T8_0_float_simd";
entry.accuracy_test<float>(cos_unit1_remez_pade_T8_0_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1413, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit1", "float", "scalar",  };
entry.subName = "cos_unit1_quart_T8_8<float>";
entry.accuracy_test<float>(cos_unit1_quart_T8_8<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1414, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit1", "float2x4", "simd",  };
entry.subName = "cos_unit1_quart_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_quart_T8_8<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1415, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit1", "float", "simd",  };
entry.subName = "cos_unit1_quart_T8_8_float_simd";
entry.accuracy_test<float>(cos_unit1_quart_T8_8_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1416, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit1", "float", "scalar",  };
entry.subName = "cos_unit1_half_T8_8<float>";
entry.accuracy_test<float>(cos_unit1_half_T8_8<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1417, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit1", "float2x4", "simd",  };
entry.subName = "cos_unit1_half_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_half_T8_8<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1418, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit1", "float", "simd",  };
entry.subName = "cos_unit1_half_T8_8_float_simd";
entry.accuracy_test<float>(cos_unit1_half_T8_8_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1419, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit2", "remez", "float", "scalar",  };
entry.subName = "cos_unit2_remez_fma_ec_T8_0<float>";
entry.accuracy_test<float>(cos_unit2_remez_fma_ec_T8_0<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1420, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit2", "remez", "float2x4", "simd",  };
entry.subName = "cos_unit2_remez_fma_ec_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_remez_fma_ec_T8_0<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1421, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit2", "remez", "float", "simd",  };
entry.subName = "cos_unit2_remez_fma_ec_T8_0_float_simd";
entry.accuracy_test<float>(cos_unit2_remez_fma_ec_T8_0_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1422, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit2", "remez_pade", "float", "scalar",  };
entry.subName = "cos_unit2_remez_pade_fma_ec_T8_0<float>";
entry.accuracy_test<float>(cos_unit2_remez_pade_fma_ec_T8_0<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1423, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit2", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_unit2_remez_pade_fma_ec_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_remez_pade_fma_ec_T8_0<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1424, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit2", "remez_pade", "float", "simd",  };
entry.subName = "cos_unit2_remez_pade_fma_ec_T8_0_float_simd";
entry.accuracy_test<float>(cos_unit2_remez_pade_fma_ec_T8_0_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1425, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit2", "float", "scalar",  };
entry.subName = "cos_unit2_quart_fma_ec_T8_8<float>";
entry.accuracy_test<float>(cos_unit2_quart_fma_ec_T8_8<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1426, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit2", "float2x4", "simd",  };
entry.subName = "cos_unit2_quart_fma_ec_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_quart_fma_ec_T8_8<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1427, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit2", "float", "simd",  };
entry.subName = "cos_unit2_quart_fma_ec_T8_8_float_simd";
entry.accuracy_test<float>(cos_unit2_quart_fma_ec_T8_8_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1428, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit2", "float", "scalar",  };
entry.subName = "cos_unit2_half_fma_ec_T8_8<float>";
entry.accuracy_test<float>(cos_unit2_half_fma_ec_T8_8<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1429, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit2", "float2x4", "simd",  };
entry.subName = "cos_unit2_half_fma_ec_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_half_fma_ec_T8_8<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1430, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit2", "float", "simd",  };
entry.subName = "cos_unit2_half_fma_ec_T8_8_float_simd";
entry.accuracy_test<float>(cos_unit2_half_fma_ec_T8_8_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1431, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit2", "remez", "float", "scalar",  };
entry.subName = "cos_unit2_remez_fma_T8_0<float>";
entry.accuracy_test<float>(cos_unit2_remez_fma_T8_0<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1432, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit2", "remez", "float2x4", "simd",  };
entry.subName = "cos_unit2_remez_fma_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_remez_fma_T8_0<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1433, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit2", "remez", "float", "simd",  };
entry.subName = "cos_unit2_remez_fma_T8_0_float_simd";
entry.accuracy_test<float>(cos_unit2_remez_fma_T8_0_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1434, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit2", "remez_pade", "float", "scalar",  };
entry.subName = "cos_unit2_remez_pade_fma_T8_0<float>";
entry.accuracy_test<float>(cos_unit2_remez_pade_fma_T8_0<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1435, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit2", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_unit2_remez_pade_fma_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_remez_pade_fma_T8_0<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1436, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit2", "remez_pade", "float", "simd",  };
entry.subName = "cos_unit2_remez_pade_fma_T8_0_float_simd";
entry.accuracy_test<float>(cos_unit2_remez_pade_fma_T8_0_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1437, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit2", "float", "scalar",  };
entry.subName = "cos_unit2_quart_fma_T8_8<float>";
entry.accuracy_test<float>(cos_unit2_quart_fma_T8_8<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1438, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit2", "float2x4", "simd",  };
entry.subName = "cos_unit2_quart_fma_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_quart_fma_T8_8<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1439, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit2", "float", "simd",  };
entry.subName = "cos_unit2_quart_fma_T8_8_float_simd";
entry.accuracy_test<float>(cos_unit2_quart_fma_T8_8_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1440, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit2", "float", "scalar",  };
entry.subName = "cos_unit2_half_fma_T8_8<float>";
entry.accuracy_test<float>(cos_unit2_half_fma_T8_8<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1441, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit2", "float2x4", "simd",  };
entry.subName = "cos_unit2_half_fma_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_half_fma_T8_8<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1442, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit2", "float", "simd",  };
entry.subName = "cos_unit2_half_fma_T8_8_float_simd";
entry.accuracy_test<float>(cos_unit2_half_fma_T8_8_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1443, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "remez", "float", "scalar",  };
entry.subName = "cos_unit2_remez_ec_T8_0<float>";
entry.accuracy_test<float>(cos_unit2_remez_ec_T8_0<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1444, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "remez", "float2x4", "simd",  };
entry.subName = "cos_unit2_remez_ec_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_remez_ec_T8_0<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1445, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "remez", "float", "simd",  };
entry.subName = "cos_unit2_remez_ec_T8_0_float_simd";
entry.accuracy_test<float>(cos_unit2_remez_ec_T8_0_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1446, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "remez_pade", "float", "scalar",  };
entry.subName = "cos_unit2_remez_pade_ec_T8_0<float>";
entry.accuracy_test<float>(cos_unit2_remez_pade_ec_T8_0<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1447, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_unit2_remez_pade_ec_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_remez_pade_ec_T8_0<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1448, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "remez_pade", "float", "simd",  };
entry.subName = "cos_unit2_remez_pade_ec_T8_0_float_simd";
entry.accuracy_test<float>(cos_unit2_remez_pade_ec_T8_0_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1449, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "float", "scalar",  };
entry.subName = "cos_unit2_quart_ec_T8_8<float>";
entry.accuracy_test<float>(cos_unit2_quart_ec_T8_8<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1450, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "float2x4", "simd",  };
entry.subName = "cos_unit2_quart_ec_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_quart_ec_T8_8<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1451, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "float", "simd",  };
entry.subName = "cos_unit2_quart_ec_T8_8_float_simd";
entry.accuracy_test<float>(cos_unit2_quart_ec_T8_8_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1452, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "float", "scalar",  };
entry.subName = "cos_unit2_half_ec_T8_8<float>";
entry.accuracy_test<float>(cos_unit2_half_ec_T8_8<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1453, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "float2x4", "simd",  };
entry.subName = "cos_unit2_half_ec_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_half_ec_T8_8<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1454, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "float", "simd",  };
entry.subName = "cos_unit2_half_ec_T8_8_float_simd";
entry.accuracy_test<float>(cos_unit2_half_ec_T8_8_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1455, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "remez", "float", "scalar",  };
entry.subName = "cos_unit2_remez_T8_0<float>";
entry.accuracy_test<float>(cos_unit2_remez_T8_0<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1456, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "remez", "float2x4", "simd",  };
entry.subName = "cos_unit2_remez_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_remez_T8_0<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1457, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "remez", "float", "simd",  };
entry.subName = "cos_unit2_remez_T8_0_float_simd";
entry.accuracy_test<float>(cos_unit2_remez_T8_0_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1458, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "remez_pade", "float", "scalar",  };
entry.subName = "cos_unit2_remez_pade_T8_0<float>";
entry.accuracy_test<float>(cos_unit2_remez_pade_T8_0<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1459, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_unit2_remez_pade_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_remez_pade_T8_0<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1460, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "remez_pade", "float", "simd",  };
entry.subName = "cos_unit2_remez_pade_T8_0_float_simd";
entry.accuracy_test<float>(cos_unit2_remez_pade_T8_0_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1461, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "float", "scalar",  };
entry.subName = "cos_unit2_quart_T8_8<float>";
entry.accuracy_test<float>(cos_unit2_quart_T8_8<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1462, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "float2x4", "simd",  };
entry.subName = "cos_unit2_quart_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_quart_T8_8<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1463, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "float", "simd",  };
entry.subName = "cos_unit2_quart_T8_8_float_simd";
entry.accuracy_test<float>(cos_unit2_quart_T8_8_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1464, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "float", "scalar",  };
entry.subName = "cos_unit2_half_T8_8<float>";
entry.accuracy_test<float>(cos_unit2_half_T8_8<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1465, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "float2x4", "simd",  };
entry.subName = "cos_unit2_half_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_half_T8_8<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1466, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "float", "simd",  };
entry.subName = "cos_unit2_half_T8_8_float_simd";
entry.accuracy_test<float>(cos_unit2_half_T8_8_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1467, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "remez", "float", "scalar",  };
entry.subName = "cos_remez_fma_ec_T8_0<float>";
entry.accuracy_test<float>(cos_remez_fma_ec_T8_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1468, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "remez", "float2x4", "simd",  };
entry.subName = "cos_remez_fma_ec_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_fma_ec_T8_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1469, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "remez", "float", "simd",  };
entry.subName = "cos_remez_fma_ec_T8_0_float_simd";
entry.accuracy_test<float>(cos_remez_fma_ec_T8_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1470, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "remez_pade", "float", "scalar",  };
entry.subName = "cos_remez_pade_fma_ec_T8_0<float>";
entry.accuracy_test<float>(cos_remez_pade_fma_ec_T8_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1471, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_remez_pade_fma_ec_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_pade_fma_ec_T8_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1472, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "remez_pade", "float", "simd",  };
entry.subName = "cos_remez_pade_fma_ec_T8_0_float_simd";
entry.accuracy_test<float>(cos_remez_pade_fma_ec_T8_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1473, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "float", "scalar",  };
entry.subName = "cos_quart_fma_ec_T8_8<float>";
entry.accuracy_test<float>(cos_quart_fma_ec_T8_8<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1474, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "float2x4", "simd",  };
entry.subName = "cos_quart_fma_ec_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_quart_fma_ec_T8_8<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1475, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "float", "simd",  };
entry.subName = "cos_quart_fma_ec_T8_8_float_simd";
entry.accuracy_test<float>(cos_quart_fma_ec_T8_8_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1476, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "float", "scalar",  };
entry.subName = "cos_half_fma_ec_T8_8<float>";
entry.accuracy_test<float>(cos_half_fma_ec_T8_8<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1477, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "float2x4", "simd",  };
entry.subName = "cos_half_fma_ec_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_half_fma_ec_T8_8<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1478, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "float", "simd",  };
entry.subName = "cos_half_fma_ec_T8_8_float_simd";
entry.accuracy_test<float>(cos_half_fma_ec_T8_8_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1479, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "remez", "float", "scalar",  };
entry.subName = "cos_remez_fma_T8_0<float>";
entry.accuracy_test<float>(cos_remez_fma_T8_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1480, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "remez", "float2x4", "simd",  };
entry.subName = "cos_remez_fma_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_fma_T8_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1481, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "remez", "float", "simd",  };
entry.subName = "cos_remez_fma_T8_0_float_simd";
entry.accuracy_test<float>(cos_remez_fma_T8_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1482, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "remez_pade", "float", "scalar",  };
entry.subName = "cos_remez_pade_fma_T8_0<float>";
entry.accuracy_test<float>(cos_remez_pade_fma_T8_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1483, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_remez_pade_fma_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_pade_fma_T8_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1484, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "remez_pade", "float", "simd",  };
entry.subName = "cos_remez_pade_fma_T8_0_float_simd";
entry.accuracy_test<float>(cos_remez_pade_fma_T8_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1485, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "float", "scalar",  };
entry.subName = "cos_quart_fma_T8_8<float>";
entry.accuracy_test<float>(cos_quart_fma_T8_8<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1486, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "float2x4", "simd",  };
entry.subName = "cos_quart_fma_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_quart_fma_T8_8<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1487, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "float", "simd",  };
entry.subName = "cos_quart_fma_T8_8_float_simd";
entry.accuracy_test<float>(cos_quart_fma_T8_8_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1488, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "float", "scalar",  };
entry.subName = "cos_half_fma_T8_8<float>";
entry.accuracy_test<float>(cos_half_fma_T8_8<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1489, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "float2x4", "simd",  };
entry.subName = "cos_half_fma_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_half_fma_T8_8<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1490, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "float", "simd",  };
entry.subName = "cos_half_fma_T8_8_float_simd";
entry.accuracy_test<float>(cos_half_fma_T8_8_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1491, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "remez", "float", "scalar",  };
entry.subName = "cos_remez_ec_T8_0<float>";
entry.accuracy_test<float>(cos_remez_ec_T8_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1492, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "remez", "float2x4", "simd",  };
entry.subName = "cos_remez_ec_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_ec_T8_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1493, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "remez", "float", "simd",  };
entry.subName = "cos_remez_ec_T8_0_float_simd";
entry.accuracy_test<float>(cos_remez_ec_T8_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1494, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "remez_pade", "float", "scalar",  };
entry.subName = "cos_remez_pade_ec_T8_0<float>";
entry.accuracy_test<float>(cos_remez_pade_ec_T8_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1495, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_remez_pade_ec_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_pade_ec_T8_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1496, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "remez_pade", "float", "simd",  };
entry.subName = "cos_remez_pade_ec_T8_0_float_simd";
entry.accuracy_test<float>(cos_remez_pade_ec_T8_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1497, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "float", "scalar",  };
entry.subName = "cos_quart_ec_T8_8<float>";
entry.accuracy_test<float>(cos_quart_ec_T8_8<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1498, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "float2x4", "simd",  };
entry.subName = "cos_quart_ec_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_quart_ec_T8_8<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1499, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "float", "simd",  };
entry.subName = "cos_quart_ec_T8_8_float_simd";
entry.accuracy_test<float>(cos_quart_ec_T8_8_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1500, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "float", "scalar",  };
entry.subName = "cos_half_ec_T8_8<float>";
entry.accuracy_test<float>(cos_half_ec_T8_8<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1501, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "float2x4", "simd",  };
entry.subName = "cos_half_ec_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_half_ec_T8_8<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1502, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "float", "simd",  };
entry.subName = "cos_half_ec_T8_8_float_simd";
entry.accuracy_test<float>(cos_half_ec_T8_8_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1503, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "remez", "float", "scalar",  };
entry.subName = "cos_remez_T8_0<float>";
entry.accuracy_test<float>(cos_remez_T8_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1504, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "remez", "float2x4", "simd",  };
entry.subName = "cos_remez_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_T8_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1505, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "remez", "float", "simd",  };
entry.subName = "cos_remez_T8_0_float_simd";
entry.accuracy_test<float>(cos_remez_T8_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1506, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "remez_pade", "float", "scalar",  };
entry.subName = "cos_remez_pade_T8_0<float>";
entry.accuracy_test<float>(cos_remez_pade_T8_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1507, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_remez_pade_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_pade_T8_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1508, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "remez_pade", "float", "simd",  };
entry.subName = "cos_remez_pade_T8_0_float_simd";
entry.accuracy_test<float>(cos_remez_pade_T8_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1509, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "float", "scalar",  };
entry.subName = "cos_quart_T8_8<float>";
entry.accuracy_test<float>(cos_quart_T8_8<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1510, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "float2x4", "simd",  };
entry.subName = "cos_quart_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_quart_T8_8<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1511, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "float", "simd",  };
entry.subName = "cos_quart_T8_8_float_simd";
entry.accuracy_test<float>(cos_quart_T8_8_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1512, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "float", "scalar",  };
entry.subName = "cos_half_T8_8<float>";
entry.accuracy_test<float>(cos_half_T8_8<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1513, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "float2x4", "simd",  };
entry.subName = "cos_half_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_half_T8_8<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1514, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "float", "simd",  };
entry.subName = "cos_half_T8_8_float_simd";
entry.accuracy_test<float>(cos_half_T8_8_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1515, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "remez", "float", "scalar",  };
entry.subName = "cos_unit1_remez_fma_ec_T9_0<float>";
entry.accuracy_test<float>(cos_unit1_remez_fma_ec_T9_0<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1516, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "remez", "float2x4", "simd",  };
entry.subName = "cos_unit1_remez_fma_ec_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_remez_fma_ec_T9_0<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1517, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "remez", "float", "simd",  };
entry.subName = "cos_unit1_remez_fma_ec_T9_0_float_simd";
entry.accuracy_test<float>(cos_unit1_remez_fma_ec_T9_0_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1518, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "remez_pade", "float", "scalar",  };
entry.subName = "cos_unit1_remez_pade_fma_ec_T9_0<float>";
entry.accuracy_test<float>(cos_unit1_remez_pade_fma_ec_T9_0<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1519, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_unit1_remez_pade_fma_ec_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_remez_pade_fma_ec_T9_0<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1520, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "remez_pade", "float", "simd",  };
entry.subName = "cos_unit1_remez_pade_fma_ec_T9_0_float_simd";
entry.accuracy_test<float>(cos_unit1_remez_pade_fma_ec_T9_0_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1521, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "float", "scalar",  };
entry.subName = "cos_unit1_quart_fma_ec_T9_9<float>";
entry.accuracy_test<float>(cos_unit1_quart_fma_ec_T9_9<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1522, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "float2x4", "simd",  };
entry.subName = "cos_unit1_quart_fma_ec_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_quart_fma_ec_T9_9<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1523, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit1", "float", "simd",  };
entry.subName = "cos_unit1_quart_fma_ec_T9_9_float_simd";
entry.accuracy_test<float>(cos_unit1_quart_fma_ec_T9_9_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1524, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "remez", "float", "scalar",  };
entry.subName = "cos_unit1_remez_fma_T9_0<float>";
entry.accuracy_test<float>(cos_unit1_remez_fma_T9_0<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1525, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "remez", "float2x4", "simd",  };
entry.subName = "cos_unit1_remez_fma_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_remez_fma_T9_0<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1526, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "remez", "float", "simd",  };
entry.subName = "cos_unit1_remez_fma_T9_0_float_simd";
entry.accuracy_test<float>(cos_unit1_remez_fma_T9_0_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1527, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "remez_pade", "float", "scalar",  };
entry.subName = "cos_unit1_remez_pade_fma_T9_0<float>";
entry.accuracy_test<float>(cos_unit1_remez_pade_fma_T9_0<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1528, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_unit1_remez_pade_fma_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_remez_pade_fma_T9_0<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1529, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "remez_pade", "float", "simd",  };
entry.subName = "cos_unit1_remez_pade_fma_T9_0_float_simd";
entry.accuracy_test<float>(cos_unit1_remez_pade_fma_T9_0_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1530, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "float", "scalar",  };
entry.subName = "cos_unit1_quart_fma_T9_9<float>";
entry.accuracy_test<float>(cos_unit1_quart_fma_T9_9<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1531, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "float2x4", "simd",  };
entry.subName = "cos_unit1_quart_fma_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_quart_fma_T9_9<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1532, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit1", "float", "simd",  };
entry.subName = "cos_unit1_quart_fma_T9_9_float_simd";
entry.accuracy_test<float>(cos_unit1_quart_fma_T9_9_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1533, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit1", "remez", "float", "scalar",  };
entry.subName = "cos_unit1_remez_ec_T9_0<float>";
entry.accuracy_test<float>(cos_unit1_remez_ec_T9_0<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1534, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit1", "remez", "float2x4", "simd",  };
entry.subName = "cos_unit1_remez_ec_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_remez_ec_T9_0<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1535, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit1", "remez", "float", "simd",  };
entry.subName = "cos_unit1_remez_ec_T9_0_float_simd";
entry.accuracy_test<float>(cos_unit1_remez_ec_T9_0_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1536, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit1", "remez_pade", "float", "scalar",  };
entry.subName = "cos_unit1_remez_pade_ec_T9_0<float>";
entry.accuracy_test<float>(cos_unit1_remez_pade_ec_T9_0<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1537, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit1", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_unit1_remez_pade_ec_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_remez_pade_ec_T9_0<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1538, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit1", "remez_pade", "float", "simd",  };
entry.subName = "cos_unit1_remez_pade_ec_T9_0_float_simd";
entry.accuracy_test<float>(cos_unit1_remez_pade_ec_T9_0_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1539, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit1", "float", "scalar",  };
entry.subName = "cos_unit1_quart_ec_T9_9<float>";
entry.accuracy_test<float>(cos_unit1_quart_ec_T9_9<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1540, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit1", "float2x4", "simd",  };
entry.subName = "cos_unit1_quart_ec_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_quart_ec_T9_9<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1541, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit1", "float", "simd",  };
entry.subName = "cos_unit1_quart_ec_T9_9_float_simd";
entry.accuracy_test<float>(cos_unit1_quart_ec_T9_9_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1542, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit1", "remez", "float", "scalar",  };
entry.subName = "cos_unit1_remez_T9_0<float>";
entry.accuracy_test<float>(cos_unit1_remez_T9_0<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1543, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit1", "remez", "float2x4", "simd",  };
entry.subName = "cos_unit1_remez_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_remez_T9_0<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1544, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit1", "remez", "float", "simd",  };
entry.subName = "cos_unit1_remez_T9_0_float_simd";
entry.accuracy_test<float>(cos_unit1_remez_T9_0_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1545, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit1", "remez_pade", "float", "scalar",  };
entry.subName = "cos_unit1_remez_pade_T9_0<float>";
entry.accuracy_test<float>(cos_unit1_remez_pade_T9_0<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1546, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit1", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_unit1_remez_pade_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_remez_pade_T9_0<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1547, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit1", "remez_pade", "float", "simd",  };
entry.subName = "cos_unit1_remez_pade_T9_0_float_simd";
entry.accuracy_test<float>(cos_unit1_remez_pade_T9_0_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1548, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit1", "float", "scalar",  };
entry.subName = "cos_unit1_quart_T9_9<float>";
entry.accuracy_test<float>(cos_unit1_quart_T9_9<float>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1549, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit1", "float2x4", "simd",  };
entry.subName = "cos_unit1_quart_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit1_quart_T9_9<cr::simd::float2x4>, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1550, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit1", "float", "simd",  };
entry.subName = "cos_unit1_quart_T9_9_float_simd";
entry.accuracy_test<float>(cos_unit1_quart_T9_9_float_simd, std::cosf, 0.0f, 1.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1551, 0.0f, 1.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit2", "remez", "float", "scalar",  };
entry.subName = "cos_unit2_remez_fma_ec_T9_0<float>";
entry.accuracy_test<float>(cos_unit2_remez_fma_ec_T9_0<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1552, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit2", "remez", "float2x4", "simd",  };
entry.subName = "cos_unit2_remez_fma_ec_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_remez_fma_ec_T9_0<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1553, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit2", "remez", "float", "simd",  };
entry.subName = "cos_unit2_remez_fma_ec_T9_0_float_simd";
entry.accuracy_test<float>(cos_unit2_remez_fma_ec_T9_0_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1554, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit2", "remez_pade", "float", "scalar",  };
entry.subName = "cos_unit2_remez_pade_fma_ec_T9_0<float>";
entry.accuracy_test<float>(cos_unit2_remez_pade_fma_ec_T9_0<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1555, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit2", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_unit2_remez_pade_fma_ec_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_remez_pade_fma_ec_T9_0<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1556, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit2", "remez_pade", "float", "simd",  };
entry.subName = "cos_unit2_remez_pade_fma_ec_T9_0_float_simd";
entry.accuracy_test<float>(cos_unit2_remez_pade_fma_ec_T9_0_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1557, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit2", "float", "scalar",  };
entry.subName = "cos_unit2_quart_fma_ec_T9_9<float>";
entry.accuracy_test<float>(cos_unit2_quart_fma_ec_T9_9<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1558, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit2", "float2x4", "simd",  };
entry.subName = "cos_unit2_quart_fma_ec_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_quart_fma_ec_T9_9<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1559, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos_unit2", "float", "simd",  };
entry.subName = "cos_unit2_quart_fma_ec_T9_9_float_simd";
entry.accuracy_test<float>(cos_unit2_quart_fma_ec_T9_9_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1560, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit2", "remez", "float", "scalar",  };
entry.subName = "cos_unit2_remez_fma_T9_0<float>";
entry.accuracy_test<float>(cos_unit2_remez_fma_T9_0<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1561, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit2", "remez", "float2x4", "simd",  };
entry.subName = "cos_unit2_remez_fma_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_remez_fma_T9_0<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1562, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit2", "remez", "float", "simd",  };
entry.subName = "cos_unit2_remez_fma_T9_0_float_simd";
entry.accuracy_test<float>(cos_unit2_remez_fma_T9_0_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1563, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit2", "remez_pade", "float", "scalar",  };
entry.subName = "cos_unit2_remez_pade_fma_T9_0<float>";
entry.accuracy_test<float>(cos_unit2_remez_pade_fma_T9_0<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1564, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit2", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_unit2_remez_pade_fma_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_remez_pade_fma_T9_0<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1565, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit2", "remez_pade", "float", "simd",  };
entry.subName = "cos_unit2_remez_pade_fma_T9_0_float_simd";
entry.accuracy_test<float>(cos_unit2_remez_pade_fma_T9_0_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1566, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit2", "float", "scalar",  };
entry.subName = "cos_unit2_quart_fma_T9_9<float>";
entry.accuracy_test<float>(cos_unit2_quart_fma_T9_9<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1567, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit2", "float2x4", "simd",  };
entry.subName = "cos_unit2_quart_fma_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_quart_fma_T9_9<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1568, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos_unit2", "float", "simd",  };
entry.subName = "cos_unit2_quart_fma_T9_9_float_simd";
entry.accuracy_test<float>(cos_unit2_quart_fma_T9_9_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1569, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "remez", "float", "scalar",  };
entry.subName = "cos_unit2_remez_ec_T9_0<float>";
entry.accuracy_test<float>(cos_unit2_remez_ec_T9_0<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1570, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "remez", "float2x4", "simd",  };
entry.subName = "cos_unit2_remez_ec_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_remez_ec_T9_0<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1571, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "remez", "float", "simd",  };
entry.subName = "cos_unit2_remez_ec_T9_0_float_simd";
entry.accuracy_test<float>(cos_unit2_remez_ec_T9_0_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1572, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "remez_pade", "float", "scalar",  };
entry.subName = "cos_unit2_remez_pade_ec_T9_0<float>";
entry.accuracy_test<float>(cos_unit2_remez_pade_ec_T9_0<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1573, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_unit2_remez_pade_ec_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_remez_pade_ec_T9_0<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1574, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "remez_pade", "float", "simd",  };
entry.subName = "cos_unit2_remez_pade_ec_T9_0_float_simd";
entry.accuracy_test<float>(cos_unit2_remez_pade_ec_T9_0_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1575, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "float", "scalar",  };
entry.subName = "cos_unit2_quart_ec_T9_9<float>";
entry.accuracy_test<float>(cos_unit2_quart_ec_T9_9<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1576, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "float2x4", "simd",  };
entry.subName = "cos_unit2_quart_ec_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_quart_ec_T9_9<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1577, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos_unit2", "float", "simd",  };
entry.subName = "cos_unit2_quart_ec_T9_9_float_simd";
entry.accuracy_test<float>(cos_unit2_quart_ec_T9_9_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1578, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "remez", "float", "scalar",  };
entry.subName = "cos_unit2_remez_T9_0<float>";
entry.accuracy_test<float>(cos_unit2_remez_T9_0<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1579, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "remez", "float2x4", "simd",  };
entry.subName = "cos_unit2_remez_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_remez_T9_0<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1580, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "remez", "float", "simd",  };
entry.subName = "cos_unit2_remez_T9_0_float_simd";
entry.accuracy_test<float>(cos_unit2_remez_T9_0_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1581, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "remez_pade", "float", "scalar",  };
entry.subName = "cos_unit2_remez_pade_T9_0<float>";
entry.accuracy_test<float>(cos_unit2_remez_pade_T9_0<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1582, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_unit2_remez_pade_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_remez_pade_T9_0<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1583, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "remez_pade", "float", "simd",  };
entry.subName = "cos_unit2_remez_pade_T9_0_float_simd";
entry.accuracy_test<float>(cos_unit2_remez_pade_T9_0_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1584, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "float", "scalar",  };
entry.subName = "cos_unit2_quart_T9_9<float>";
entry.accuracy_test<float>(cos_unit2_quart_T9_9<float>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1585, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "float2x4", "simd",  };
entry.subName = "cos_unit2_quart_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_unit2_quart_T9_9<cr::simd::float2x4>, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1586, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos_unit2", "float", "simd",  };
entry.subName = "cos_unit2_quart_T9_9_float_simd";
entry.accuracy_test<float>(cos_unit2_quart_T9_9_float_simd, std::cosf, 0.0f, 2.0f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1587, 0.0f, 2.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "remez", "float", "scalar",  };
entry.subName = "cos_remez_fma_ec_T9_0<float>";
entry.accuracy_test<float>(cos_remez_fma_ec_T9_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1588, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "remez", "float2x4", "simd",  };
entry.subName = "cos_remez_fma_ec_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_fma_ec_T9_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1589, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "remez", "float", "simd",  };
entry.subName = "cos_remez_fma_ec_T9_0_float_simd";
entry.accuracy_test<float>(cos_remez_fma_ec_T9_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1590, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "remez_pade", "float", "scalar",  };
entry.subName = "cos_remez_pade_fma_ec_T9_0<float>";
entry.accuracy_test<float>(cos_remez_pade_fma_ec_T9_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1591, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_remez_pade_fma_ec_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_pade_fma_ec_T9_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1592, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "remez_pade", "float", "simd",  };
entry.subName = "cos_remez_pade_fma_ec_T9_0_float_simd";
entry.accuracy_test<float>(cos_remez_pade_fma_ec_T9_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1593, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "float", "scalar",  };
entry.subName = "cos_quart_fma_ec_T9_9<float>";
entry.accuracy_test<float>(cos_quart_fma_ec_T9_9<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1594, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "float2x4", "simd",  };
entry.subName = "cos_quart_fma_ec_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_quart_fma_ec_T9_9<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1595, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "cos", "float", "simd",  };
entry.subName = "cos_quart_fma_ec_T9_9_float_simd";
entry.accuracy_test<float>(cos_quart_fma_ec_T9_9_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1596, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "remez", "float", "scalar",  };
entry.subName = "cos_remez_fma_T9_0<float>";
entry.accuracy_test<float>(cos_remez_fma_T9_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1597, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "remez", "float2x4", "simd",  };
entry.subName = "cos_remez_fma_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_fma_T9_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1598, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "remez", "float", "simd",  };
entry.subName = "cos_remez_fma_T9_0_float_simd";
entry.accuracy_test<float>(cos_remez_fma_T9_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1599, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "remez_pade", "float", "scalar",  };
entry.subName = "cos_remez_pade_fma_T9_0<float>";
entry.accuracy_test<float>(cos_remez_pade_fma_T9_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1600, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_remez_pade_fma_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_pade_fma_T9_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1601, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "remez_pade", "float", "simd",  };
entry.subName = "cos_remez_pade_fma_T9_0_float_simd";
entry.accuracy_test<float>(cos_remez_pade_fma_T9_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1602, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "float", "scalar",  };
entry.subName = "cos_quart_fma_T9_9<float>";
entry.accuracy_test<float>(cos_quart_fma_T9_9<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1603, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "float2x4", "simd",  };
entry.subName = "cos_quart_fma_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_quart_fma_T9_9<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1604, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "cos", "float", "simd",  };
entry.subName = "cos_quart_fma_T9_9_float_simd";
entry.accuracy_test<float>(cos_quart_fma_T9_9_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1605, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "remez", "float", "scalar",  };
entry.subName = "cos_remez_ec_T9_0<float>";
entry.accuracy_test<float>(cos_remez_ec_T9_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1606, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "remez", "float2x4", "simd",  };
entry.subName = "cos_remez_ec_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_ec_T9_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1607, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "remez", "float", "simd",  };
entry.subName = "cos_remez_ec_T9_0_float_simd";
entry.accuracy_test<float>(cos_remez_ec_T9_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1608, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "remez_pade", "float", "scalar",  };
entry.subName = "cos_remez_pade_ec_T9_0<float>";
entry.accuracy_test<float>(cos_remez_pade_ec_T9_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1609, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_remez_pade_ec_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_pade_ec_T9_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1610, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "remez_pade", "float", "simd",  };
entry.subName = "cos_remez_pade_ec_T9_0_float_simd";
entry.accuracy_test<float>(cos_remez_pade_ec_T9_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1611, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "float", "scalar",  };
entry.subName = "cos_quart_ec_T9_9<float>";
entry.accuracy_test<float>(cos_quart_ec_T9_9<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1612, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "float2x4", "simd",  };
entry.subName = "cos_quart_ec_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_quart_ec_T9_9<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1613, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "cos", "float", "simd",  };
entry.subName = "cos_quart_ec_T9_9_float_simd";
entry.accuracy_test<float>(cos_quart_ec_T9_9_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1614, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "remez", "float", "scalar",  };
entry.subName = "cos_remez_T9_0<float>";
entry.accuracy_test<float>(cos_remez_T9_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1615, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "remez", "float2x4", "simd",  };
entry.subName = "cos_remez_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_T9_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1616, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "remez", "float", "simd",  };
entry.subName = "cos_remez_T9_0_float_simd";
entry.accuracy_test<float>(cos_remez_T9_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1617, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "remez_pade", "float", "scalar",  };
entry.subName = "cos_remez_pade_T9_0<float>";
entry.accuracy_test<float>(cos_remez_pade_T9_0<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1618, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "remez_pade", "float2x4", "simd",  };
entry.subName = "cos_remez_pade_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_remez_pade_T9_0<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1619, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "remez_pade", "float", "simd",  };
entry.subName = "cos_remez_pade_T9_0_float_simd";
entry.accuracy_test<float>(cos_remez_pade_T9_0_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1620, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "float", "scalar",  };
entry.subName = "cos_quart_T9_9<float>";
entry.accuracy_test<float>(cos_quart_T9_9<float>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1621, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "float2x4", "simd",  };
entry.subName = "cos_quart_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(cos_quart_T9_9<cr::simd::float2x4>, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1622, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "cos", "float", "simd",  };
entry.subName = "cos_quart_T9_9_float_simd";
entry.accuracy_test<float>(cos_quart_T9_9_float_simd, std::cosf, 0.0f, 6.283185307179586f, 0.0f, 6.283185307179586f);
entry.time<float>(msvc_bug_workaround_1623, 0.0f, 6.283185307179586f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "std::tanhf", "float", "scalar", "reference",  };
entry.subName = "std::tanhf";
entry.accuracy_test<float>(std::tanhf, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1624, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "remez", "float", "scalar",  };
entry.subName = "tanh_remez_fma_ec_T3_0<float>";
entry.accuracy_test<float>(tanh_remez_fma_ec_T3_0<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1625, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "remez", "float2x4", "simd",  };
entry.subName = "tanh_remez_fma_ec_T3_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_fma_ec_T3_0<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1626, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "remez", "float", "simd",  };
entry.subName = "tanh_remez_fma_ec_T3_0_float_simd";
entry.accuracy_test<float>(tanh_remez_fma_ec_T3_0_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1627, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "remez_pade", "float", "scalar",  };
entry.subName = "tanh_remez_pade_fma_ec_T3_3<float>";
entry.accuracy_test<float>(tanh_remez_pade_fma_ec_T3_3<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1628, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "remez_pade", "float2x4", "simd",  };
entry.subName = "tanh_remez_pade_fma_ec_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_pade_fma_ec_T3_3<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1629, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "remez_pade", "float", "simd",  };
entry.subName = "tanh_remez_pade_fma_ec_T3_3_float_simd";
entry.accuracy_test<float>(tanh_remez_pade_fma_ec_T3_3_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1630, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "float", "scalar",  };
entry.subName = "tanh_fma_ec_T3_3<float>";
entry.accuracy_test<float>(tanh_fma_ec_T3_3<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1631, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "float2x4", "simd",  };
entry.subName = "tanh_fma_ec_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_fma_ec_T3_3<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1632, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "float", "simd",  };
entry.subName = "tanh_fma_ec_T3_3_float_simd";
entry.accuracy_test<float>(tanh_fma_ec_T3_3_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1633, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "remez", "float", "scalar",  };
entry.subName = "tanh_remez_fma_T3_0<float>";
entry.accuracy_test<float>(tanh_remez_fma_T3_0<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1634, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "remez", "float2x4", "simd",  };
entry.subName = "tanh_remez_fma_T3_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_fma_T3_0<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1635, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "remez", "float", "simd",  };
entry.subName = "tanh_remez_fma_T3_0_float_simd";
entry.accuracy_test<float>(tanh_remez_fma_T3_0_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1636, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "remez_pade", "float", "scalar",  };
entry.subName = "tanh_remez_pade_fma_T3_3<float>";
entry.accuracy_test<float>(tanh_remez_pade_fma_T3_3<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1637, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "remez_pade", "float2x4", "simd",  };
entry.subName = "tanh_remez_pade_fma_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_pade_fma_T3_3<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1638, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "remez_pade", "float", "simd",  };
entry.subName = "tanh_remez_pade_fma_T3_3_float_simd";
entry.accuracy_test<float>(tanh_remez_pade_fma_T3_3_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1639, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "float", "scalar",  };
entry.subName = "tanh_fma_T3_3<float>";
entry.accuracy_test<float>(tanh_fma_T3_3<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1640, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "float2x4", "simd",  };
entry.subName = "tanh_fma_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_fma_T3_3<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1641, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "float", "simd",  };
entry.subName = "tanh_fma_T3_3_float_simd";
entry.accuracy_test<float>(tanh_fma_T3_3_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1642, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "remez", "float", "scalar",  };
entry.subName = "tanh_remez_ec_T3_0<float>";
entry.accuracy_test<float>(tanh_remez_ec_T3_0<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1643, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "remez", "float2x4", "simd",  };
entry.subName = "tanh_remez_ec_T3_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_ec_T3_0<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1644, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "remez", "float", "simd",  };
entry.subName = "tanh_remez_ec_T3_0_float_simd";
entry.accuracy_test<float>(tanh_remez_ec_T3_0_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1645, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "remez_pade", "float", "scalar",  };
entry.subName = "tanh_remez_pade_ec_T3_3<float>";
entry.accuracy_test<float>(tanh_remez_pade_ec_T3_3<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1646, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "remez_pade", "float2x4", "simd",  };
entry.subName = "tanh_remez_pade_ec_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_pade_ec_T3_3<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1647, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "remez_pade", "float", "simd",  };
entry.subName = "tanh_remez_pade_ec_T3_3_float_simd";
entry.accuracy_test<float>(tanh_remez_pade_ec_T3_3_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1648, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "float", "scalar",  };
entry.subName = "tanh_ec_T3_3<float>";
entry.accuracy_test<float>(tanh_ec_T3_3<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1649, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "float2x4", "simd",  };
entry.subName = "tanh_ec_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_ec_T3_3<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1650, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "float", "simd",  };
entry.subName = "tanh_ec_T3_3_float_simd";
entry.accuracy_test<float>(tanh_ec_T3_3_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1651, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "remez", "float", "scalar",  };
entry.subName = "tanh_remez_T3_0<float>";
entry.accuracy_test<float>(tanh_remez_T3_0<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1652, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "remez", "float2x4", "simd",  };
entry.subName = "tanh_remez_T3_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_T3_0<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1653, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "remez", "float", "simd",  };
entry.subName = "tanh_remez_T3_0_float_simd";
entry.accuracy_test<float>(tanh_remez_T3_0_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1654, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "remez_pade", "float", "scalar",  };
entry.subName = "tanh_remez_pade_T3_3<float>";
entry.accuracy_test<float>(tanh_remez_pade_T3_3<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1655, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "remez_pade", "float2x4", "simd",  };
entry.subName = "tanh_remez_pade_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_pade_T3_3<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1656, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "remez_pade", "float", "simd",  };
entry.subName = "tanh_remez_pade_T3_3_float_simd";
entry.accuracy_test<float>(tanh_remez_pade_T3_3_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1657, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "float", "scalar",  };
entry.subName = "tanh_T3_3<float>";
entry.accuracy_test<float>(tanh_T3_3<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1658, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "float2x4", "simd",  };
entry.subName = "tanh_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_T3_3<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1659, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "float", "simd",  };
entry.subName = "tanh_T3_3_float_simd";
entry.accuracy_test<float>(tanh_T3_3_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1660, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "remez", "float", "scalar",  };
entry.subName = "tanh_remez_fma_ec_T4_0<float>";
entry.accuracy_test<float>(tanh_remez_fma_ec_T4_0<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1661, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "remez", "float2x4", "simd",  };
entry.subName = "tanh_remez_fma_ec_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_fma_ec_T4_0<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1662, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "remez", "float", "simd",  };
entry.subName = "tanh_remez_fma_ec_T4_0_float_simd";
entry.accuracy_test<float>(tanh_remez_fma_ec_T4_0_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1663, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "remez_pade", "float", "scalar",  };
entry.subName = "tanh_remez_pade_fma_ec_T4_4<float>";
entry.accuracy_test<float>(tanh_remez_pade_fma_ec_T4_4<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1664, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "remez_pade", "float2x4", "simd",  };
entry.subName = "tanh_remez_pade_fma_ec_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_pade_fma_ec_T4_4<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1665, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "remez_pade", "float", "simd",  };
entry.subName = "tanh_remez_pade_fma_ec_T4_4_float_simd";
entry.accuracy_test<float>(tanh_remez_pade_fma_ec_T4_4_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1666, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "float", "scalar",  };
entry.subName = "tanh_fma_ec_T4_4<float>";
entry.accuracy_test<float>(tanh_fma_ec_T4_4<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1667, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "float2x4", "simd",  };
entry.subName = "tanh_fma_ec_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_fma_ec_T4_4<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1668, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "float", "simd",  };
entry.subName = "tanh_fma_ec_T4_4_float_simd";
entry.accuracy_test<float>(tanh_fma_ec_T4_4_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1669, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "remez", "float", "scalar",  };
entry.subName = "tanh_remez_fma_T4_0<float>";
entry.accuracy_test<float>(tanh_remez_fma_T4_0<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1670, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "remez", "float2x4", "simd",  };
entry.subName = "tanh_remez_fma_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_fma_T4_0<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1671, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "remez", "float", "simd",  };
entry.subName = "tanh_remez_fma_T4_0_float_simd";
entry.accuracy_test<float>(tanh_remez_fma_T4_0_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1672, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "remez_pade", "float", "scalar",  };
entry.subName = "tanh_remez_pade_fma_T4_4<float>";
entry.accuracy_test<float>(tanh_remez_pade_fma_T4_4<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1673, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "remez_pade", "float2x4", "simd",  };
entry.subName = "tanh_remez_pade_fma_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_pade_fma_T4_4<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1674, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "remez_pade", "float", "simd",  };
entry.subName = "tanh_remez_pade_fma_T4_4_float_simd";
entry.accuracy_test<float>(tanh_remez_pade_fma_T4_4_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1675, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "float", "scalar",  };
entry.subName = "tanh_fma_T4_4<float>";
entry.accuracy_test<float>(tanh_fma_T4_4<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1676, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "float2x4", "simd",  };
entry.subName = "tanh_fma_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_fma_T4_4<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1677, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "float", "simd",  };
entry.subName = "tanh_fma_T4_4_float_simd";
entry.accuracy_test<float>(tanh_fma_T4_4_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1678, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "remez", "float", "scalar",  };
entry.subName = "tanh_remez_ec_T4_0<float>";
entry.accuracy_test<float>(tanh_remez_ec_T4_0<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1679, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "remez", "float2x4", "simd",  };
entry.subName = "tanh_remez_ec_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_ec_T4_0<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1680, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "remez", "float", "simd",  };
entry.subName = "tanh_remez_ec_T4_0_float_simd";
entry.accuracy_test<float>(tanh_remez_ec_T4_0_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1681, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "remez_pade", "float", "scalar",  };
entry.subName = "tanh_remez_pade_ec_T4_4<float>";
entry.accuracy_test<float>(tanh_remez_pade_ec_T4_4<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1682, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "remez_pade", "float2x4", "simd",  };
entry.subName = "tanh_remez_pade_ec_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_pade_ec_T4_4<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1683, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "remez_pade", "float", "simd",  };
entry.subName = "tanh_remez_pade_ec_T4_4_float_simd";
entry.accuracy_test<float>(tanh_remez_pade_ec_T4_4_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1684, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "float", "scalar",  };
entry.subName = "tanh_ec_T4_4<float>";
entry.accuracy_test<float>(tanh_ec_T4_4<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1685, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "float2x4", "simd",  };
entry.subName = "tanh_ec_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_ec_T4_4<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1686, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "float", "simd",  };
entry.subName = "tanh_ec_T4_4_float_simd";
entry.accuracy_test<float>(tanh_ec_T4_4_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1687, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "remez", "float", "scalar",  };
entry.subName = "tanh_remez_T4_0<float>";
entry.accuracy_test<float>(tanh_remez_T4_0<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1688, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "remez", "float2x4", "simd",  };
entry.subName = "tanh_remez_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_T4_0<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1689, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "remez", "float", "simd",  };
entry.subName = "tanh_remez_T4_0_float_simd";
entry.accuracy_test<float>(tanh_remez_T4_0_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1690, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "remez_pade", "float", "scalar",  };
entry.subName = "tanh_remez_pade_T4_4<float>";
entry.accuracy_test<float>(tanh_remez_pade_T4_4<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1691, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "remez_pade", "float2x4", "simd",  };
entry.subName = "tanh_remez_pade_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_pade_T4_4<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1692, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "remez_pade", "float", "simd",  };
entry.subName = "tanh_remez_pade_T4_4_float_simd";
entry.accuracy_test<float>(tanh_remez_pade_T4_4_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1693, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "float", "scalar",  };
entry.subName = "tanh_T4_4<float>";
entry.accuracy_test<float>(tanh_T4_4<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1694, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "float2x4", "simd",  };
entry.subName = "tanh_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_T4_4<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1695, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "float", "simd",  };
entry.subName = "tanh_T4_4_float_simd";
entry.accuracy_test<float>(tanh_T4_4_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1696, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "remez", "float", "scalar",  };
entry.subName = "tanh_remez_fma_ec_T5_0<float>";
entry.accuracy_test<float>(tanh_remez_fma_ec_T5_0<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1697, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "remez", "float2x4", "simd",  };
entry.subName = "tanh_remez_fma_ec_T5_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_fma_ec_T5_0<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1698, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "remez", "float", "simd",  };
entry.subName = "tanh_remez_fma_ec_T5_0_float_simd";
entry.accuracy_test<float>(tanh_remez_fma_ec_T5_0_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1699, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "remez_pade", "float", "scalar",  };
entry.subName = "tanh_remez_pade_fma_ec_T5_5<float>";
entry.accuracy_test<float>(tanh_remez_pade_fma_ec_T5_5<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1700, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "remez_pade", "float2x4", "simd",  };
entry.subName = "tanh_remez_pade_fma_ec_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_pade_fma_ec_T5_5<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1701, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "remez_pade", "float", "simd",  };
entry.subName = "tanh_remez_pade_fma_ec_T5_5_float_simd";
entry.accuracy_test<float>(tanh_remez_pade_fma_ec_T5_5_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1702, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "float", "scalar",  };
entry.subName = "tanh_fma_ec_T5_5<float>";
entry.accuracy_test<float>(tanh_fma_ec_T5_5<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1703, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "float2x4", "simd",  };
entry.subName = "tanh_fma_ec_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_fma_ec_T5_5<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1704, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "float", "simd",  };
entry.subName = "tanh_fma_ec_T5_5_float_simd";
entry.accuracy_test<float>(tanh_fma_ec_T5_5_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1705, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "remez", "float", "scalar",  };
entry.subName = "tanh_remez_fma_T5_0<float>";
entry.accuracy_test<float>(tanh_remez_fma_T5_0<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1706, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "remez", "float2x4", "simd",  };
entry.subName = "tanh_remez_fma_T5_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_fma_T5_0<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1707, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "remez", "float", "simd",  };
entry.subName = "tanh_remez_fma_T5_0_float_simd";
entry.accuracy_test<float>(tanh_remez_fma_T5_0_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1708, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "remez_pade", "float", "scalar",  };
entry.subName = "tanh_remez_pade_fma_T5_5<float>";
entry.accuracy_test<float>(tanh_remez_pade_fma_T5_5<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1709, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "remez_pade", "float2x4", "simd",  };
entry.subName = "tanh_remez_pade_fma_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_pade_fma_T5_5<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1710, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "remez_pade", "float", "simd",  };
entry.subName = "tanh_remez_pade_fma_T5_5_float_simd";
entry.accuracy_test<float>(tanh_remez_pade_fma_T5_5_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1711, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "float", "scalar",  };
entry.subName = "tanh_fma_T5_5<float>";
entry.accuracy_test<float>(tanh_fma_T5_5<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1712, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "float2x4", "simd",  };
entry.subName = "tanh_fma_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_fma_T5_5<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1713, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "float", "simd",  };
entry.subName = "tanh_fma_T5_5_float_simd";
entry.accuracy_test<float>(tanh_fma_T5_5_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1714, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "remez", "float", "scalar",  };
entry.subName = "tanh_remez_ec_T5_0<float>";
entry.accuracy_test<float>(tanh_remez_ec_T5_0<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1715, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "remez", "float2x4", "simd",  };
entry.subName = "tanh_remez_ec_T5_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_ec_T5_0<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1716, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "remez", "float", "simd",  };
entry.subName = "tanh_remez_ec_T5_0_float_simd";
entry.accuracy_test<float>(tanh_remez_ec_T5_0_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1717, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "remez_pade", "float", "scalar",  };
entry.subName = "tanh_remez_pade_ec_T5_5<float>";
entry.accuracy_test<float>(tanh_remez_pade_ec_T5_5<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1718, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "remez_pade", "float2x4", "simd",  };
entry.subName = "tanh_remez_pade_ec_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_pade_ec_T5_5<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1719, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "remez_pade", "float", "simd",  };
entry.subName = "tanh_remez_pade_ec_T5_5_float_simd";
entry.accuracy_test<float>(tanh_remez_pade_ec_T5_5_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1720, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "float", "scalar",  };
entry.subName = "tanh_ec_T5_5<float>";
entry.accuracy_test<float>(tanh_ec_T5_5<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1721, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "float2x4", "simd",  };
entry.subName = "tanh_ec_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_ec_T5_5<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1722, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "float", "simd",  };
entry.subName = "tanh_ec_T5_5_float_simd";
entry.accuracy_test<float>(tanh_ec_T5_5_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1723, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "remez", "float", "scalar",  };
entry.subName = "tanh_remez_T5_0<float>";
entry.accuracy_test<float>(tanh_remez_T5_0<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1724, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "remez", "float2x4", "simd",  };
entry.subName = "tanh_remez_T5_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_T5_0<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1725, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "remez", "float", "simd",  };
entry.subName = "tanh_remez_T5_0_float_simd";
entry.accuracy_test<float>(tanh_remez_T5_0_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1726, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "remez_pade", "float", "scalar",  };
entry.subName = "tanh_remez_pade_T5_5<float>";
entry.accuracy_test<float>(tanh_remez_pade_T5_5<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1727, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "remez_pade", "float2x4", "simd",  };
entry.subName = "tanh_remez_pade_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_pade_T5_5<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1728, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "remez_pade", "float", "simd",  };
entry.subName = "tanh_remez_pade_T5_5_float_simd";
entry.accuracy_test<float>(tanh_remez_pade_T5_5_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1729, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "float", "scalar",  };
entry.subName = "tanh_T5_5<float>";
entry.accuracy_test<float>(tanh_T5_5<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1730, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "float2x4", "simd",  };
entry.subName = "tanh_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_T5_5<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1731, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "float", "simd",  };
entry.subName = "tanh_T5_5_float_simd";
entry.accuracy_test<float>(tanh_T5_5_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1732, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "remez", "float", "scalar",  };
entry.subName = "tanh_remez_fma_ec_T6_0<float>";
entry.accuracy_test<float>(tanh_remez_fma_ec_T6_0<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1733, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "remez", "float2x4", "simd",  };
entry.subName = "tanh_remez_fma_ec_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_fma_ec_T6_0<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1734, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "remez", "float", "simd",  };
entry.subName = "tanh_remez_fma_ec_T6_0_float_simd";
entry.accuracy_test<float>(tanh_remez_fma_ec_T6_0_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1735, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "remez_pade", "float", "scalar",  };
entry.subName = "tanh_remez_pade_fma_ec_T6_6<float>";
entry.accuracy_test<float>(tanh_remez_pade_fma_ec_T6_6<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1736, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "remez_pade", "float2x4", "simd",  };
entry.subName = "tanh_remez_pade_fma_ec_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_pade_fma_ec_T6_6<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1737, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "remez_pade", "float", "simd",  };
entry.subName = "tanh_remez_pade_fma_ec_T6_6_float_simd";
entry.accuracy_test<float>(tanh_remez_pade_fma_ec_T6_6_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1738, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "float", "scalar",  };
entry.subName = "tanh_fma_ec_T6_6<float>";
entry.accuracy_test<float>(tanh_fma_ec_T6_6<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1739, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "float2x4", "simd",  };
entry.subName = "tanh_fma_ec_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_fma_ec_T6_6<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1740, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "float", "simd",  };
entry.subName = "tanh_fma_ec_T6_6_float_simd";
entry.accuracy_test<float>(tanh_fma_ec_T6_6_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1741, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "remez", "float", "scalar",  };
entry.subName = "tanh_remez_fma_T6_0<float>";
entry.accuracy_test<float>(tanh_remez_fma_T6_0<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1742, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "remez", "float2x4", "simd",  };
entry.subName = "tanh_remez_fma_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_fma_T6_0<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1743, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "remez", "float", "simd",  };
entry.subName = "tanh_remez_fma_T6_0_float_simd";
entry.accuracy_test<float>(tanh_remez_fma_T6_0_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1744, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "remez_pade", "float", "scalar",  };
entry.subName = "tanh_remez_pade_fma_T6_6<float>";
entry.accuracy_test<float>(tanh_remez_pade_fma_T6_6<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1745, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "remez_pade", "float2x4", "simd",  };
entry.subName = "tanh_remez_pade_fma_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_pade_fma_T6_6<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1746, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "remez_pade", "float", "simd",  };
entry.subName = "tanh_remez_pade_fma_T6_6_float_simd";
entry.accuracy_test<float>(tanh_remez_pade_fma_T6_6_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1747, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "float", "scalar",  };
entry.subName = "tanh_fma_T6_6<float>";
entry.accuracy_test<float>(tanh_fma_T6_6<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1748, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "float2x4", "simd",  };
entry.subName = "tanh_fma_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_fma_T6_6<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1749, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "float", "simd",  };
entry.subName = "tanh_fma_T6_6_float_simd";
entry.accuracy_test<float>(tanh_fma_T6_6_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1750, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "remez", "float", "scalar",  };
entry.subName = "tanh_remez_ec_T6_0<float>";
entry.accuracy_test<float>(tanh_remez_ec_T6_0<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1751, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "remez", "float2x4", "simd",  };
entry.subName = "tanh_remez_ec_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_ec_T6_0<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1752, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "remez", "float", "simd",  };
entry.subName = "tanh_remez_ec_T6_0_float_simd";
entry.accuracy_test<float>(tanh_remez_ec_T6_0_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1753, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "remez_pade", "float", "scalar",  };
entry.subName = "tanh_remez_pade_ec_T6_6<float>";
entry.accuracy_test<float>(tanh_remez_pade_ec_T6_6<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1754, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "remez_pade", "float2x4", "simd",  };
entry.subName = "tanh_remez_pade_ec_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_pade_ec_T6_6<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1755, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "remez_pade", "float", "simd",  };
entry.subName = "tanh_remez_pade_ec_T6_6_float_simd";
entry.accuracy_test<float>(tanh_remez_pade_ec_T6_6_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1756, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "float", "scalar",  };
entry.subName = "tanh_ec_T6_6<float>";
entry.accuracy_test<float>(tanh_ec_T6_6<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1757, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "float2x4", "simd",  };
entry.subName = "tanh_ec_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_ec_T6_6<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1758, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "float", "simd",  };
entry.subName = "tanh_ec_T6_6_float_simd";
entry.accuracy_test<float>(tanh_ec_T6_6_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1759, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "remez", "float", "scalar",  };
entry.subName = "tanh_remez_T6_0<float>";
entry.accuracy_test<float>(tanh_remez_T6_0<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1760, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "remez", "float2x4", "simd",  };
entry.subName = "tanh_remez_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_T6_0<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1761, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "remez", "float", "simd",  };
entry.subName = "tanh_remez_T6_0_float_simd";
entry.accuracy_test<float>(tanh_remez_T6_0_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1762, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "remez_pade", "float", "scalar",  };
entry.subName = "tanh_remez_pade_T6_6<float>";
entry.accuracy_test<float>(tanh_remez_pade_T6_6<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1763, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "remez_pade", "float2x4", "simd",  };
entry.subName = "tanh_remez_pade_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_pade_T6_6<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1764, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "remez_pade", "float", "simd",  };
entry.subName = "tanh_remez_pade_T6_6_float_simd";
entry.accuracy_test<float>(tanh_remez_pade_T6_6_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1765, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "float", "scalar",  };
entry.subName = "tanh_T6_6<float>";
entry.accuracy_test<float>(tanh_T6_6<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1766, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "float2x4", "simd",  };
entry.subName = "tanh_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_T6_6<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1767, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "float", "simd",  };
entry.subName = "tanh_T6_6_float_simd";
entry.accuracy_test<float>(tanh_T6_6_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1768, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "remez", "float", "scalar",  };
entry.subName = "tanh_remez_fma_ec_T7_0<float>";
entry.accuracy_test<float>(tanh_remez_fma_ec_T7_0<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1769, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "remez", "float2x4", "simd",  };
entry.subName = "tanh_remez_fma_ec_T7_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_fma_ec_T7_0<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1770, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "remez", "float", "simd",  };
entry.subName = "tanh_remez_fma_ec_T7_0_float_simd";
entry.accuracy_test<float>(tanh_remez_fma_ec_T7_0_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1771, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "remez_pade", "float", "scalar",  };
entry.subName = "tanh_remez_pade_fma_ec_T7_7<float>";
entry.accuracy_test<float>(tanh_remez_pade_fma_ec_T7_7<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1772, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "remez_pade", "float2x4", "simd",  };
entry.subName = "tanh_remez_pade_fma_ec_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_pade_fma_ec_T7_7<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1773, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "remez_pade", "float", "simd",  };
entry.subName = "tanh_remez_pade_fma_ec_T7_7_float_simd";
entry.accuracy_test<float>(tanh_remez_pade_fma_ec_T7_7_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1774, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "float", "scalar",  };
entry.subName = "tanh_fma_ec_T7_7<float>";
entry.accuracy_test<float>(tanh_fma_ec_T7_7<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1775, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "float2x4", "simd",  };
entry.subName = "tanh_fma_ec_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_fma_ec_T7_7<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1776, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "float", "simd",  };
entry.subName = "tanh_fma_ec_T7_7_float_simd";
entry.accuracy_test<float>(tanh_fma_ec_T7_7_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1777, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "remez", "float", "scalar",  };
entry.subName = "tanh_remez_fma_T7_0<float>";
entry.accuracy_test<float>(tanh_remez_fma_T7_0<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1778, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "remez", "float2x4", "simd",  };
entry.subName = "tanh_remez_fma_T7_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_fma_T7_0<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1779, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "remez", "float", "simd",  };
entry.subName = "tanh_remez_fma_T7_0_float_simd";
entry.accuracy_test<float>(tanh_remez_fma_T7_0_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1780, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "remez_pade", "float", "scalar",  };
entry.subName = "tanh_remez_pade_fma_T7_7<float>";
entry.accuracy_test<float>(tanh_remez_pade_fma_T7_7<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1781, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "remez_pade", "float2x4", "simd",  };
entry.subName = "tanh_remez_pade_fma_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_pade_fma_T7_7<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1782, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "remez_pade", "float", "simd",  };
entry.subName = "tanh_remez_pade_fma_T7_7_float_simd";
entry.accuracy_test<float>(tanh_remez_pade_fma_T7_7_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1783, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "float", "scalar",  };
entry.subName = "tanh_fma_T7_7<float>";
entry.accuracy_test<float>(tanh_fma_T7_7<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1784, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "float2x4", "simd",  };
entry.subName = "tanh_fma_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_fma_T7_7<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1785, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "float", "simd",  };
entry.subName = "tanh_fma_T7_7_float_simd";
entry.accuracy_test<float>(tanh_fma_T7_7_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1786, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "remez", "float", "scalar",  };
entry.subName = "tanh_remez_ec_T7_0<float>";
entry.accuracy_test<float>(tanh_remez_ec_T7_0<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1787, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "remez", "float2x4", "simd",  };
entry.subName = "tanh_remez_ec_T7_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_ec_T7_0<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1788, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "remez", "float", "simd",  };
entry.subName = "tanh_remez_ec_T7_0_float_simd";
entry.accuracy_test<float>(tanh_remez_ec_T7_0_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1789, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "remez_pade", "float", "scalar",  };
entry.subName = "tanh_remez_pade_ec_T7_7<float>";
entry.accuracy_test<float>(tanh_remez_pade_ec_T7_7<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1790, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "remez_pade", "float2x4", "simd",  };
entry.subName = "tanh_remez_pade_ec_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_pade_ec_T7_7<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1791, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "remez_pade", "float", "simd",  };
entry.subName = "tanh_remez_pade_ec_T7_7_float_simd";
entry.accuracy_test<float>(tanh_remez_pade_ec_T7_7_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1792, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "float", "scalar",  };
entry.subName = "tanh_ec_T7_7<float>";
entry.accuracy_test<float>(tanh_ec_T7_7<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1793, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "float2x4", "simd",  };
entry.subName = "tanh_ec_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_ec_T7_7<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1794, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "float", "simd",  };
entry.subName = "tanh_ec_T7_7_float_simd";
entry.accuracy_test<float>(tanh_ec_T7_7_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1795, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "remez", "float", "scalar",  };
entry.subName = "tanh_remez_T7_0<float>";
entry.accuracy_test<float>(tanh_remez_T7_0<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1796, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "remez", "float2x4", "simd",  };
entry.subName = "tanh_remez_T7_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_T7_0<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1797, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "remez", "float", "simd",  };
entry.subName = "tanh_remez_T7_0_float_simd";
entry.accuracy_test<float>(tanh_remez_T7_0_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1798, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "remez_pade", "float", "scalar",  };
entry.subName = "tanh_remez_pade_T7_7<float>";
entry.accuracy_test<float>(tanh_remez_pade_T7_7<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1799, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "remez_pade", "float2x4", "simd",  };
entry.subName = "tanh_remez_pade_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_pade_T7_7<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1800, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "remez_pade", "float", "simd",  };
entry.subName = "tanh_remez_pade_T7_7_float_simd";
entry.accuracy_test<float>(tanh_remez_pade_T7_7_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1801, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "float", "scalar",  };
entry.subName = "tanh_T7_7<float>";
entry.accuracy_test<float>(tanh_T7_7<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1802, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "float2x4", "simd",  };
entry.subName = "tanh_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_T7_7<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1803, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "float", "simd",  };
entry.subName = "tanh_T7_7_float_simd";
entry.accuracy_test<float>(tanh_T7_7_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1804, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "remez", "float", "scalar",  };
entry.subName = "tanh_remez_fma_ec_T8_0<float>";
entry.accuracy_test<float>(tanh_remez_fma_ec_T8_0<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1805, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "remez", "float2x4", "simd",  };
entry.subName = "tanh_remez_fma_ec_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_fma_ec_T8_0<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1806, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "remez", "float", "simd",  };
entry.subName = "tanh_remez_fma_ec_T8_0_float_simd";
entry.accuracy_test<float>(tanh_remez_fma_ec_T8_0_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1807, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "remez_pade", "float", "scalar",  };
entry.subName = "tanh_remez_pade_fma_ec_T8_8<float>";
entry.accuracy_test<float>(tanh_remez_pade_fma_ec_T8_8<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1808, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "remez_pade", "float2x4", "simd",  };
entry.subName = "tanh_remez_pade_fma_ec_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_pade_fma_ec_T8_8<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1809, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "remez_pade", "float", "simd",  };
entry.subName = "tanh_remez_pade_fma_ec_T8_8_float_simd";
entry.accuracy_test<float>(tanh_remez_pade_fma_ec_T8_8_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1810, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "float", "scalar",  };
entry.subName = "tanh_fma_ec_T8_8<float>";
entry.accuracy_test<float>(tanh_fma_ec_T8_8<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1811, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "float2x4", "simd",  };
entry.subName = "tanh_fma_ec_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_fma_ec_T8_8<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1812, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "float", "simd",  };
entry.subName = "tanh_fma_ec_T8_8_float_simd";
entry.accuracy_test<float>(tanh_fma_ec_T8_8_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1813, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "remez", "float", "scalar",  };
entry.subName = "tanh_remez_fma_T8_0<float>";
entry.accuracy_test<float>(tanh_remez_fma_T8_0<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1814, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "remez", "float2x4", "simd",  };
entry.subName = "tanh_remez_fma_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_fma_T8_0<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1815, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "remez", "float", "simd",  };
entry.subName = "tanh_remez_fma_T8_0_float_simd";
entry.accuracy_test<float>(tanh_remez_fma_T8_0_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1816, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "remez_pade", "float", "scalar",  };
entry.subName = "tanh_remez_pade_fma_T8_8<float>";
entry.accuracy_test<float>(tanh_remez_pade_fma_T8_8<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1817, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "remez_pade", "float2x4", "simd",  };
entry.subName = "tanh_remez_pade_fma_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_pade_fma_T8_8<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1818, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "remez_pade", "float", "simd",  };
entry.subName = "tanh_remez_pade_fma_T8_8_float_simd";
entry.accuracy_test<float>(tanh_remez_pade_fma_T8_8_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1819, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "float", "scalar",  };
entry.subName = "tanh_fma_T8_8<float>";
entry.accuracy_test<float>(tanh_fma_T8_8<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1820, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "float2x4", "simd",  };
entry.subName = "tanh_fma_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_fma_T8_8<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1821, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "float", "simd",  };
entry.subName = "tanh_fma_T8_8_float_simd";
entry.accuracy_test<float>(tanh_fma_T8_8_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1822, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "remez", "float", "scalar",  };
entry.subName = "tanh_remez_ec_T8_0<float>";
entry.accuracy_test<float>(tanh_remez_ec_T8_0<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1823, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "remez", "float2x4", "simd",  };
entry.subName = "tanh_remez_ec_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_ec_T8_0<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1824, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "remez", "float", "simd",  };
entry.subName = "tanh_remez_ec_T8_0_float_simd";
entry.accuracy_test<float>(tanh_remez_ec_T8_0_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1825, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "remez_pade", "float", "scalar",  };
entry.subName = "tanh_remez_pade_ec_T8_8<float>";
entry.accuracy_test<float>(tanh_remez_pade_ec_T8_8<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1826, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "remez_pade", "float2x4", "simd",  };
entry.subName = "tanh_remez_pade_ec_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_pade_ec_T8_8<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1827, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "remez_pade", "float", "simd",  };
entry.subName = "tanh_remez_pade_ec_T8_8_float_simd";
entry.accuracy_test<float>(tanh_remez_pade_ec_T8_8_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1828, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "float", "scalar",  };
entry.subName = "tanh_ec_T8_8<float>";
entry.accuracy_test<float>(tanh_ec_T8_8<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1829, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "float2x4", "simd",  };
entry.subName = "tanh_ec_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_ec_T8_8<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1830, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "float", "simd",  };
entry.subName = "tanh_ec_T8_8_float_simd";
entry.accuracy_test<float>(tanh_ec_T8_8_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1831, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "remez", "float", "scalar",  };
entry.subName = "tanh_remez_T8_0<float>";
entry.accuracy_test<float>(tanh_remez_T8_0<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1832, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "remez", "float2x4", "simd",  };
entry.subName = "tanh_remez_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_T8_0<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1833, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "remez", "float", "simd",  };
entry.subName = "tanh_remez_T8_0_float_simd";
entry.accuracy_test<float>(tanh_remez_T8_0_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1834, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "remez_pade", "float", "scalar",  };
entry.subName = "tanh_remez_pade_T8_8<float>";
entry.accuracy_test<float>(tanh_remez_pade_T8_8<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1835, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "remez_pade", "float2x4", "simd",  };
entry.subName = "tanh_remez_pade_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_pade_T8_8<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1836, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "remez_pade", "float", "simd",  };
entry.subName = "tanh_remez_pade_T8_8_float_simd";
entry.accuracy_test<float>(tanh_remez_pade_T8_8_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1837, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "float", "scalar",  };
entry.subName = "tanh_T8_8<float>";
entry.accuracy_test<float>(tanh_T8_8<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1838, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "float2x4", "simd",  };
entry.subName = "tanh_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_T8_8<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1839, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "float", "simd",  };
entry.subName = "tanh_T8_8_float_simd";
entry.accuracy_test<float>(tanh_T8_8_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1840, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "remez", "float", "scalar",  };
entry.subName = "tanh_remez_fma_ec_T9_0<float>";
entry.accuracy_test<float>(tanh_remez_fma_ec_T9_0<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1841, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "remez", "float2x4", "simd",  };
entry.subName = "tanh_remez_fma_ec_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_fma_ec_T9_0<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1842, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "remez", "float", "simd",  };
entry.subName = "tanh_remez_fma_ec_T9_0_float_simd";
entry.accuracy_test<float>(tanh_remez_fma_ec_T9_0_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1843, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "remez_pade", "float", "scalar",  };
entry.subName = "tanh_remez_pade_fma_ec_T9_9<float>";
entry.accuracy_test<float>(tanh_remez_pade_fma_ec_T9_9<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1844, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "remez_pade", "float2x4", "simd",  };
entry.subName = "tanh_remez_pade_fma_ec_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_pade_fma_ec_T9_9<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1845, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "remez_pade", "float", "simd",  };
entry.subName = "tanh_remez_pade_fma_ec_T9_9_float_simd";
entry.accuracy_test<float>(tanh_remez_pade_fma_ec_T9_9_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1846, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "float", "scalar",  };
entry.subName = "tanh_fma_ec_T9_9<float>";
entry.accuracy_test<float>(tanh_fma_ec_T9_9<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1847, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "float2x4", "simd",  };
entry.subName = "tanh_fma_ec_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_fma_ec_T9_9<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1848, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tanh", "float", "simd",  };
entry.subName = "tanh_fma_ec_T9_9_float_simd";
entry.accuracy_test<float>(tanh_fma_ec_T9_9_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1849, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "remez", "float", "scalar",  };
entry.subName = "tanh_remez_fma_T9_0<float>";
entry.accuracy_test<float>(tanh_remez_fma_T9_0<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1850, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "remez", "float2x4", "simd",  };
entry.subName = "tanh_remez_fma_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_fma_T9_0<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1851, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "remez", "float", "simd",  };
entry.subName = "tanh_remez_fma_T9_0_float_simd";
entry.accuracy_test<float>(tanh_remez_fma_T9_0_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1852, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "remez_pade", "float", "scalar",  };
entry.subName = "tanh_remez_pade_fma_T9_9<float>";
entry.accuracy_test<float>(tanh_remez_pade_fma_T9_9<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1853, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "remez_pade", "float2x4", "simd",  };
entry.subName = "tanh_remez_pade_fma_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_pade_fma_T9_9<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1854, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "remez_pade", "float", "simd",  };
entry.subName = "tanh_remez_pade_fma_T9_9_float_simd";
entry.accuracy_test<float>(tanh_remez_pade_fma_T9_9_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1855, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "float", "scalar",  };
entry.subName = "tanh_fma_T9_9<float>";
entry.accuracy_test<float>(tanh_fma_T9_9<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1856, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "float2x4", "simd",  };
entry.subName = "tanh_fma_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_fma_T9_9<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1857, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tanh", "float", "simd",  };
entry.subName = "tanh_fma_T9_9_float_simd";
entry.accuracy_test<float>(tanh_fma_T9_9_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1858, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "remez", "float", "scalar",  };
entry.subName = "tanh_remez_ec_T9_0<float>";
entry.accuracy_test<float>(tanh_remez_ec_T9_0<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1859, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "remez", "float2x4", "simd",  };
entry.subName = "tanh_remez_ec_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_ec_T9_0<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1860, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "remez", "float", "simd",  };
entry.subName = "tanh_remez_ec_T9_0_float_simd";
entry.accuracy_test<float>(tanh_remez_ec_T9_0_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1861, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "remez_pade", "float", "scalar",  };
entry.subName = "tanh_remez_pade_ec_T9_9<float>";
entry.accuracy_test<float>(tanh_remez_pade_ec_T9_9<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1862, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "remez_pade", "float2x4", "simd",  };
entry.subName = "tanh_remez_pade_ec_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_pade_ec_T9_9<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1863, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "remez_pade", "float", "simd",  };
entry.subName = "tanh_remez_pade_ec_T9_9_float_simd";
entry.accuracy_test<float>(tanh_remez_pade_ec_T9_9_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1864, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "float", "scalar",  };
entry.subName = "tanh_ec_T9_9<float>";
entry.accuracy_test<float>(tanh_ec_T9_9<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1865, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "float2x4", "simd",  };
entry.subName = "tanh_ec_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_ec_T9_9<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1866, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tanh", "float", "simd",  };
entry.subName = "tanh_ec_T9_9_float_simd";
entry.accuracy_test<float>(tanh_ec_T9_9_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1867, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "remez", "float", "scalar",  };
entry.subName = "tanh_remez_T9_0<float>";
entry.accuracy_test<float>(tanh_remez_T9_0<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1868, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "remez", "float2x4", "simd",  };
entry.subName = "tanh_remez_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_T9_0<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1869, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "remez", "float", "simd",  };
entry.subName = "tanh_remez_T9_0_float_simd";
entry.accuracy_test<float>(tanh_remez_T9_0_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1870, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "remez_pade", "float", "scalar",  };
entry.subName = "tanh_remez_pade_T9_9<float>";
entry.accuracy_test<float>(tanh_remez_pade_T9_9<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1871, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "remez_pade", "float2x4", "simd",  };
entry.subName = "tanh_remez_pade_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_remez_pade_T9_9<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1872, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "remez_pade", "float", "simd",  };
entry.subName = "tanh_remez_pade_T9_9_float_simd";
entry.accuracy_test<float>(tanh_remez_pade_T9_9_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1873, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "float", "scalar",  };
entry.subName = "tanh_T9_9<float>";
entry.accuracy_test<float>(tanh_T9_9<float>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1874, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "float2x4", "simd",  };
entry.subName = "tanh_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tanh_T9_9<cr::simd::float2x4>, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1875, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "tanh", "float", "simd",  };
entry.subName = "tanh_T9_9_float_simd";
entry.accuracy_test<float>(tanh_T9_9_float_simd, std::tanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1876, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "std::expf", "float", "scalar", "reference",  };
entry.subName = "std::expf";
entry.accuracy_test<float>(std::expf, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1877, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "remez", "float", "scalar",  };
entry.subName = "exp_remez_fma_T3_0<float>";
entry.accuracy_test<float>(exp_remez_fma_T3_0<float>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1878, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "remez", "float2x4", "simd",  };
entry.subName = "exp_remez_fma_T3_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_remez_fma_T3_0<cr::simd::float2x4>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1879, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "remez", "float", "simd",  };
entry.subName = "exp_remez_fma_T3_0_float_simd";
entry.accuracy_test<float>(exp_remez_fma_T3_0_float_simd, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1880, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "remez_pade", "float", "scalar",  };
entry.subName = "exp_remez_pade_fma_T3_0<float>";
entry.accuracy_test<float>(exp_remez_pade_fma_T3_0<float>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1881, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "remez_pade", "float2x4", "simd",  };
entry.subName = "exp_remez_pade_fma_T3_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_remez_pade_fma_T3_0<cr::simd::float2x4>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1882, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "remez_pade", "float", "simd",  };
entry.subName = "exp_remez_pade_fma_T3_0_float_simd";
entry.accuracy_test<float>(exp_remez_pade_fma_T3_0_float_simd, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1883, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "float", "scalar",  };
entry.subName = "exp_fma_T3_3<float>";
entry.accuracy_test<float>(exp_fma_T3_3<float>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1884, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "float2x4", "simd",  };
entry.subName = "exp_fma_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_fma_T3_3<cr::simd::float2x4>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1885, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "float", "simd",  };
entry.subName = "exp_fma_T3_3_float_simd";
entry.accuracy_test<float>(exp_fma_T3_3_float_simd, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1886, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "remez", "float", "scalar",  };
entry.subName = "exp_remez_T3_0<float>";
entry.accuracy_test<float>(exp_remez_T3_0<float>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1887, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "remez", "float2x4", "simd",  };
entry.subName = "exp_remez_T3_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_remez_T3_0<cr::simd::float2x4>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1888, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "remez", "float", "simd",  };
entry.subName = "exp_remez_T3_0_float_simd";
entry.accuracy_test<float>(exp_remez_T3_0_float_simd, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1889, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "remez_pade", "float", "scalar",  };
entry.subName = "exp_remez_pade_T3_0<float>";
entry.accuracy_test<float>(exp_remez_pade_T3_0<float>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1890, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "remez_pade", "float2x4", "simd",  };
entry.subName = "exp_remez_pade_T3_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_remez_pade_T3_0<cr::simd::float2x4>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1891, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "remez_pade", "float", "simd",  };
entry.subName = "exp_remez_pade_T3_0_float_simd";
entry.accuracy_test<float>(exp_remez_pade_T3_0_float_simd, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1892, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "float", "scalar",  };
entry.subName = "exp_T3_3<float>";
entry.accuracy_test<float>(exp_T3_3<float>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1893, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "float2x4", "simd",  };
entry.subName = "exp_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_T3_3<cr::simd::float2x4>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1894, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "float", "simd",  };
entry.subName = "exp_T3_3_float_simd";
entry.accuracy_test<float>(exp_T3_3_float_simd, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1895, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "remez", "float", "scalar",  };
entry.subName = "exp_remez_fma_T4_0<float>";
entry.accuracy_test<float>(exp_remez_fma_T4_0<float>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1896, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "remez", "float2x4", "simd",  };
entry.subName = "exp_remez_fma_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_remez_fma_T4_0<cr::simd::float2x4>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1897, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "remez", "float", "simd",  };
entry.subName = "exp_remez_fma_T4_0_float_simd";
entry.accuracy_test<float>(exp_remez_fma_T4_0_float_simd, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1898, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "remez_pade", "float", "scalar",  };
entry.subName = "exp_remez_pade_fma_T4_0<float>";
entry.accuracy_test<float>(exp_remez_pade_fma_T4_0<float>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1899, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "remez_pade", "float2x4", "simd",  };
entry.subName = "exp_remez_pade_fma_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_remez_pade_fma_T4_0<cr::simd::float2x4>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1900, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "remez_pade", "float", "simd",  };
entry.subName = "exp_remez_pade_fma_T4_0_float_simd";
entry.accuracy_test<float>(exp_remez_pade_fma_T4_0_float_simd, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1901, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "float", "scalar",  };
entry.subName = "exp_fma_T4_4<float>";
entry.accuracy_test<float>(exp_fma_T4_4<float>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1902, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "float2x4", "simd",  };
entry.subName = "exp_fma_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_fma_T4_4<cr::simd::float2x4>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1903, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "float", "simd",  };
entry.subName = "exp_fma_T4_4_float_simd";
entry.accuracy_test<float>(exp_fma_T4_4_float_simd, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1904, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "remez", "float", "scalar",  };
entry.subName = "exp_remez_T4_0<float>";
entry.accuracy_test<float>(exp_remez_T4_0<float>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1905, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "remez", "float2x4", "simd",  };
entry.subName = "exp_remez_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_remez_T4_0<cr::simd::float2x4>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1906, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "remez", "float", "simd",  };
entry.subName = "exp_remez_T4_0_float_simd";
entry.accuracy_test<float>(exp_remez_T4_0_float_simd, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1907, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "remez_pade", "float", "scalar",  };
entry.subName = "exp_remez_pade_T4_0<float>";
entry.accuracy_test<float>(exp_remez_pade_T4_0<float>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1908, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "remez_pade", "float2x4", "simd",  };
entry.subName = "exp_remez_pade_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_remez_pade_T4_0<cr::simd::float2x4>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1909, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "remez_pade", "float", "simd",  };
entry.subName = "exp_remez_pade_T4_0_float_simd";
entry.accuracy_test<float>(exp_remez_pade_T4_0_float_simd, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1910, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "float", "scalar",  };
entry.subName = "exp_T4_4<float>";
entry.accuracy_test<float>(exp_T4_4<float>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1911, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "float2x4", "simd",  };
entry.subName = "exp_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_T4_4<cr::simd::float2x4>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1912, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "float", "simd",  };
entry.subName = "exp_T4_4_float_simd";
entry.accuracy_test<float>(exp_T4_4_float_simd, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1913, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "remez", "float", "scalar",  };
entry.subName = "exp_remez_fma_T5_0<float>";
entry.accuracy_test<float>(exp_remez_fma_T5_0<float>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1914, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "remez", "float2x4", "simd",  };
entry.subName = "exp_remez_fma_T5_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_remez_fma_T5_0<cr::simd::float2x4>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1915, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "remez", "float", "simd",  };
entry.subName = "exp_remez_fma_T5_0_float_simd";
entry.accuracy_test<float>(exp_remez_fma_T5_0_float_simd, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1916, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "remez_pade", "float", "scalar",  };
entry.subName = "exp_remez_pade_fma_T5_0<float>";
entry.accuracy_test<float>(exp_remez_pade_fma_T5_0<float>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1917, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "remez_pade", "float2x4", "simd",  };
entry.subName = "exp_remez_pade_fma_T5_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_remez_pade_fma_T5_0<cr::simd::float2x4>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1918, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "remez_pade", "float", "simd",  };
entry.subName = "exp_remez_pade_fma_T5_0_float_simd";
entry.accuracy_test<float>(exp_remez_pade_fma_T5_0_float_simd, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1919, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "float", "scalar",  };
entry.subName = "exp_fma_T5_5<float>";
entry.accuracy_test<float>(exp_fma_T5_5<float>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1920, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "float2x4", "simd",  };
entry.subName = "exp_fma_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_fma_T5_5<cr::simd::float2x4>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1921, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "float", "simd",  };
entry.subName = "exp_fma_T5_5_float_simd";
entry.accuracy_test<float>(exp_fma_T5_5_float_simd, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1922, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "remez", "float", "scalar",  };
entry.subName = "exp_remez_T5_0<float>";
entry.accuracy_test<float>(exp_remez_T5_0<float>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1923, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "remez", "float2x4", "simd",  };
entry.subName = "exp_remez_T5_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_remez_T5_0<cr::simd::float2x4>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1924, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "remez", "float", "simd",  };
entry.subName = "exp_remez_T5_0_float_simd";
entry.accuracy_test<float>(exp_remez_T5_0_float_simd, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1925, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "remez_pade", "float", "scalar",  };
entry.subName = "exp_remez_pade_T5_0<float>";
entry.accuracy_test<float>(exp_remez_pade_T5_0<float>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1926, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "remez_pade", "float2x4", "simd",  };
entry.subName = "exp_remez_pade_T5_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_remez_pade_T5_0<cr::simd::float2x4>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1927, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "remez_pade", "float", "simd",  };
entry.subName = "exp_remez_pade_T5_0_float_simd";
entry.accuracy_test<float>(exp_remez_pade_T5_0_float_simd, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1928, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "float", "scalar",  };
entry.subName = "exp_T5_5<float>";
entry.accuracy_test<float>(exp_T5_5<float>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1929, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "float2x4", "simd",  };
entry.subName = "exp_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_T5_5<cr::simd::float2x4>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1930, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "float", "simd",  };
entry.subName = "exp_T5_5_float_simd";
entry.accuracy_test<float>(exp_T5_5_float_simd, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1931, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "remez", "float", "scalar",  };
entry.subName = "exp_remez_fma_T6_0<float>";
entry.accuracy_test<float>(exp_remez_fma_T6_0<float>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1932, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "remez", "float2x4", "simd",  };
entry.subName = "exp_remez_fma_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_remez_fma_T6_0<cr::simd::float2x4>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1933, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "remez", "float", "simd",  };
entry.subName = "exp_remez_fma_T6_0_float_simd";
entry.accuracy_test<float>(exp_remez_fma_T6_0_float_simd, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1934, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "remez_pade", "float", "scalar",  };
entry.subName = "exp_remez_pade_fma_T6_0<float>";
entry.accuracy_test<float>(exp_remez_pade_fma_T6_0<float>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1935, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "remez_pade", "float2x4", "simd",  };
entry.subName = "exp_remez_pade_fma_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_remez_pade_fma_T6_0<cr::simd::float2x4>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1936, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "remez_pade", "float", "simd",  };
entry.subName = "exp_remez_pade_fma_T6_0_float_simd";
entry.accuracy_test<float>(exp_remez_pade_fma_T6_0_float_simd, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1937, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "float", "scalar",  };
entry.subName = "exp_fma_T6_6<float>";
entry.accuracy_test<float>(exp_fma_T6_6<float>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1938, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "float2x4", "simd",  };
entry.subName = "exp_fma_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_fma_T6_6<cr::simd::float2x4>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1939, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "float", "simd",  };
entry.subName = "exp_fma_T6_6_float_simd";
entry.accuracy_test<float>(exp_fma_T6_6_float_simd, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1940, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "remez", "float", "scalar",  };
entry.subName = "exp_remez_T6_0<float>";
entry.accuracy_test<float>(exp_remez_T6_0<float>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1941, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "remez", "float2x4", "simd",  };
entry.subName = "exp_remez_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_remez_T6_0<cr::simd::float2x4>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1942, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "remez", "float", "simd",  };
entry.subName = "exp_remez_T6_0_float_simd";
entry.accuracy_test<float>(exp_remez_T6_0_float_simd, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1943, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "remez_pade", "float", "scalar",  };
entry.subName = "exp_remez_pade_T6_0<float>";
entry.accuracy_test<float>(exp_remez_pade_T6_0<float>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1944, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "remez_pade", "float2x4", "simd",  };
entry.subName = "exp_remez_pade_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_remez_pade_T6_0<cr::simd::float2x4>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1945, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "remez_pade", "float", "simd",  };
entry.subName = "exp_remez_pade_T6_0_float_simd";
entry.accuracy_test<float>(exp_remez_pade_T6_0_float_simd, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1946, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "float", "scalar",  };
entry.subName = "exp_T6_6<float>";
entry.accuracy_test<float>(exp_T6_6<float>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1947, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "float2x4", "simd",  };
entry.subName = "exp_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_T6_6<cr::simd::float2x4>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1948, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "float", "simd",  };
entry.subName = "exp_T6_6_float_simd";
entry.accuracy_test<float>(exp_T6_6_float_simd, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1949, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "remez", "float", "scalar",  };
entry.subName = "exp_remez_fma_T7_0<float>";
entry.accuracy_test<float>(exp_remez_fma_T7_0<float>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1950, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "remez", "float2x4", "simd",  };
entry.subName = "exp_remez_fma_T7_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_remez_fma_T7_0<cr::simd::float2x4>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1951, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "remez", "float", "simd",  };
entry.subName = "exp_remez_fma_T7_0_float_simd";
entry.accuracy_test<float>(exp_remez_fma_T7_0_float_simd, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1952, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "remez_pade", "float", "scalar",  };
entry.subName = "exp_remez_pade_fma_T7_0<float>";
entry.accuracy_test<float>(exp_remez_pade_fma_T7_0<float>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1953, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "remez_pade", "float2x4", "simd",  };
entry.subName = "exp_remez_pade_fma_T7_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_remez_pade_fma_T7_0<cr::simd::float2x4>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1954, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "remez_pade", "float", "simd",  };
entry.subName = "exp_remez_pade_fma_T7_0_float_simd";
entry.accuracy_test<float>(exp_remez_pade_fma_T7_0_float_simd, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1955, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "float", "scalar",  };
entry.subName = "exp_fma_T7_7<float>";
entry.accuracy_test<float>(exp_fma_T7_7<float>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1956, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "float2x4", "simd",  };
entry.subName = "exp_fma_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_fma_T7_7<cr::simd::float2x4>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1957, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "float", "simd",  };
entry.subName = "exp_fma_T7_7_float_simd";
entry.accuracy_test<float>(exp_fma_T7_7_float_simd, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1958, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "remez", "float", "scalar",  };
entry.subName = "exp_remez_T7_0<float>";
entry.accuracy_test<float>(exp_remez_T7_0<float>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1959, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "remez", "float2x4", "simd",  };
entry.subName = "exp_remez_T7_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_remez_T7_0<cr::simd::float2x4>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1960, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "remez", "float", "simd",  };
entry.subName = "exp_remez_T7_0_float_simd";
entry.accuracy_test<float>(exp_remez_T7_0_float_simd, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1961, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "remez_pade", "float", "scalar",  };
entry.subName = "exp_remez_pade_T7_0<float>";
entry.accuracy_test<float>(exp_remez_pade_T7_0<float>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1962, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "remez_pade", "float2x4", "simd",  };
entry.subName = "exp_remez_pade_T7_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_remez_pade_T7_0<cr::simd::float2x4>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1963, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "remez_pade", "float", "simd",  };
entry.subName = "exp_remez_pade_T7_0_float_simd";
entry.accuracy_test<float>(exp_remez_pade_T7_0_float_simd, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1964, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "float", "scalar",  };
entry.subName = "exp_T7_7<float>";
entry.accuracy_test<float>(exp_T7_7<float>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1965, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "float2x4", "simd",  };
entry.subName = "exp_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_T7_7<cr::simd::float2x4>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1966, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "float", "simd",  };
entry.subName = "exp_T7_7_float_simd";
entry.accuracy_test<float>(exp_T7_7_float_simd, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1967, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "remez", "float", "scalar",  };
entry.subName = "exp_remez_fma_T8_0<float>";
entry.accuracy_test<float>(exp_remez_fma_T8_0<float>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1968, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "remez", "float2x4", "simd",  };
entry.subName = "exp_remez_fma_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_remez_fma_T8_0<cr::simd::float2x4>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1969, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "remez", "float", "simd",  };
entry.subName = "exp_remez_fma_T8_0_float_simd";
entry.accuracy_test<float>(exp_remez_fma_T8_0_float_simd, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1970, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "remez_pade", "float", "scalar",  };
entry.subName = "exp_remez_pade_fma_T8_0<float>";
entry.accuracy_test<float>(exp_remez_pade_fma_T8_0<float>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1971, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "remez_pade", "float2x4", "simd",  };
entry.subName = "exp_remez_pade_fma_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_remez_pade_fma_T8_0<cr::simd::float2x4>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1972, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "remez_pade", "float", "simd",  };
entry.subName = "exp_remez_pade_fma_T8_0_float_simd";
entry.accuracy_test<float>(exp_remez_pade_fma_T8_0_float_simd, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1973, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "float", "scalar",  };
entry.subName = "exp_fma_T8_8<float>";
entry.accuracy_test<float>(exp_fma_T8_8<float>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1974, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "float2x4", "simd",  };
entry.subName = "exp_fma_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_fma_T8_8<cr::simd::float2x4>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1975, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "float", "simd",  };
entry.subName = "exp_fma_T8_8_float_simd";
entry.accuracy_test<float>(exp_fma_T8_8_float_simd, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1976, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "remez", "float", "scalar",  };
entry.subName = "exp_remez_T8_0<float>";
entry.accuracy_test<float>(exp_remez_T8_0<float>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1977, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "remez", "float2x4", "simd",  };
entry.subName = "exp_remez_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_remez_T8_0<cr::simd::float2x4>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1978, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "remez", "float", "simd",  };
entry.subName = "exp_remez_T8_0_float_simd";
entry.accuracy_test<float>(exp_remez_T8_0_float_simd, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1979, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "remez_pade", "float", "scalar",  };
entry.subName = "exp_remez_pade_T8_0<float>";
entry.accuracy_test<float>(exp_remez_pade_T8_0<float>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1980, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "remez_pade", "float2x4", "simd",  };
entry.subName = "exp_remez_pade_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_remez_pade_T8_0<cr::simd::float2x4>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1981, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "remez_pade", "float", "simd",  };
entry.subName = "exp_remez_pade_T8_0_float_simd";
entry.accuracy_test<float>(exp_remez_pade_T8_0_float_simd, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1982, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "float", "scalar",  };
entry.subName = "exp_T8_8<float>";
entry.accuracy_test<float>(exp_T8_8<float>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1983, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "float2x4", "simd",  };
entry.subName = "exp_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_T8_8<cr::simd::float2x4>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1984, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "float", "simd",  };
entry.subName = "exp_T8_8_float_simd";
entry.accuracy_test<float>(exp_T8_8_float_simd, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1985, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "remez", "float", "scalar",  };
entry.subName = "exp_remez_fma_T9_0<float>";
entry.accuracy_test<float>(exp_remez_fma_T9_0<float>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1986, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "remez", "float2x4", "simd",  };
entry.subName = "exp_remez_fma_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_remez_fma_T9_0<cr::simd::float2x4>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1987, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "remez", "float", "simd",  };
entry.subName = "exp_remez_fma_T9_0_float_simd";
entry.accuracy_test<float>(exp_remez_fma_T9_0_float_simd, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1988, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "remez_pade", "float", "scalar",  };
entry.subName = "exp_remez_pade_fma_T9_0<float>";
entry.accuracy_test<float>(exp_remez_pade_fma_T9_0<float>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1989, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "remez_pade", "float2x4", "simd",  };
entry.subName = "exp_remez_pade_fma_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_remez_pade_fma_T9_0<cr::simd::float2x4>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1990, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "remez_pade", "float", "simd",  };
entry.subName = "exp_remez_pade_fma_T9_0_float_simd";
entry.accuracy_test<float>(exp_remez_pade_fma_T9_0_float_simd, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1991, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "float", "scalar",  };
entry.subName = "exp_fma_T9_9<float>";
entry.accuracy_test<float>(exp_fma_T9_9<float>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1992, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "float2x4", "simd",  };
entry.subName = "exp_fma_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_fma_T9_9<cr::simd::float2x4>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1993, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp", "float", "simd",  };
entry.subName = "exp_fma_T9_9_float_simd";
entry.accuracy_test<float>(exp_fma_T9_9_float_simd, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1994, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "remez", "float", "scalar",  };
entry.subName = "exp_remez_T9_0<float>";
entry.accuracy_test<float>(exp_remez_T9_0<float>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1995, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "remez", "float2x4", "simd",  };
entry.subName = "exp_remez_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_remez_T9_0<cr::simd::float2x4>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1996, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "remez", "float", "simd",  };
entry.subName = "exp_remez_T9_0_float_simd";
entry.accuracy_test<float>(exp_remez_T9_0_float_simd, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1997, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "remez_pade", "float", "scalar",  };
entry.subName = "exp_remez_pade_T9_0<float>";
entry.accuracy_test<float>(exp_remez_pade_T9_0<float>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_1998, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "remez_pade", "float2x4", "simd",  };
entry.subName = "exp_remez_pade_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_remez_pade_T9_0<cr::simd::float2x4>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_1999, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "remez_pade", "float", "simd",  };
entry.subName = "exp_remez_pade_T9_0_float_simd";
entry.accuracy_test<float>(exp_remez_pade_T9_0_float_simd, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2000, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "float", "scalar",  };
entry.subName = "exp_T9_9<float>";
entry.accuracy_test<float>(exp_T9_9<float>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2001, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "float2x4", "simd",  };
entry.subName = "exp_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_T9_9<cr::simd::float2x4>, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2002, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp", "float", "simd",  };
entry.subName = "exp_T9_9_float_simd";
entry.accuracy_test<float>(exp_T9_9_float_simd, std::expf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2003, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atanh", "std::atanhf", "float", "scalar", "reference",  };
entry.subName = "std::atanhf";
entry.accuracy_test<float>(std::atanhf, std::atanhf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2004, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "remez", "float", "scalar",  };
entry.subName = "atan_remez_fma_ec_T3_0<float>";
entry.accuracy_test<float>(atan_remez_fma_ec_T3_0<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2005, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "remez", "float2x4", "simd",  };
entry.subName = "atan_remez_fma_ec_T3_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_fma_ec_T3_0<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2006, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "remez", "float", "simd",  };
entry.subName = "atan_remez_fma_ec_T3_0_float_simd";
entry.accuracy_test<float>(atan_remez_fma_ec_T3_0_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2007, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "remez_pade", "float", "scalar",  };
entry.subName = "atan_remez_pade_fma_ec_T3_3<float>";
entry.accuracy_test<float>(atan_remez_pade_fma_ec_T3_3<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2008, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "remez_pade", "float2x4", "simd",  };
entry.subName = "atan_remez_pade_fma_ec_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_pade_fma_ec_T3_3<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2009, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "remez_pade", "float", "simd",  };
entry.subName = "atan_remez_pade_fma_ec_T3_3_float_simd";
entry.accuracy_test<float>(atan_remez_pade_fma_ec_T3_3_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2010, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "float", "scalar",  };
entry.subName = "atan_fma_ec_T3_3<float>";
entry.accuracy_test<float>(atan_fma_ec_T3_3<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2011, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "float2x4", "simd",  };
entry.subName = "atan_fma_ec_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_fma_ec_T3_3<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2012, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "float", "simd",  };
entry.subName = "atan_fma_ec_T3_3_float_simd";
entry.accuracy_test<float>(atan_fma_ec_T3_3_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2013, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "remez", "float", "scalar",  };
entry.subName = "atan_remez_fma_T3_0<float>";
entry.accuracy_test<float>(atan_remez_fma_T3_0<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2014, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "remez", "float2x4", "simd",  };
entry.subName = "atan_remez_fma_T3_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_fma_T3_0<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2015, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "remez", "float", "simd",  };
entry.subName = "atan_remez_fma_T3_0_float_simd";
entry.accuracy_test<float>(atan_remez_fma_T3_0_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2016, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "remez_pade", "float", "scalar",  };
entry.subName = "atan_remez_pade_fma_T3_3<float>";
entry.accuracy_test<float>(atan_remez_pade_fma_T3_3<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2017, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "remez_pade", "float2x4", "simd",  };
entry.subName = "atan_remez_pade_fma_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_pade_fma_T3_3<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2018, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "remez_pade", "float", "simd",  };
entry.subName = "atan_remez_pade_fma_T3_3_float_simd";
entry.accuracy_test<float>(atan_remez_pade_fma_T3_3_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2019, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "float", "scalar",  };
entry.subName = "atan_fma_T3_3<float>";
entry.accuracy_test<float>(atan_fma_T3_3<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2020, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "float2x4", "simd",  };
entry.subName = "atan_fma_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_fma_T3_3<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2021, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "float", "simd",  };
entry.subName = "atan_fma_T3_3_float_simd";
entry.accuracy_test<float>(atan_fma_T3_3_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2022, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "remez", "float", "scalar",  };
entry.subName = "atan_remez_ec_T3_0<float>";
entry.accuracy_test<float>(atan_remez_ec_T3_0<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2023, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "remez", "float2x4", "simd",  };
entry.subName = "atan_remez_ec_T3_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_ec_T3_0<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2024, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "remez", "float", "simd",  };
entry.subName = "atan_remez_ec_T3_0_float_simd";
entry.accuracy_test<float>(atan_remez_ec_T3_0_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2025, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "remez_pade", "float", "scalar",  };
entry.subName = "atan_remez_pade_ec_T3_3<float>";
entry.accuracy_test<float>(atan_remez_pade_ec_T3_3<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2026, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "remez_pade", "float2x4", "simd",  };
entry.subName = "atan_remez_pade_ec_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_pade_ec_T3_3<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2027, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "remez_pade", "float", "simd",  };
entry.subName = "atan_remez_pade_ec_T3_3_float_simd";
entry.accuracy_test<float>(atan_remez_pade_ec_T3_3_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2028, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "float", "scalar",  };
entry.subName = "atan_ec_T3_3<float>";
entry.accuracy_test<float>(atan_ec_T3_3<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2029, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "float2x4", "simd",  };
entry.subName = "atan_ec_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_ec_T3_3<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2030, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "float", "simd",  };
entry.subName = "atan_ec_T3_3_float_simd";
entry.accuracy_test<float>(atan_ec_T3_3_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2031, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "remez", "float", "scalar",  };
entry.subName = "atan_remez_T3_0<float>";
entry.accuracy_test<float>(atan_remez_T3_0<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2032, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "remez", "float2x4", "simd",  };
entry.subName = "atan_remez_T3_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_T3_0<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2033, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "remez", "float", "simd",  };
entry.subName = "atan_remez_T3_0_float_simd";
entry.accuracy_test<float>(atan_remez_T3_0_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2034, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "remez_pade", "float", "scalar",  };
entry.subName = "atan_remez_pade_T3_3<float>";
entry.accuracy_test<float>(atan_remez_pade_T3_3<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2035, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "remez_pade", "float2x4", "simd",  };
entry.subName = "atan_remez_pade_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_pade_T3_3<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2036, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "remez_pade", "float", "simd",  };
entry.subName = "atan_remez_pade_T3_3_float_simd";
entry.accuracy_test<float>(atan_remez_pade_T3_3_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2037, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "float", "scalar",  };
entry.subName = "atan_T3_3<float>";
entry.accuracy_test<float>(atan_T3_3<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2038, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "float2x4", "simd",  };
entry.subName = "atan_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_T3_3<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2039, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "float", "simd",  };
entry.subName = "atan_T3_3_float_simd";
entry.accuracy_test<float>(atan_T3_3_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2040, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "remez", "float", "scalar",  };
entry.subName = "atan_remez_fma_ec_T4_0<float>";
entry.accuracy_test<float>(atan_remez_fma_ec_T4_0<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2041, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "remez", "float2x4", "simd",  };
entry.subName = "atan_remez_fma_ec_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_fma_ec_T4_0<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2042, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "remez", "float", "simd",  };
entry.subName = "atan_remez_fma_ec_T4_0_float_simd";
entry.accuracy_test<float>(atan_remez_fma_ec_T4_0_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2043, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "remez_pade", "float", "scalar",  };
entry.subName = "atan_remez_pade_fma_ec_T4_4<float>";
entry.accuracy_test<float>(atan_remez_pade_fma_ec_T4_4<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2044, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "remez_pade", "float2x4", "simd",  };
entry.subName = "atan_remez_pade_fma_ec_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_pade_fma_ec_T4_4<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2045, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "remez_pade", "float", "simd",  };
entry.subName = "atan_remez_pade_fma_ec_T4_4_float_simd";
entry.accuracy_test<float>(atan_remez_pade_fma_ec_T4_4_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2046, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "float", "scalar",  };
entry.subName = "atan_fma_ec_T4_4<float>";
entry.accuracy_test<float>(atan_fma_ec_T4_4<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2047, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "float2x4", "simd",  };
entry.subName = "atan_fma_ec_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_fma_ec_T4_4<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2048, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "float", "simd",  };
entry.subName = "atan_fma_ec_T4_4_float_simd";
entry.accuracy_test<float>(atan_fma_ec_T4_4_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2049, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "remez", "float", "scalar",  };
entry.subName = "atan_remez_fma_T4_0<float>";
entry.accuracy_test<float>(atan_remez_fma_T4_0<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2050, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "remez", "float2x4", "simd",  };
entry.subName = "atan_remez_fma_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_fma_T4_0<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2051, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "remez", "float", "simd",  };
entry.subName = "atan_remez_fma_T4_0_float_simd";
entry.accuracy_test<float>(atan_remez_fma_T4_0_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2052, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "remez_pade", "float", "scalar",  };
entry.subName = "atan_remez_pade_fma_T4_4<float>";
entry.accuracy_test<float>(atan_remez_pade_fma_T4_4<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2053, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "remez_pade", "float2x4", "simd",  };
entry.subName = "atan_remez_pade_fma_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_pade_fma_T4_4<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2054, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "remez_pade", "float", "simd",  };
entry.subName = "atan_remez_pade_fma_T4_4_float_simd";
entry.accuracy_test<float>(atan_remez_pade_fma_T4_4_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2055, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "float", "scalar",  };
entry.subName = "atan_fma_T4_4<float>";
entry.accuracy_test<float>(atan_fma_T4_4<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2056, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "float2x4", "simd",  };
entry.subName = "atan_fma_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_fma_T4_4<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2057, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "float", "simd",  };
entry.subName = "atan_fma_T4_4_float_simd";
entry.accuracy_test<float>(atan_fma_T4_4_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2058, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "remez", "float", "scalar",  };
entry.subName = "atan_remez_ec_T4_0<float>";
entry.accuracy_test<float>(atan_remez_ec_T4_0<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2059, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "remez", "float2x4", "simd",  };
entry.subName = "atan_remez_ec_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_ec_T4_0<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2060, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "remez", "float", "simd",  };
entry.subName = "atan_remez_ec_T4_0_float_simd";
entry.accuracy_test<float>(atan_remez_ec_T4_0_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2061, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "remez_pade", "float", "scalar",  };
entry.subName = "atan_remez_pade_ec_T4_4<float>";
entry.accuracy_test<float>(atan_remez_pade_ec_T4_4<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2062, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "remez_pade", "float2x4", "simd",  };
entry.subName = "atan_remez_pade_ec_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_pade_ec_T4_4<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2063, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "remez_pade", "float", "simd",  };
entry.subName = "atan_remez_pade_ec_T4_4_float_simd";
entry.accuracy_test<float>(atan_remez_pade_ec_T4_4_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2064, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "float", "scalar",  };
entry.subName = "atan_ec_T4_4<float>";
entry.accuracy_test<float>(atan_ec_T4_4<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2065, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "float2x4", "simd",  };
entry.subName = "atan_ec_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_ec_T4_4<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2066, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "float", "simd",  };
entry.subName = "atan_ec_T4_4_float_simd";
entry.accuracy_test<float>(atan_ec_T4_4_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2067, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "remez", "float", "scalar",  };
entry.subName = "atan_remez_T4_0<float>";
entry.accuracy_test<float>(atan_remez_T4_0<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2068, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "remez", "float2x4", "simd",  };
entry.subName = "atan_remez_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_T4_0<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2069, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "remez", "float", "simd",  };
entry.subName = "atan_remez_T4_0_float_simd";
entry.accuracy_test<float>(atan_remez_T4_0_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2070, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "remez_pade", "float", "scalar",  };
entry.subName = "atan_remez_pade_T4_4<float>";
entry.accuracy_test<float>(atan_remez_pade_T4_4<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2071, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "remez_pade", "float2x4", "simd",  };
entry.subName = "atan_remez_pade_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_pade_T4_4<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2072, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "remez_pade", "float", "simd",  };
entry.subName = "atan_remez_pade_T4_4_float_simd";
entry.accuracy_test<float>(atan_remez_pade_T4_4_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2073, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "float", "scalar",  };
entry.subName = "atan_T4_4<float>";
entry.accuracy_test<float>(atan_T4_4<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2074, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "float2x4", "simd",  };
entry.subName = "atan_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_T4_4<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2075, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "float", "simd",  };
entry.subName = "atan_T4_4_float_simd";
entry.accuracy_test<float>(atan_T4_4_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2076, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "remez", "float", "scalar",  };
entry.subName = "atan_remez_fma_ec_T5_0<float>";
entry.accuracy_test<float>(atan_remez_fma_ec_T5_0<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2077, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "remez", "float2x4", "simd",  };
entry.subName = "atan_remez_fma_ec_T5_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_fma_ec_T5_0<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2078, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "remez", "float", "simd",  };
entry.subName = "atan_remez_fma_ec_T5_0_float_simd";
entry.accuracy_test<float>(atan_remez_fma_ec_T5_0_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2079, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "remez_pade", "float", "scalar",  };
entry.subName = "atan_remez_pade_fma_ec_T5_5<float>";
entry.accuracy_test<float>(atan_remez_pade_fma_ec_T5_5<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2080, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "remez_pade", "float2x4", "simd",  };
entry.subName = "atan_remez_pade_fma_ec_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_pade_fma_ec_T5_5<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2081, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "remez_pade", "float", "simd",  };
entry.subName = "atan_remez_pade_fma_ec_T5_5_float_simd";
entry.accuracy_test<float>(atan_remez_pade_fma_ec_T5_5_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2082, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "float", "scalar",  };
entry.subName = "atan_fma_ec_T5_5<float>";
entry.accuracy_test<float>(atan_fma_ec_T5_5<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2083, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "float2x4", "simd",  };
entry.subName = "atan_fma_ec_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_fma_ec_T5_5<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2084, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "float", "simd",  };
entry.subName = "atan_fma_ec_T5_5_float_simd";
entry.accuracy_test<float>(atan_fma_ec_T5_5_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2085, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "remez", "float", "scalar",  };
entry.subName = "atan_remez_fma_T5_0<float>";
entry.accuracy_test<float>(atan_remez_fma_T5_0<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2086, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "remez", "float2x4", "simd",  };
entry.subName = "atan_remez_fma_T5_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_fma_T5_0<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2087, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "remez", "float", "simd",  };
entry.subName = "atan_remez_fma_T5_0_float_simd";
entry.accuracy_test<float>(atan_remez_fma_T5_0_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2088, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "remez_pade", "float", "scalar",  };
entry.subName = "atan_remez_pade_fma_T5_5<float>";
entry.accuracy_test<float>(atan_remez_pade_fma_T5_5<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2089, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "remez_pade", "float2x4", "simd",  };
entry.subName = "atan_remez_pade_fma_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_pade_fma_T5_5<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2090, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "remez_pade", "float", "simd",  };
entry.subName = "atan_remez_pade_fma_T5_5_float_simd";
entry.accuracy_test<float>(atan_remez_pade_fma_T5_5_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2091, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "float", "scalar",  };
entry.subName = "atan_fma_T5_5<float>";
entry.accuracy_test<float>(atan_fma_T5_5<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2092, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "float2x4", "simd",  };
entry.subName = "atan_fma_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_fma_T5_5<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2093, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "float", "simd",  };
entry.subName = "atan_fma_T5_5_float_simd";
entry.accuracy_test<float>(atan_fma_T5_5_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2094, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "remez", "float", "scalar",  };
entry.subName = "atan_remez_ec_T5_0<float>";
entry.accuracy_test<float>(atan_remez_ec_T5_0<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2095, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "remez", "float2x4", "simd",  };
entry.subName = "atan_remez_ec_T5_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_ec_T5_0<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2096, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "remez", "float", "simd",  };
entry.subName = "atan_remez_ec_T5_0_float_simd";
entry.accuracy_test<float>(atan_remez_ec_T5_0_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2097, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "remez_pade", "float", "scalar",  };
entry.subName = "atan_remez_pade_ec_T5_5<float>";
entry.accuracy_test<float>(atan_remez_pade_ec_T5_5<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2098, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "remez_pade", "float2x4", "simd",  };
entry.subName = "atan_remez_pade_ec_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_pade_ec_T5_5<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2099, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "remez_pade", "float", "simd",  };
entry.subName = "atan_remez_pade_ec_T5_5_float_simd";
entry.accuracy_test<float>(atan_remez_pade_ec_T5_5_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2100, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "float", "scalar",  };
entry.subName = "atan_ec_T5_5<float>";
entry.accuracy_test<float>(atan_ec_T5_5<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2101, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "float2x4", "simd",  };
entry.subName = "atan_ec_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_ec_T5_5<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2102, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "float", "simd",  };
entry.subName = "atan_ec_T5_5_float_simd";
entry.accuracy_test<float>(atan_ec_T5_5_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2103, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "remez", "float", "scalar",  };
entry.subName = "atan_remez_T5_0<float>";
entry.accuracy_test<float>(atan_remez_T5_0<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2104, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "remez", "float2x4", "simd",  };
entry.subName = "atan_remez_T5_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_T5_0<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2105, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "remez", "float", "simd",  };
entry.subName = "atan_remez_T5_0_float_simd";
entry.accuracy_test<float>(atan_remez_T5_0_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2106, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "remez_pade", "float", "scalar",  };
entry.subName = "atan_remez_pade_T5_5<float>";
entry.accuracy_test<float>(atan_remez_pade_T5_5<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2107, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "remez_pade", "float2x4", "simd",  };
entry.subName = "atan_remez_pade_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_pade_T5_5<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2108, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "remez_pade", "float", "simd",  };
entry.subName = "atan_remez_pade_T5_5_float_simd";
entry.accuracy_test<float>(atan_remez_pade_T5_5_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2109, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "float", "scalar",  };
entry.subName = "atan_T5_5<float>";
entry.accuracy_test<float>(atan_T5_5<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2110, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "float2x4", "simd",  };
entry.subName = "atan_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_T5_5<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2111, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "float", "simd",  };
entry.subName = "atan_T5_5_float_simd";
entry.accuracy_test<float>(atan_T5_5_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2112, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "remez", "float", "scalar",  };
entry.subName = "atan_remez_fma_ec_T6_0<float>";
entry.accuracy_test<float>(atan_remez_fma_ec_T6_0<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2113, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "remez", "float2x4", "simd",  };
entry.subName = "atan_remez_fma_ec_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_fma_ec_T6_0<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2114, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "remez", "float", "simd",  };
entry.subName = "atan_remez_fma_ec_T6_0_float_simd";
entry.accuracy_test<float>(atan_remez_fma_ec_T6_0_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2115, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "remez_pade", "float", "scalar",  };
entry.subName = "atan_remez_pade_fma_ec_T6_6<float>";
entry.accuracy_test<float>(atan_remez_pade_fma_ec_T6_6<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2116, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "remez_pade", "float2x4", "simd",  };
entry.subName = "atan_remez_pade_fma_ec_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_pade_fma_ec_T6_6<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2117, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "remez_pade", "float", "simd",  };
entry.subName = "atan_remez_pade_fma_ec_T6_6_float_simd";
entry.accuracy_test<float>(atan_remez_pade_fma_ec_T6_6_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2118, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "float", "scalar",  };
entry.subName = "atan_fma_ec_T6_6<float>";
entry.accuracy_test<float>(atan_fma_ec_T6_6<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2119, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "float2x4", "simd",  };
entry.subName = "atan_fma_ec_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_fma_ec_T6_6<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2120, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "float", "simd",  };
entry.subName = "atan_fma_ec_T6_6_float_simd";
entry.accuracy_test<float>(atan_fma_ec_T6_6_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2121, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "remez", "float", "scalar",  };
entry.subName = "atan_remez_fma_T6_0<float>";
entry.accuracy_test<float>(atan_remez_fma_T6_0<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2122, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "remez", "float2x4", "simd",  };
entry.subName = "atan_remez_fma_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_fma_T6_0<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2123, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "remez", "float", "simd",  };
entry.subName = "atan_remez_fma_T6_0_float_simd";
entry.accuracy_test<float>(atan_remez_fma_T6_0_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2124, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "remez_pade", "float", "scalar",  };
entry.subName = "atan_remez_pade_fma_T6_6<float>";
entry.accuracy_test<float>(atan_remez_pade_fma_T6_6<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2125, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "remez_pade", "float2x4", "simd",  };
entry.subName = "atan_remez_pade_fma_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_pade_fma_T6_6<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2126, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "remez_pade", "float", "simd",  };
entry.subName = "atan_remez_pade_fma_T6_6_float_simd";
entry.accuracy_test<float>(atan_remez_pade_fma_T6_6_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2127, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "float", "scalar",  };
entry.subName = "atan_fma_T6_6<float>";
entry.accuracy_test<float>(atan_fma_T6_6<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2128, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "float2x4", "simd",  };
entry.subName = "atan_fma_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_fma_T6_6<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2129, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "float", "simd",  };
entry.subName = "atan_fma_T6_6_float_simd";
entry.accuracy_test<float>(atan_fma_T6_6_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2130, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "remez", "float", "scalar",  };
entry.subName = "atan_remez_ec_T6_0<float>";
entry.accuracy_test<float>(atan_remez_ec_T6_0<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2131, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "remez", "float2x4", "simd",  };
entry.subName = "atan_remez_ec_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_ec_T6_0<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2132, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "remez", "float", "simd",  };
entry.subName = "atan_remez_ec_T6_0_float_simd";
entry.accuracy_test<float>(atan_remez_ec_T6_0_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2133, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "remez_pade", "float", "scalar",  };
entry.subName = "atan_remez_pade_ec_T6_6<float>";
entry.accuracy_test<float>(atan_remez_pade_ec_T6_6<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2134, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "remez_pade", "float2x4", "simd",  };
entry.subName = "atan_remez_pade_ec_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_pade_ec_T6_6<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2135, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "remez_pade", "float", "simd",  };
entry.subName = "atan_remez_pade_ec_T6_6_float_simd";
entry.accuracy_test<float>(atan_remez_pade_ec_T6_6_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2136, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "float", "scalar",  };
entry.subName = "atan_ec_T6_6<float>";
entry.accuracy_test<float>(atan_ec_T6_6<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2137, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "float2x4", "simd",  };
entry.subName = "atan_ec_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_ec_T6_6<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2138, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "float", "simd",  };
entry.subName = "atan_ec_T6_6_float_simd";
entry.accuracy_test<float>(atan_ec_T6_6_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2139, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "remez", "float", "scalar",  };
entry.subName = "atan_remez_T6_0<float>";
entry.accuracy_test<float>(atan_remez_T6_0<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2140, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "remez", "float2x4", "simd",  };
entry.subName = "atan_remez_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_T6_0<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2141, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "remez", "float", "simd",  };
entry.subName = "atan_remez_T6_0_float_simd";
entry.accuracy_test<float>(atan_remez_T6_0_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2142, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "remez_pade", "float", "scalar",  };
entry.subName = "atan_remez_pade_T6_6<float>";
entry.accuracy_test<float>(atan_remez_pade_T6_6<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2143, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "remez_pade", "float2x4", "simd",  };
entry.subName = "atan_remez_pade_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_pade_T6_6<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2144, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "remez_pade", "float", "simd",  };
entry.subName = "atan_remez_pade_T6_6_float_simd";
entry.accuracy_test<float>(atan_remez_pade_T6_6_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2145, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "float", "scalar",  };
entry.subName = "atan_T6_6<float>";
entry.accuracy_test<float>(atan_T6_6<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2146, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "float2x4", "simd",  };
entry.subName = "atan_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_T6_6<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2147, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "float", "simd",  };
entry.subName = "atan_T6_6_float_simd";
entry.accuracy_test<float>(atan_T6_6_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2148, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "remez", "float", "scalar",  };
entry.subName = "atan_remez_fma_ec_T7_0<float>";
entry.accuracy_test<float>(atan_remez_fma_ec_T7_0<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2149, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "remez", "float2x4", "simd",  };
entry.subName = "atan_remez_fma_ec_T7_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_fma_ec_T7_0<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2150, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "remez", "float", "simd",  };
entry.subName = "atan_remez_fma_ec_T7_0_float_simd";
entry.accuracy_test<float>(atan_remez_fma_ec_T7_0_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2151, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "remez_pade", "float", "scalar",  };
entry.subName = "atan_remez_pade_fma_ec_T7_7<float>";
entry.accuracy_test<float>(atan_remez_pade_fma_ec_T7_7<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2152, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "remez_pade", "float2x4", "simd",  };
entry.subName = "atan_remez_pade_fma_ec_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_pade_fma_ec_T7_7<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2153, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "remez_pade", "float", "simd",  };
entry.subName = "atan_remez_pade_fma_ec_T7_7_float_simd";
entry.accuracy_test<float>(atan_remez_pade_fma_ec_T7_7_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2154, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "float", "scalar",  };
entry.subName = "atan_fma_ec_T7_7<float>";
entry.accuracy_test<float>(atan_fma_ec_T7_7<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2155, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "float2x4", "simd",  };
entry.subName = "atan_fma_ec_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_fma_ec_T7_7<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2156, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "float", "simd",  };
entry.subName = "atan_fma_ec_T7_7_float_simd";
entry.accuracy_test<float>(atan_fma_ec_T7_7_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2157, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "remez", "float", "scalar",  };
entry.subName = "atan_remez_fma_T7_0<float>";
entry.accuracy_test<float>(atan_remez_fma_T7_0<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2158, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "remez", "float2x4", "simd",  };
entry.subName = "atan_remez_fma_T7_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_fma_T7_0<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2159, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "remez", "float", "simd",  };
entry.subName = "atan_remez_fma_T7_0_float_simd";
entry.accuracy_test<float>(atan_remez_fma_T7_0_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2160, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "remez_pade", "float", "scalar",  };
entry.subName = "atan_remez_pade_fma_T7_7<float>";
entry.accuracy_test<float>(atan_remez_pade_fma_T7_7<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2161, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "remez_pade", "float2x4", "simd",  };
entry.subName = "atan_remez_pade_fma_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_pade_fma_T7_7<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2162, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "remez_pade", "float", "simd",  };
entry.subName = "atan_remez_pade_fma_T7_7_float_simd";
entry.accuracy_test<float>(atan_remez_pade_fma_T7_7_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2163, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "float", "scalar",  };
entry.subName = "atan_fma_T7_7<float>";
entry.accuracy_test<float>(atan_fma_T7_7<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2164, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "float2x4", "simd",  };
entry.subName = "atan_fma_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_fma_T7_7<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2165, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "float", "simd",  };
entry.subName = "atan_fma_T7_7_float_simd";
entry.accuracy_test<float>(atan_fma_T7_7_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2166, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "remez", "float", "scalar",  };
entry.subName = "atan_remez_ec_T7_0<float>";
entry.accuracy_test<float>(atan_remez_ec_T7_0<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2167, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "remez", "float2x4", "simd",  };
entry.subName = "atan_remez_ec_T7_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_ec_T7_0<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2168, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "remez", "float", "simd",  };
entry.subName = "atan_remez_ec_T7_0_float_simd";
entry.accuracy_test<float>(atan_remez_ec_T7_0_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2169, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "remez_pade", "float", "scalar",  };
entry.subName = "atan_remez_pade_ec_T7_7<float>";
entry.accuracy_test<float>(atan_remez_pade_ec_T7_7<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2170, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "remez_pade", "float2x4", "simd",  };
entry.subName = "atan_remez_pade_ec_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_pade_ec_T7_7<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2171, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "remez_pade", "float", "simd",  };
entry.subName = "atan_remez_pade_ec_T7_7_float_simd";
entry.accuracy_test<float>(atan_remez_pade_ec_T7_7_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2172, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "float", "scalar",  };
entry.subName = "atan_ec_T7_7<float>";
entry.accuracy_test<float>(atan_ec_T7_7<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2173, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "float2x4", "simd",  };
entry.subName = "atan_ec_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_ec_T7_7<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2174, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "float", "simd",  };
entry.subName = "atan_ec_T7_7_float_simd";
entry.accuracy_test<float>(atan_ec_T7_7_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2175, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "remez", "float", "scalar",  };
entry.subName = "atan_remez_T7_0<float>";
entry.accuracy_test<float>(atan_remez_T7_0<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2176, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "remez", "float2x4", "simd",  };
entry.subName = "atan_remez_T7_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_T7_0<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2177, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "remez", "float", "simd",  };
entry.subName = "atan_remez_T7_0_float_simd";
entry.accuracy_test<float>(atan_remez_T7_0_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2178, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "remez_pade", "float", "scalar",  };
entry.subName = "atan_remez_pade_T7_7<float>";
entry.accuracy_test<float>(atan_remez_pade_T7_7<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2179, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "remez_pade", "float2x4", "simd",  };
entry.subName = "atan_remez_pade_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_pade_T7_7<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2180, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "remez_pade", "float", "simd",  };
entry.subName = "atan_remez_pade_T7_7_float_simd";
entry.accuracy_test<float>(atan_remez_pade_T7_7_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2181, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "float", "scalar",  };
entry.subName = "atan_T7_7<float>";
entry.accuracy_test<float>(atan_T7_7<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2182, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "float2x4", "simd",  };
entry.subName = "atan_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_T7_7<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2183, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "float", "simd",  };
entry.subName = "atan_T7_7_float_simd";
entry.accuracy_test<float>(atan_T7_7_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2184, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "remez", "float", "scalar",  };
entry.subName = "atan_remez_fma_ec_T8_0<float>";
entry.accuracy_test<float>(atan_remez_fma_ec_T8_0<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2185, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "remez", "float2x4", "simd",  };
entry.subName = "atan_remez_fma_ec_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_fma_ec_T8_0<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2186, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "remez", "float", "simd",  };
entry.subName = "atan_remez_fma_ec_T8_0_float_simd";
entry.accuracy_test<float>(atan_remez_fma_ec_T8_0_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2187, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "remez_pade", "float", "scalar",  };
entry.subName = "atan_remez_pade_fma_ec_T8_8<float>";
entry.accuracy_test<float>(atan_remez_pade_fma_ec_T8_8<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2188, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "remez_pade", "float2x4", "simd",  };
entry.subName = "atan_remez_pade_fma_ec_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_pade_fma_ec_T8_8<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2189, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "remez_pade", "float", "simd",  };
entry.subName = "atan_remez_pade_fma_ec_T8_8_float_simd";
entry.accuracy_test<float>(atan_remez_pade_fma_ec_T8_8_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2190, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "float", "scalar",  };
entry.subName = "atan_fma_ec_T8_8<float>";
entry.accuracy_test<float>(atan_fma_ec_T8_8<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2191, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "float2x4", "simd",  };
entry.subName = "atan_fma_ec_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_fma_ec_T8_8<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2192, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "float", "simd",  };
entry.subName = "atan_fma_ec_T8_8_float_simd";
entry.accuracy_test<float>(atan_fma_ec_T8_8_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2193, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "remez", "float", "scalar",  };
entry.subName = "atan_remez_fma_T8_0<float>";
entry.accuracy_test<float>(atan_remez_fma_T8_0<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2194, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "remez", "float2x4", "simd",  };
entry.subName = "atan_remez_fma_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_fma_T8_0<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2195, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "remez", "float", "simd",  };
entry.subName = "atan_remez_fma_T8_0_float_simd";
entry.accuracy_test<float>(atan_remez_fma_T8_0_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2196, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "remez_pade", "float", "scalar",  };
entry.subName = "atan_remez_pade_fma_T8_8<float>";
entry.accuracy_test<float>(atan_remez_pade_fma_T8_8<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2197, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "remez_pade", "float2x4", "simd",  };
entry.subName = "atan_remez_pade_fma_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_pade_fma_T8_8<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2198, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "remez_pade", "float", "simd",  };
entry.subName = "atan_remez_pade_fma_T8_8_float_simd";
entry.accuracy_test<float>(atan_remez_pade_fma_T8_8_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2199, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "float", "scalar",  };
entry.subName = "atan_fma_T8_8<float>";
entry.accuracy_test<float>(atan_fma_T8_8<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2200, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "float2x4", "simd",  };
entry.subName = "atan_fma_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_fma_T8_8<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2201, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "float", "simd",  };
entry.subName = "atan_fma_T8_8_float_simd";
entry.accuracy_test<float>(atan_fma_T8_8_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2202, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "remez", "float", "scalar",  };
entry.subName = "atan_remez_ec_T8_0<float>";
entry.accuracy_test<float>(atan_remez_ec_T8_0<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2203, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "remez", "float2x4", "simd",  };
entry.subName = "atan_remez_ec_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_ec_T8_0<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2204, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "remez", "float", "simd",  };
entry.subName = "atan_remez_ec_T8_0_float_simd";
entry.accuracy_test<float>(atan_remez_ec_T8_0_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2205, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "remez_pade", "float", "scalar",  };
entry.subName = "atan_remez_pade_ec_T8_8<float>";
entry.accuracy_test<float>(atan_remez_pade_ec_T8_8<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2206, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "remez_pade", "float2x4", "simd",  };
entry.subName = "atan_remez_pade_ec_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_pade_ec_T8_8<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2207, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "remez_pade", "float", "simd",  };
entry.subName = "atan_remez_pade_ec_T8_8_float_simd";
entry.accuracy_test<float>(atan_remez_pade_ec_T8_8_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2208, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "float", "scalar",  };
entry.subName = "atan_ec_T8_8<float>";
entry.accuracy_test<float>(atan_ec_T8_8<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2209, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "float2x4", "simd",  };
entry.subName = "atan_ec_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_ec_T8_8<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2210, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "float", "simd",  };
entry.subName = "atan_ec_T8_8_float_simd";
entry.accuracy_test<float>(atan_ec_T8_8_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2211, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "remez", "float", "scalar",  };
entry.subName = "atan_remez_T8_0<float>";
entry.accuracy_test<float>(atan_remez_T8_0<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2212, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "remez", "float2x4", "simd",  };
entry.subName = "atan_remez_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_T8_0<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2213, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "remez", "float", "simd",  };
entry.subName = "atan_remez_T8_0_float_simd";
entry.accuracy_test<float>(atan_remez_T8_0_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2214, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "remez_pade", "float", "scalar",  };
entry.subName = "atan_remez_pade_T8_8<float>";
entry.accuracy_test<float>(atan_remez_pade_T8_8<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2215, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "remez_pade", "float2x4", "simd",  };
entry.subName = "atan_remez_pade_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_pade_T8_8<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2216, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "remez_pade", "float", "simd",  };
entry.subName = "atan_remez_pade_T8_8_float_simd";
entry.accuracy_test<float>(atan_remez_pade_T8_8_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2217, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "float", "scalar",  };
entry.subName = "atan_T8_8<float>";
entry.accuracy_test<float>(atan_T8_8<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2218, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "float2x4", "simd",  };
entry.subName = "atan_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_T8_8<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2219, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "float", "simd",  };
entry.subName = "atan_T8_8_float_simd";
entry.accuracy_test<float>(atan_T8_8_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2220, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "remez", "float", "scalar",  };
entry.subName = "atan_remez_fma_ec_T9_0<float>";
entry.accuracy_test<float>(atan_remez_fma_ec_T9_0<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2221, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "remez", "float2x4", "simd",  };
entry.subName = "atan_remez_fma_ec_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_fma_ec_T9_0<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2222, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "remez", "float", "simd",  };
entry.subName = "atan_remez_fma_ec_T9_0_float_simd";
entry.accuracy_test<float>(atan_remez_fma_ec_T9_0_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2223, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "remez_pade", "float", "scalar",  };
entry.subName = "atan_remez_pade_fma_ec_T9_9<float>";
entry.accuracy_test<float>(atan_remez_pade_fma_ec_T9_9<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2224, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "remez_pade", "float2x4", "simd",  };
entry.subName = "atan_remez_pade_fma_ec_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_pade_fma_ec_T9_9<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2225, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "remez_pade", "float", "simd",  };
entry.subName = "atan_remez_pade_fma_ec_T9_9_float_simd";
entry.accuracy_test<float>(atan_remez_pade_fma_ec_T9_9_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2226, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "float", "scalar",  };
entry.subName = "atan_fma_ec_T9_9<float>";
entry.accuracy_test<float>(atan_fma_ec_T9_9<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2227, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "float2x4", "simd",  };
entry.subName = "atan_fma_ec_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_fma_ec_T9_9<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2228, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "atan", "float", "simd",  };
entry.subName = "atan_fma_ec_T9_9_float_simd";
entry.accuracy_test<float>(atan_fma_ec_T9_9_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2229, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "remez", "float", "scalar",  };
entry.subName = "atan_remez_fma_T9_0<float>";
entry.accuracy_test<float>(atan_remez_fma_T9_0<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2230, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "remez", "float2x4", "simd",  };
entry.subName = "atan_remez_fma_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_fma_T9_0<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2231, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "remez", "float", "simd",  };
entry.subName = "atan_remez_fma_T9_0_float_simd";
entry.accuracy_test<float>(atan_remez_fma_T9_0_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2232, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "remez_pade", "float", "scalar",  };
entry.subName = "atan_remez_pade_fma_T9_9<float>";
entry.accuracy_test<float>(atan_remez_pade_fma_T9_9<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2233, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "remez_pade", "float2x4", "simd",  };
entry.subName = "atan_remez_pade_fma_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_pade_fma_T9_9<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2234, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "remez_pade", "float", "simd",  };
entry.subName = "atan_remez_pade_fma_T9_9_float_simd";
entry.accuracy_test<float>(atan_remez_pade_fma_T9_9_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2235, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "float", "scalar",  };
entry.subName = "atan_fma_T9_9<float>";
entry.accuracy_test<float>(atan_fma_T9_9<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2236, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "float2x4", "simd",  };
entry.subName = "atan_fma_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_fma_T9_9<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2237, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "atan", "float", "simd",  };
entry.subName = "atan_fma_T9_9_float_simd";
entry.accuracy_test<float>(atan_fma_T9_9_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2238, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "remez", "float", "scalar",  };
entry.subName = "atan_remez_ec_T9_0<float>";
entry.accuracy_test<float>(atan_remez_ec_T9_0<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2239, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "remez", "float2x4", "simd",  };
entry.subName = "atan_remez_ec_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_ec_T9_0<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2240, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "remez", "float", "simd",  };
entry.subName = "atan_remez_ec_T9_0_float_simd";
entry.accuracy_test<float>(atan_remez_ec_T9_0_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2241, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "remez_pade", "float", "scalar",  };
entry.subName = "atan_remez_pade_ec_T9_9<float>";
entry.accuracy_test<float>(atan_remez_pade_ec_T9_9<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2242, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "remez_pade", "float2x4", "simd",  };
entry.subName = "atan_remez_pade_ec_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_pade_ec_T9_9<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2243, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "remez_pade", "float", "simd",  };
entry.subName = "atan_remez_pade_ec_T9_9_float_simd";
entry.accuracy_test<float>(atan_remez_pade_ec_T9_9_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2244, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "float", "scalar",  };
entry.subName = "atan_ec_T9_9<float>";
entry.accuracy_test<float>(atan_ec_T9_9<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2245, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "float2x4", "simd",  };
entry.subName = "atan_ec_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_ec_T9_9<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2246, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "atan", "float", "simd",  };
entry.subName = "atan_ec_T9_9_float_simd";
entry.accuracy_test<float>(atan_ec_T9_9_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2247, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "remez", "float", "scalar",  };
entry.subName = "atan_remez_T9_0<float>";
entry.accuracy_test<float>(atan_remez_T9_0<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2248, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "remez", "float2x4", "simd",  };
entry.subName = "atan_remez_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_T9_0<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2249, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "remez", "float", "simd",  };
entry.subName = "atan_remez_T9_0_float_simd";
entry.accuracy_test<float>(atan_remez_T9_0_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2250, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "remez_pade", "float", "scalar",  };
entry.subName = "atan_remez_pade_T9_9<float>";
entry.accuracy_test<float>(atan_remez_pade_T9_9<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2251, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "remez_pade", "float2x4", "simd",  };
entry.subName = "atan_remez_pade_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_remez_pade_T9_9<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2252, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "remez_pade", "float", "simd",  };
entry.subName = "atan_remez_pade_T9_9_float_simd";
entry.accuracy_test<float>(atan_remez_pade_T9_9_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2253, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "float", "scalar",  };
entry.subName = "atan_T9_9<float>";
entry.accuracy_test<float>(atan_T9_9<float>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2254, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "float2x4", "simd",  };
entry.subName = "atan_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(atan_T9_9<cr::simd::float2x4>, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2255, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "atan", "float", "simd",  };
entry.subName = "atan_T9_9_float_simd";
entry.accuracy_test<float>(atan_T9_9_float_simd, std::atanf, -10.0f, 10.0f, -10.0f, 10.0f);
entry.time<float>(msvc_bug_workaround_2256, -10.0f, 10.0f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "std::logf", "float", "scalar", "reference",  };
entry.subName = "std::logf";
entry.accuracy_test<float>(std::logf, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2257, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_fma_ec_T3_0<float>";
entry.accuracy_test<float>(log_remez_fma_ec_T3_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2258, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_fma_ec_T3_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_fma_ec_T3_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2259, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez", "float", "simd",  };
entry.subName = "log_remez_fma_ec_T3_0_float_simd";
entry.accuracy_test<float>(log_remez_fma_ec_T3_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2260, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_fma_ec_T3_3<float>";
entry.accuracy_test<float>(log_remez_pade_fma_ec_T3_3<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2261, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_fma_ec_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_fma_ec_T3_3<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2262, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_fma_ec_T3_3_float_simd";
entry.accuracy_test<float>(log_remez_pade_fma_ec_T3_3_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2263, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_recip_fma_ec_T3_0<float>";
entry.accuracy_test<float>(log_remez_recip_fma_ec_T3_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2264, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_recip_fma_ec_T3_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_recip_fma_ec_T3_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2265, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez", "float", "simd",  };
entry.subName = "log_remez_recip_fma_ec_T3_0_float_simd";
entry.accuracy_test<float>(log_remez_recip_fma_ec_T3_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2266, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_recip_fma_ec_T3_3<float>";
entry.accuracy_test<float>(log_remez_pade_recip_fma_ec_T3_3<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2267, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_recip_fma_ec_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_recip_fma_ec_T3_3<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2268, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_recip_fma_ec_T3_3_float_simd";
entry.accuracy_test<float>(log_remez_pade_recip_fma_ec_T3_3_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2269, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_fma_T3_0<float>";
entry.accuracy_test<float>(log_remez_fma_T3_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2270, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_fma_T3_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_fma_T3_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2271, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez", "float", "simd",  };
entry.subName = "log_remez_fma_T3_0_float_simd";
entry.accuracy_test<float>(log_remez_fma_T3_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2272, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_fma_T3_3<float>";
entry.accuracy_test<float>(log_remez_pade_fma_T3_3<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2273, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_fma_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_fma_T3_3<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2274, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_fma_T3_3_float_simd";
entry.accuracy_test<float>(log_remez_pade_fma_T3_3_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2275, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_recip_fma_T3_0<float>";
entry.accuracy_test<float>(log_remez_recip_fma_T3_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2276, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_recip_fma_T3_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_recip_fma_T3_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2277, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez", "float", "simd",  };
entry.subName = "log_remez_recip_fma_T3_0_float_simd";
entry.accuracy_test<float>(log_remez_recip_fma_T3_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2278, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_recip_fma_T3_3<float>";
entry.accuracy_test<float>(log_remez_pade_recip_fma_T3_3<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2279, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_recip_fma_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_recip_fma_T3_3<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2280, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_recip_fma_T3_3_float_simd";
entry.accuracy_test<float>(log_remez_pade_recip_fma_T3_3_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2281, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_ec_T3_0<float>";
entry.accuracy_test<float>(log_remez_ec_T3_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2282, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_ec_T3_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_ec_T3_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2283, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez", "float", "simd",  };
entry.subName = "log_remez_ec_T3_0_float_simd";
entry.accuracy_test<float>(log_remez_ec_T3_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2284, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_ec_T3_3<float>";
entry.accuracy_test<float>(log_remez_pade_ec_T3_3<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2285, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_ec_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_ec_T3_3<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2286, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_ec_T3_3_float_simd";
entry.accuracy_test<float>(log_remez_pade_ec_T3_3_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2287, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_recip_ec_T3_0<float>";
entry.accuracy_test<float>(log_remez_recip_ec_T3_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2288, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_recip_ec_T3_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_recip_ec_T3_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2289, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez", "float", "simd",  };
entry.subName = "log_remez_recip_ec_T3_0_float_simd";
entry.accuracy_test<float>(log_remez_recip_ec_T3_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2290, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_recip_ec_T3_3<float>";
entry.accuracy_test<float>(log_remez_pade_recip_ec_T3_3<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2291, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_recip_ec_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_recip_ec_T3_3<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2292, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_recip_ec_T3_3_float_simd";
entry.accuracy_test<float>(log_remez_pade_recip_ec_T3_3_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2293, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_T3_0<float>";
entry.accuracy_test<float>(log_remez_T3_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2294, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_T3_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_T3_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2295, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez", "float", "simd",  };
entry.subName = "log_remez_T3_0_float_simd";
entry.accuracy_test<float>(log_remez_T3_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2296, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_T3_3<float>";
entry.accuracy_test<float>(log_remez_pade_T3_3<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2297, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_T3_3<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2298, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_T3_3_float_simd";
entry.accuracy_test<float>(log_remez_pade_T3_3_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2299, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_recip_T3_0<float>";
entry.accuracy_test<float>(log_remez_recip_T3_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2300, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_recip_T3_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_recip_T3_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2301, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez", "float", "simd",  };
entry.subName = "log_remez_recip_T3_0_float_simd";
entry.accuracy_test<float>(log_remez_recip_T3_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2302, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_recip_T3_3<float>";
entry.accuracy_test<float>(log_remez_pade_recip_T3_3<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2303, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_recip_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_recip_T3_3<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2304, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_recip_T3_3_float_simd";
entry.accuracy_test<float>(log_remez_pade_recip_T3_3_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2305, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "float", "scalar",  };
entry.subName = "log_fma_ec_T3_3<float>";
entry.accuracy_test<float>(log_fma_ec_T3_3<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2306, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "float2x4", "simd",  };
entry.subName = "log_fma_ec_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_fma_ec_T3_3<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2307, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "float", "simd",  };
entry.subName = "log_fma_ec_T3_3_float_simd";
entry.accuracy_test<float>(log_fma_ec_T3_3_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2308, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "float", "scalar",  };
entry.subName = "log_fma_T3_3<float>";
entry.accuracy_test<float>(log_fma_T3_3<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2309, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "float2x4", "simd",  };
entry.subName = "log_fma_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_fma_T3_3<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2310, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "float", "simd",  };
entry.subName = "log_fma_T3_3_float_simd";
entry.accuracy_test<float>(log_fma_T3_3_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2311, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "float", "scalar",  };
entry.subName = "log_ec_T3_3<float>";
entry.accuracy_test<float>(log_ec_T3_3<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2312, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "float2x4", "simd",  };
entry.subName = "log_ec_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_ec_T3_3<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2313, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "float", "simd",  };
entry.subName = "log_ec_T3_3_float_simd";
entry.accuracy_test<float>(log_ec_T3_3_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2314, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "float", "scalar",  };
entry.subName = "log_T3_3<float>";
entry.accuracy_test<float>(log_T3_3<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2315, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "float2x4", "simd",  };
entry.subName = "log_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_T3_3<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2316, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "float", "simd",  };
entry.subName = "log_T3_3_float_simd";
entry.accuracy_test<float>(log_T3_3_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2317, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_fma_ec_T4_0<float>";
entry.accuracy_test<float>(log_remez_fma_ec_T4_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2318, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_fma_ec_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_fma_ec_T4_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2319, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez", "float", "simd",  };
entry.subName = "log_remez_fma_ec_T4_0_float_simd";
entry.accuracy_test<float>(log_remez_fma_ec_T4_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2320, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_fma_ec_T4_4<float>";
entry.accuracy_test<float>(log_remez_pade_fma_ec_T4_4<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2321, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_fma_ec_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_fma_ec_T4_4<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2322, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_fma_ec_T4_4_float_simd";
entry.accuracy_test<float>(log_remez_pade_fma_ec_T4_4_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2323, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_recip_fma_ec_T4_0<float>";
entry.accuracy_test<float>(log_remez_recip_fma_ec_T4_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2324, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_recip_fma_ec_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_recip_fma_ec_T4_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2325, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez", "float", "simd",  };
entry.subName = "log_remez_recip_fma_ec_T4_0_float_simd";
entry.accuracy_test<float>(log_remez_recip_fma_ec_T4_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2326, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_recip_fma_ec_T4_4<float>";
entry.accuracy_test<float>(log_remez_pade_recip_fma_ec_T4_4<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2327, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_recip_fma_ec_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_recip_fma_ec_T4_4<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2328, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_recip_fma_ec_T4_4_float_simd";
entry.accuracy_test<float>(log_remez_pade_recip_fma_ec_T4_4_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2329, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_fma_T4_0<float>";
entry.accuracy_test<float>(log_remez_fma_T4_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2330, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_fma_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_fma_T4_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2331, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez", "float", "simd",  };
entry.subName = "log_remez_fma_T4_0_float_simd";
entry.accuracy_test<float>(log_remez_fma_T4_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2332, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_fma_T4_4<float>";
entry.accuracy_test<float>(log_remez_pade_fma_T4_4<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2333, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_fma_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_fma_T4_4<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2334, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_fma_T4_4_float_simd";
entry.accuracy_test<float>(log_remez_pade_fma_T4_4_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2335, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_recip_fma_T4_0<float>";
entry.accuracy_test<float>(log_remez_recip_fma_T4_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2336, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_recip_fma_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_recip_fma_T4_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2337, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez", "float", "simd",  };
entry.subName = "log_remez_recip_fma_T4_0_float_simd";
entry.accuracy_test<float>(log_remez_recip_fma_T4_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2338, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_recip_fma_T4_4<float>";
entry.accuracy_test<float>(log_remez_pade_recip_fma_T4_4<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2339, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_recip_fma_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_recip_fma_T4_4<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2340, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_recip_fma_T4_4_float_simd";
entry.accuracy_test<float>(log_remez_pade_recip_fma_T4_4_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2341, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_ec_T4_0<float>";
entry.accuracy_test<float>(log_remez_ec_T4_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2342, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_ec_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_ec_T4_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2343, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez", "float", "simd",  };
entry.subName = "log_remez_ec_T4_0_float_simd";
entry.accuracy_test<float>(log_remez_ec_T4_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2344, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_ec_T4_4<float>";
entry.accuracy_test<float>(log_remez_pade_ec_T4_4<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2345, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_ec_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_ec_T4_4<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2346, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_ec_T4_4_float_simd";
entry.accuracy_test<float>(log_remez_pade_ec_T4_4_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2347, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_recip_ec_T4_0<float>";
entry.accuracy_test<float>(log_remez_recip_ec_T4_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2348, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_recip_ec_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_recip_ec_T4_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2349, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez", "float", "simd",  };
entry.subName = "log_remez_recip_ec_T4_0_float_simd";
entry.accuracy_test<float>(log_remez_recip_ec_T4_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2350, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_recip_ec_T4_4<float>";
entry.accuracy_test<float>(log_remez_pade_recip_ec_T4_4<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2351, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_recip_ec_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_recip_ec_T4_4<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2352, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_recip_ec_T4_4_float_simd";
entry.accuracy_test<float>(log_remez_pade_recip_ec_T4_4_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2353, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_T4_0<float>";
entry.accuracy_test<float>(log_remez_T4_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2354, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_T4_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2355, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez", "float", "simd",  };
entry.subName = "log_remez_T4_0_float_simd";
entry.accuracy_test<float>(log_remez_T4_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2356, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_T4_4<float>";
entry.accuracy_test<float>(log_remez_pade_T4_4<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2357, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_T4_4<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2358, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_T4_4_float_simd";
entry.accuracy_test<float>(log_remez_pade_T4_4_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2359, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_recip_T4_0<float>";
entry.accuracy_test<float>(log_remez_recip_T4_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2360, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_recip_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_recip_T4_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2361, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez", "float", "simd",  };
entry.subName = "log_remez_recip_T4_0_float_simd";
entry.accuracy_test<float>(log_remez_recip_T4_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2362, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_recip_T4_4<float>";
entry.accuracy_test<float>(log_remez_pade_recip_T4_4<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2363, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_recip_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_recip_T4_4<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2364, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_recip_T4_4_float_simd";
entry.accuracy_test<float>(log_remez_pade_recip_T4_4_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2365, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "float", "scalar",  };
entry.subName = "log_fma_ec_T4_4<float>";
entry.accuracy_test<float>(log_fma_ec_T4_4<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2366, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "float2x4", "simd",  };
entry.subName = "log_fma_ec_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_fma_ec_T4_4<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2367, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "float", "simd",  };
entry.subName = "log_fma_ec_T4_4_float_simd";
entry.accuracy_test<float>(log_fma_ec_T4_4_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2368, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "float", "scalar",  };
entry.subName = "log_fma_T4_4<float>";
entry.accuracy_test<float>(log_fma_T4_4<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2369, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "float2x4", "simd",  };
entry.subName = "log_fma_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_fma_T4_4<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2370, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "float", "simd",  };
entry.subName = "log_fma_T4_4_float_simd";
entry.accuracy_test<float>(log_fma_T4_4_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2371, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "float", "scalar",  };
entry.subName = "log_ec_T4_4<float>";
entry.accuracy_test<float>(log_ec_T4_4<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2372, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "float2x4", "simd",  };
entry.subName = "log_ec_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_ec_T4_4<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2373, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "float", "simd",  };
entry.subName = "log_ec_T4_4_float_simd";
entry.accuracy_test<float>(log_ec_T4_4_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2374, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "float", "scalar",  };
entry.subName = "log_T4_4<float>";
entry.accuracy_test<float>(log_T4_4<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2375, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "float2x4", "simd",  };
entry.subName = "log_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_T4_4<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2376, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "float", "simd",  };
entry.subName = "log_T4_4_float_simd";
entry.accuracy_test<float>(log_T4_4_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2377, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_fma_ec_T5_0<float>";
entry.accuracy_test<float>(log_remez_fma_ec_T5_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2378, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_fma_ec_T5_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_fma_ec_T5_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2379, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez", "float", "simd",  };
entry.subName = "log_remez_fma_ec_T5_0_float_simd";
entry.accuracy_test<float>(log_remez_fma_ec_T5_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2380, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_fma_ec_T5_5<float>";
entry.accuracy_test<float>(log_remez_pade_fma_ec_T5_5<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2381, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_fma_ec_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_fma_ec_T5_5<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2382, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_fma_ec_T5_5_float_simd";
entry.accuracy_test<float>(log_remez_pade_fma_ec_T5_5_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2383, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_recip_fma_ec_T5_0<float>";
entry.accuracy_test<float>(log_remez_recip_fma_ec_T5_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2384, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_recip_fma_ec_T5_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_recip_fma_ec_T5_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2385, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez", "float", "simd",  };
entry.subName = "log_remez_recip_fma_ec_T5_0_float_simd";
entry.accuracy_test<float>(log_remez_recip_fma_ec_T5_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2386, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_recip_fma_ec_T5_5<float>";
entry.accuracy_test<float>(log_remez_pade_recip_fma_ec_T5_5<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2387, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_recip_fma_ec_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_recip_fma_ec_T5_5<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2388, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_recip_fma_ec_T5_5_float_simd";
entry.accuracy_test<float>(log_remez_pade_recip_fma_ec_T5_5_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2389, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_fma_T5_0<float>";
entry.accuracy_test<float>(log_remez_fma_T5_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2390, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_fma_T5_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_fma_T5_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2391, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez", "float", "simd",  };
entry.subName = "log_remez_fma_T5_0_float_simd";
entry.accuracy_test<float>(log_remez_fma_T5_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2392, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_fma_T5_5<float>";
entry.accuracy_test<float>(log_remez_pade_fma_T5_5<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2393, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_fma_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_fma_T5_5<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2394, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_fma_T5_5_float_simd";
entry.accuracy_test<float>(log_remez_pade_fma_T5_5_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2395, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_recip_fma_T5_0<float>";
entry.accuracy_test<float>(log_remez_recip_fma_T5_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2396, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_recip_fma_T5_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_recip_fma_T5_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2397, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez", "float", "simd",  };
entry.subName = "log_remez_recip_fma_T5_0_float_simd";
entry.accuracy_test<float>(log_remez_recip_fma_T5_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2398, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_recip_fma_T5_5<float>";
entry.accuracy_test<float>(log_remez_pade_recip_fma_T5_5<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2399, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_recip_fma_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_recip_fma_T5_5<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2400, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_recip_fma_T5_5_float_simd";
entry.accuracy_test<float>(log_remez_pade_recip_fma_T5_5_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2401, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_ec_T5_0<float>";
entry.accuracy_test<float>(log_remez_ec_T5_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2402, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_ec_T5_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_ec_T5_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2403, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez", "float", "simd",  };
entry.subName = "log_remez_ec_T5_0_float_simd";
entry.accuracy_test<float>(log_remez_ec_T5_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2404, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_ec_T5_5<float>";
entry.accuracy_test<float>(log_remez_pade_ec_T5_5<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2405, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_ec_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_ec_T5_5<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2406, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_ec_T5_5_float_simd";
entry.accuracy_test<float>(log_remez_pade_ec_T5_5_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2407, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_recip_ec_T5_0<float>";
entry.accuracy_test<float>(log_remez_recip_ec_T5_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2408, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_recip_ec_T5_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_recip_ec_T5_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2409, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez", "float", "simd",  };
entry.subName = "log_remez_recip_ec_T5_0_float_simd";
entry.accuracy_test<float>(log_remez_recip_ec_T5_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2410, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_recip_ec_T5_5<float>";
entry.accuracy_test<float>(log_remez_pade_recip_ec_T5_5<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2411, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_recip_ec_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_recip_ec_T5_5<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2412, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_recip_ec_T5_5_float_simd";
entry.accuracy_test<float>(log_remez_pade_recip_ec_T5_5_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2413, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_T5_0<float>";
entry.accuracy_test<float>(log_remez_T5_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2414, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_T5_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_T5_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2415, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez", "float", "simd",  };
entry.subName = "log_remez_T5_0_float_simd";
entry.accuracy_test<float>(log_remez_T5_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2416, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_T5_5<float>";
entry.accuracy_test<float>(log_remez_pade_T5_5<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2417, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_T5_5<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2418, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_T5_5_float_simd";
entry.accuracy_test<float>(log_remez_pade_T5_5_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2419, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_recip_T5_0<float>";
entry.accuracy_test<float>(log_remez_recip_T5_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2420, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_recip_T5_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_recip_T5_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2421, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez", "float", "simd",  };
entry.subName = "log_remez_recip_T5_0_float_simd";
entry.accuracy_test<float>(log_remez_recip_T5_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2422, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_recip_T5_5<float>";
entry.accuracy_test<float>(log_remez_pade_recip_T5_5<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2423, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_recip_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_recip_T5_5<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2424, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_recip_T5_5_float_simd";
entry.accuracy_test<float>(log_remez_pade_recip_T5_5_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2425, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "float", "scalar",  };
entry.subName = "log_fma_ec_T5_5<float>";
entry.accuracy_test<float>(log_fma_ec_T5_5<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2426, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "float2x4", "simd",  };
entry.subName = "log_fma_ec_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_fma_ec_T5_5<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2427, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "float", "simd",  };
entry.subName = "log_fma_ec_T5_5_float_simd";
entry.accuracy_test<float>(log_fma_ec_T5_5_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2428, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "float", "scalar",  };
entry.subName = "log_fma_T5_5<float>";
entry.accuracy_test<float>(log_fma_T5_5<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2429, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "float2x4", "simd",  };
entry.subName = "log_fma_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_fma_T5_5<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2430, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "float", "simd",  };
entry.subName = "log_fma_T5_5_float_simd";
entry.accuracy_test<float>(log_fma_T5_5_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2431, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "float", "scalar",  };
entry.subName = "log_ec_T5_5<float>";
entry.accuracy_test<float>(log_ec_T5_5<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2432, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "float2x4", "simd",  };
entry.subName = "log_ec_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_ec_T5_5<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2433, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "float", "simd",  };
entry.subName = "log_ec_T5_5_float_simd";
entry.accuracy_test<float>(log_ec_T5_5_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2434, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "float", "scalar",  };
entry.subName = "log_T5_5<float>";
entry.accuracy_test<float>(log_T5_5<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2435, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "float2x4", "simd",  };
entry.subName = "log_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_T5_5<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2436, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "float", "simd",  };
entry.subName = "log_T5_5_float_simd";
entry.accuracy_test<float>(log_T5_5_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2437, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_fma_ec_T6_0<float>";
entry.accuracy_test<float>(log_remez_fma_ec_T6_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2438, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_fma_ec_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_fma_ec_T6_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2439, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez", "float", "simd",  };
entry.subName = "log_remez_fma_ec_T6_0_float_simd";
entry.accuracy_test<float>(log_remez_fma_ec_T6_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2440, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_fma_ec_T6_6<float>";
entry.accuracy_test<float>(log_remez_pade_fma_ec_T6_6<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2441, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_fma_ec_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_fma_ec_T6_6<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2442, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_fma_ec_T6_6_float_simd";
entry.accuracy_test<float>(log_remez_pade_fma_ec_T6_6_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2443, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_recip_fma_ec_T6_0<float>";
entry.accuracy_test<float>(log_remez_recip_fma_ec_T6_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2444, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_recip_fma_ec_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_recip_fma_ec_T6_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2445, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez", "float", "simd",  };
entry.subName = "log_remez_recip_fma_ec_T6_0_float_simd";
entry.accuracy_test<float>(log_remez_recip_fma_ec_T6_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2446, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_recip_fma_ec_T6_6<float>";
entry.accuracy_test<float>(log_remez_pade_recip_fma_ec_T6_6<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2447, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_recip_fma_ec_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_recip_fma_ec_T6_6<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2448, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_recip_fma_ec_T6_6_float_simd";
entry.accuracy_test<float>(log_remez_pade_recip_fma_ec_T6_6_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2449, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_fma_T6_0<float>";
entry.accuracy_test<float>(log_remez_fma_T6_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2450, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_fma_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_fma_T6_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2451, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez", "float", "simd",  };
entry.subName = "log_remez_fma_T6_0_float_simd";
entry.accuracy_test<float>(log_remez_fma_T6_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2452, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_fma_T6_6<float>";
entry.accuracy_test<float>(log_remez_pade_fma_T6_6<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2453, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_fma_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_fma_T6_6<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2454, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_fma_T6_6_float_simd";
entry.accuracy_test<float>(log_remez_pade_fma_T6_6_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2455, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_recip_fma_T6_0<float>";
entry.accuracy_test<float>(log_remez_recip_fma_T6_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2456, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_recip_fma_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_recip_fma_T6_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2457, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez", "float", "simd",  };
entry.subName = "log_remez_recip_fma_T6_0_float_simd";
entry.accuracy_test<float>(log_remez_recip_fma_T6_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2458, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_recip_fma_T6_6<float>";
entry.accuracy_test<float>(log_remez_pade_recip_fma_T6_6<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2459, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_recip_fma_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_recip_fma_T6_6<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2460, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_recip_fma_T6_6_float_simd";
entry.accuracy_test<float>(log_remez_pade_recip_fma_T6_6_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2461, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_ec_T6_0<float>";
entry.accuracy_test<float>(log_remez_ec_T6_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2462, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_ec_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_ec_T6_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2463, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez", "float", "simd",  };
entry.subName = "log_remez_ec_T6_0_float_simd";
entry.accuracy_test<float>(log_remez_ec_T6_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2464, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_ec_T6_6<float>";
entry.accuracy_test<float>(log_remez_pade_ec_T6_6<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2465, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_ec_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_ec_T6_6<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2466, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_ec_T6_6_float_simd";
entry.accuracy_test<float>(log_remez_pade_ec_T6_6_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2467, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_recip_ec_T6_0<float>";
entry.accuracy_test<float>(log_remez_recip_ec_T6_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2468, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_recip_ec_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_recip_ec_T6_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2469, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez", "float", "simd",  };
entry.subName = "log_remez_recip_ec_T6_0_float_simd";
entry.accuracy_test<float>(log_remez_recip_ec_T6_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2470, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_recip_ec_T6_6<float>";
entry.accuracy_test<float>(log_remez_pade_recip_ec_T6_6<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2471, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_recip_ec_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_recip_ec_T6_6<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2472, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_recip_ec_T6_6_float_simd";
entry.accuracy_test<float>(log_remez_pade_recip_ec_T6_6_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2473, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_T6_0<float>";
entry.accuracy_test<float>(log_remez_T6_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2474, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_T6_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2475, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez", "float", "simd",  };
entry.subName = "log_remez_T6_0_float_simd";
entry.accuracy_test<float>(log_remez_T6_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2476, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_T6_6<float>";
entry.accuracy_test<float>(log_remez_pade_T6_6<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2477, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_T6_6<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2478, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_T6_6_float_simd";
entry.accuracy_test<float>(log_remez_pade_T6_6_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2479, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_recip_T6_0<float>";
entry.accuracy_test<float>(log_remez_recip_T6_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2480, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_recip_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_recip_T6_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2481, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez", "float", "simd",  };
entry.subName = "log_remez_recip_T6_0_float_simd";
entry.accuracy_test<float>(log_remez_recip_T6_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2482, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_recip_T6_6<float>";
entry.accuracy_test<float>(log_remez_pade_recip_T6_6<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2483, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_recip_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_recip_T6_6<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2484, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_recip_T6_6_float_simd";
entry.accuracy_test<float>(log_remez_pade_recip_T6_6_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2485, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "float", "scalar",  };
entry.subName = "log_fma_ec_T6_6<float>";
entry.accuracy_test<float>(log_fma_ec_T6_6<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2486, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "float2x4", "simd",  };
entry.subName = "log_fma_ec_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_fma_ec_T6_6<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2487, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "float", "simd",  };
entry.subName = "log_fma_ec_T6_6_float_simd";
entry.accuracy_test<float>(log_fma_ec_T6_6_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2488, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "float", "scalar",  };
entry.subName = "log_fma_T6_6<float>";
entry.accuracy_test<float>(log_fma_T6_6<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2489, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "float2x4", "simd",  };
entry.subName = "log_fma_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_fma_T6_6<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2490, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "float", "simd",  };
entry.subName = "log_fma_T6_6_float_simd";
entry.accuracy_test<float>(log_fma_T6_6_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2491, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "float", "scalar",  };
entry.subName = "log_ec_T6_6<float>";
entry.accuracy_test<float>(log_ec_T6_6<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2492, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "float2x4", "simd",  };
entry.subName = "log_ec_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_ec_T6_6<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2493, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "float", "simd",  };
entry.subName = "log_ec_T6_6_float_simd";
entry.accuracy_test<float>(log_ec_T6_6_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2494, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "float", "scalar",  };
entry.subName = "log_T6_6<float>";
entry.accuracy_test<float>(log_T6_6<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2495, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "float2x4", "simd",  };
entry.subName = "log_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_T6_6<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2496, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "float", "simd",  };
entry.subName = "log_T6_6_float_simd";
entry.accuracy_test<float>(log_T6_6_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2497, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_fma_ec_T7_0<float>";
entry.accuracy_test<float>(log_remez_fma_ec_T7_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2498, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_fma_ec_T7_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_fma_ec_T7_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2499, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez", "float", "simd",  };
entry.subName = "log_remez_fma_ec_T7_0_float_simd";
entry.accuracy_test<float>(log_remez_fma_ec_T7_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2500, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_fma_ec_T7_7<float>";
entry.accuracy_test<float>(log_remez_pade_fma_ec_T7_7<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2501, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_fma_ec_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_fma_ec_T7_7<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2502, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_fma_ec_T7_7_float_simd";
entry.accuracy_test<float>(log_remez_pade_fma_ec_T7_7_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2503, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_recip_fma_ec_T7_0<float>";
entry.accuracy_test<float>(log_remez_recip_fma_ec_T7_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2504, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_recip_fma_ec_T7_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_recip_fma_ec_T7_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2505, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez", "float", "simd",  };
entry.subName = "log_remez_recip_fma_ec_T7_0_float_simd";
entry.accuracy_test<float>(log_remez_recip_fma_ec_T7_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2506, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_recip_fma_ec_T7_7<float>";
entry.accuracy_test<float>(log_remez_pade_recip_fma_ec_T7_7<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2507, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_recip_fma_ec_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_recip_fma_ec_T7_7<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2508, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_recip_fma_ec_T7_7_float_simd";
entry.accuracy_test<float>(log_remez_pade_recip_fma_ec_T7_7_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2509, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_fma_T7_0<float>";
entry.accuracy_test<float>(log_remez_fma_T7_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2510, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_fma_T7_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_fma_T7_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2511, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez", "float", "simd",  };
entry.subName = "log_remez_fma_T7_0_float_simd";
entry.accuracy_test<float>(log_remez_fma_T7_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2512, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_fma_T7_7<float>";
entry.accuracy_test<float>(log_remez_pade_fma_T7_7<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2513, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_fma_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_fma_T7_7<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2514, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_fma_T7_7_float_simd";
entry.accuracy_test<float>(log_remez_pade_fma_T7_7_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2515, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_recip_fma_T7_0<float>";
entry.accuracy_test<float>(log_remez_recip_fma_T7_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2516, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_recip_fma_T7_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_recip_fma_T7_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2517, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez", "float", "simd",  };
entry.subName = "log_remez_recip_fma_T7_0_float_simd";
entry.accuracy_test<float>(log_remez_recip_fma_T7_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2518, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_recip_fma_T7_7<float>";
entry.accuracy_test<float>(log_remez_pade_recip_fma_T7_7<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2519, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_recip_fma_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_recip_fma_T7_7<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2520, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_recip_fma_T7_7_float_simd";
entry.accuracy_test<float>(log_remez_pade_recip_fma_T7_7_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2521, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_ec_T7_0<float>";
entry.accuracy_test<float>(log_remez_ec_T7_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2522, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_ec_T7_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_ec_T7_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2523, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez", "float", "simd",  };
entry.subName = "log_remez_ec_T7_0_float_simd";
entry.accuracy_test<float>(log_remez_ec_T7_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2524, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_ec_T7_7<float>";
entry.accuracy_test<float>(log_remez_pade_ec_T7_7<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2525, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_ec_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_ec_T7_7<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2526, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_ec_T7_7_float_simd";
entry.accuracy_test<float>(log_remez_pade_ec_T7_7_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2527, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_recip_ec_T7_0<float>";
entry.accuracy_test<float>(log_remez_recip_ec_T7_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2528, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_recip_ec_T7_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_recip_ec_T7_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2529, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez", "float", "simd",  };
entry.subName = "log_remez_recip_ec_T7_0_float_simd";
entry.accuracy_test<float>(log_remez_recip_ec_T7_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2530, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_recip_ec_T7_7<float>";
entry.accuracy_test<float>(log_remez_pade_recip_ec_T7_7<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2531, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_recip_ec_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_recip_ec_T7_7<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2532, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_recip_ec_T7_7_float_simd";
entry.accuracy_test<float>(log_remez_pade_recip_ec_T7_7_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2533, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_T7_0<float>";
entry.accuracy_test<float>(log_remez_T7_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2534, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_T7_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_T7_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2535, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez", "float", "simd",  };
entry.subName = "log_remez_T7_0_float_simd";
entry.accuracy_test<float>(log_remez_T7_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2536, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_T7_7<float>";
entry.accuracy_test<float>(log_remez_pade_T7_7<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2537, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_T7_7<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2538, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_T7_7_float_simd";
entry.accuracy_test<float>(log_remez_pade_T7_7_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2539, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_recip_T7_0<float>";
entry.accuracy_test<float>(log_remez_recip_T7_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2540, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_recip_T7_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_recip_T7_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2541, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez", "float", "simd",  };
entry.subName = "log_remez_recip_T7_0_float_simd";
entry.accuracy_test<float>(log_remez_recip_T7_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2542, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_recip_T7_7<float>";
entry.accuracy_test<float>(log_remez_pade_recip_T7_7<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2543, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_recip_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_recip_T7_7<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2544, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_recip_T7_7_float_simd";
entry.accuracy_test<float>(log_remez_pade_recip_T7_7_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2545, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "float", "scalar",  };
entry.subName = "log_fma_ec_T7_7<float>";
entry.accuracy_test<float>(log_fma_ec_T7_7<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2546, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "float2x4", "simd",  };
entry.subName = "log_fma_ec_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_fma_ec_T7_7<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2547, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "float", "simd",  };
entry.subName = "log_fma_ec_T7_7_float_simd";
entry.accuracy_test<float>(log_fma_ec_T7_7_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2548, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "float", "scalar",  };
entry.subName = "log_fma_T7_7<float>";
entry.accuracy_test<float>(log_fma_T7_7<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2549, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "float2x4", "simd",  };
entry.subName = "log_fma_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_fma_T7_7<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2550, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "float", "simd",  };
entry.subName = "log_fma_T7_7_float_simd";
entry.accuracy_test<float>(log_fma_T7_7_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2551, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "float", "scalar",  };
entry.subName = "log_ec_T7_7<float>";
entry.accuracy_test<float>(log_ec_T7_7<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2552, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "float2x4", "simd",  };
entry.subName = "log_ec_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_ec_T7_7<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2553, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "float", "simd",  };
entry.subName = "log_ec_T7_7_float_simd";
entry.accuracy_test<float>(log_ec_T7_7_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2554, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "float", "scalar",  };
entry.subName = "log_T7_7<float>";
entry.accuracy_test<float>(log_T7_7<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2555, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "float2x4", "simd",  };
entry.subName = "log_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_T7_7<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2556, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "float", "simd",  };
entry.subName = "log_T7_7_float_simd";
entry.accuracy_test<float>(log_T7_7_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2557, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_fma_ec_T8_0<float>";
entry.accuracy_test<float>(log_remez_fma_ec_T8_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2558, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_fma_ec_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_fma_ec_T8_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2559, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez", "float", "simd",  };
entry.subName = "log_remez_fma_ec_T8_0_float_simd";
entry.accuracy_test<float>(log_remez_fma_ec_T8_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2560, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_fma_ec_T8_8<float>";
entry.accuracy_test<float>(log_remez_pade_fma_ec_T8_8<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2561, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_fma_ec_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_fma_ec_T8_8<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2562, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_fma_ec_T8_8_float_simd";
entry.accuracy_test<float>(log_remez_pade_fma_ec_T8_8_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2563, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_recip_fma_ec_T8_0<float>";
entry.accuracy_test<float>(log_remez_recip_fma_ec_T8_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2564, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_recip_fma_ec_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_recip_fma_ec_T8_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2565, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez", "float", "simd",  };
entry.subName = "log_remez_recip_fma_ec_T8_0_float_simd";
entry.accuracy_test<float>(log_remez_recip_fma_ec_T8_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2566, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_recip_fma_ec_T8_8<float>";
entry.accuracy_test<float>(log_remez_pade_recip_fma_ec_T8_8<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2567, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_recip_fma_ec_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_recip_fma_ec_T8_8<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2568, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_recip_fma_ec_T8_8_float_simd";
entry.accuracy_test<float>(log_remez_pade_recip_fma_ec_T8_8_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2569, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_fma_T8_0<float>";
entry.accuracy_test<float>(log_remez_fma_T8_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2570, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_fma_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_fma_T8_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2571, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez", "float", "simd",  };
entry.subName = "log_remez_fma_T8_0_float_simd";
entry.accuracy_test<float>(log_remez_fma_T8_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2572, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_fma_T8_8<float>";
entry.accuracy_test<float>(log_remez_pade_fma_T8_8<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2573, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_fma_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_fma_T8_8<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2574, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_fma_T8_8_float_simd";
entry.accuracy_test<float>(log_remez_pade_fma_T8_8_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2575, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_recip_fma_T8_0<float>";
entry.accuracy_test<float>(log_remez_recip_fma_T8_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2576, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_recip_fma_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_recip_fma_T8_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2577, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez", "float", "simd",  };
entry.subName = "log_remez_recip_fma_T8_0_float_simd";
entry.accuracy_test<float>(log_remez_recip_fma_T8_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2578, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_recip_fma_T8_8<float>";
entry.accuracy_test<float>(log_remez_pade_recip_fma_T8_8<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2579, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_recip_fma_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_recip_fma_T8_8<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2580, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_recip_fma_T8_8_float_simd";
entry.accuracy_test<float>(log_remez_pade_recip_fma_T8_8_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2581, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_ec_T8_0<float>";
entry.accuracy_test<float>(log_remez_ec_T8_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2582, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_ec_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_ec_T8_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2583, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez", "float", "simd",  };
entry.subName = "log_remez_ec_T8_0_float_simd";
entry.accuracy_test<float>(log_remez_ec_T8_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2584, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_ec_T8_8<float>";
entry.accuracy_test<float>(log_remez_pade_ec_T8_8<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2585, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_ec_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_ec_T8_8<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2586, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_ec_T8_8_float_simd";
entry.accuracy_test<float>(log_remez_pade_ec_T8_8_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2587, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_recip_ec_T8_0<float>";
entry.accuracy_test<float>(log_remez_recip_ec_T8_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2588, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_recip_ec_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_recip_ec_T8_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2589, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez", "float", "simd",  };
entry.subName = "log_remez_recip_ec_T8_0_float_simd";
entry.accuracy_test<float>(log_remez_recip_ec_T8_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2590, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_recip_ec_T8_8<float>";
entry.accuracy_test<float>(log_remez_pade_recip_ec_T8_8<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2591, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_recip_ec_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_recip_ec_T8_8<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2592, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_recip_ec_T8_8_float_simd";
entry.accuracy_test<float>(log_remez_pade_recip_ec_T8_8_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2593, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_T8_0<float>";
entry.accuracy_test<float>(log_remez_T8_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2594, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_T8_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2595, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez", "float", "simd",  };
entry.subName = "log_remez_T8_0_float_simd";
entry.accuracy_test<float>(log_remez_T8_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2596, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_T8_8<float>";
entry.accuracy_test<float>(log_remez_pade_T8_8<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2597, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_T8_8<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2598, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_T8_8_float_simd";
entry.accuracy_test<float>(log_remez_pade_T8_8_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2599, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_recip_T8_0<float>";
entry.accuracy_test<float>(log_remez_recip_T8_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2600, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_recip_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_recip_T8_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2601, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez", "float", "simd",  };
entry.subName = "log_remez_recip_T8_0_float_simd";
entry.accuracy_test<float>(log_remez_recip_T8_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2602, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_recip_T8_8<float>";
entry.accuracy_test<float>(log_remez_pade_recip_T8_8<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2603, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_recip_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_recip_T8_8<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2604, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_recip_T8_8_float_simd";
entry.accuracy_test<float>(log_remez_pade_recip_T8_8_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2605, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "float", "scalar",  };
entry.subName = "log_fma_ec_T8_8<float>";
entry.accuracy_test<float>(log_fma_ec_T8_8<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2606, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "float2x4", "simd",  };
entry.subName = "log_fma_ec_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_fma_ec_T8_8<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2607, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "float", "simd",  };
entry.subName = "log_fma_ec_T8_8_float_simd";
entry.accuracy_test<float>(log_fma_ec_T8_8_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2608, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "float", "scalar",  };
entry.subName = "log_fma_T8_8<float>";
entry.accuracy_test<float>(log_fma_T8_8<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2609, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "float2x4", "simd",  };
entry.subName = "log_fma_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_fma_T8_8<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2610, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "float", "simd",  };
entry.subName = "log_fma_T8_8_float_simd";
entry.accuracy_test<float>(log_fma_T8_8_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2611, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "float", "scalar",  };
entry.subName = "log_ec_T8_8<float>";
entry.accuracy_test<float>(log_ec_T8_8<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2612, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "float2x4", "simd",  };
entry.subName = "log_ec_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_ec_T8_8<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2613, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "float", "simd",  };
entry.subName = "log_ec_T8_8_float_simd";
entry.accuracy_test<float>(log_ec_T8_8_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2614, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "float", "scalar",  };
entry.subName = "log_T8_8<float>";
entry.accuracy_test<float>(log_T8_8<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2615, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "float2x4", "simd",  };
entry.subName = "log_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_T8_8<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2616, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "float", "simd",  };
entry.subName = "log_T8_8_float_simd";
entry.accuracy_test<float>(log_T8_8_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2617, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_fma_ec_T9_0<float>";
entry.accuracy_test<float>(log_remez_fma_ec_T9_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2618, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_fma_ec_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_fma_ec_T9_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2619, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez", "float", "simd",  };
entry.subName = "log_remez_fma_ec_T9_0_float_simd";
entry.accuracy_test<float>(log_remez_fma_ec_T9_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2620, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_fma_ec_T9_9<float>";
entry.accuracy_test<float>(log_remez_pade_fma_ec_T9_9<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2621, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_fma_ec_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_fma_ec_T9_9<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2622, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_fma_ec_T9_9_float_simd";
entry.accuracy_test<float>(log_remez_pade_fma_ec_T9_9_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2623, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_recip_fma_ec_T9_0<float>";
entry.accuracy_test<float>(log_remez_recip_fma_ec_T9_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2624, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_recip_fma_ec_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_recip_fma_ec_T9_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2625, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez", "float", "simd",  };
entry.subName = "log_remez_recip_fma_ec_T9_0_float_simd";
entry.accuracy_test<float>(log_remez_recip_fma_ec_T9_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2626, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_recip_fma_ec_T9_9<float>";
entry.accuracy_test<float>(log_remez_pade_recip_fma_ec_T9_9<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2627, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_recip_fma_ec_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_recip_fma_ec_T9_9<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2628, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_recip_fma_ec_T9_9_float_simd";
entry.accuracy_test<float>(log_remez_pade_recip_fma_ec_T9_9_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2629, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_fma_T9_0<float>";
entry.accuracy_test<float>(log_remez_fma_T9_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2630, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_fma_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_fma_T9_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2631, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez", "float", "simd",  };
entry.subName = "log_remez_fma_T9_0_float_simd";
entry.accuracy_test<float>(log_remez_fma_T9_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2632, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_fma_T9_9<float>";
entry.accuracy_test<float>(log_remez_pade_fma_T9_9<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2633, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_fma_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_fma_T9_9<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2634, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_fma_T9_9_float_simd";
entry.accuracy_test<float>(log_remez_pade_fma_T9_9_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2635, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_recip_fma_T9_0<float>";
entry.accuracy_test<float>(log_remez_recip_fma_T9_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2636, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_recip_fma_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_recip_fma_T9_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2637, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez", "float", "simd",  };
entry.subName = "log_remez_recip_fma_T9_0_float_simd";
entry.accuracy_test<float>(log_remez_recip_fma_T9_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2638, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_recip_fma_T9_9<float>";
entry.accuracy_test<float>(log_remez_pade_recip_fma_T9_9<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2639, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_recip_fma_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_recip_fma_T9_9<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2640, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_recip_fma_T9_9_float_simd";
entry.accuracy_test<float>(log_remez_pade_recip_fma_T9_9_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2641, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_ec_T9_0<float>";
entry.accuracy_test<float>(log_remez_ec_T9_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2642, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_ec_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_ec_T9_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2643, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez", "float", "simd",  };
entry.subName = "log_remez_ec_T9_0_float_simd";
entry.accuracy_test<float>(log_remez_ec_T9_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2644, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_ec_T9_9<float>";
entry.accuracy_test<float>(log_remez_pade_ec_T9_9<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2645, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_ec_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_ec_T9_9<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2646, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_ec_T9_9_float_simd";
entry.accuracy_test<float>(log_remez_pade_ec_T9_9_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2647, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_recip_ec_T9_0<float>";
entry.accuracy_test<float>(log_remez_recip_ec_T9_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2648, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_recip_ec_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_recip_ec_T9_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2649, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez", "float", "simd",  };
entry.subName = "log_remez_recip_ec_T9_0_float_simd";
entry.accuracy_test<float>(log_remez_recip_ec_T9_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2650, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_recip_ec_T9_9<float>";
entry.accuracy_test<float>(log_remez_pade_recip_ec_T9_9<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2651, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_recip_ec_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_recip_ec_T9_9<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2652, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_recip_ec_T9_9_float_simd";
entry.accuracy_test<float>(log_remez_pade_recip_ec_T9_9_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2653, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_T9_0<float>";
entry.accuracy_test<float>(log_remez_T9_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2654, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_T9_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2655, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez", "float", "simd",  };
entry.subName = "log_remez_T9_0_float_simd";
entry.accuracy_test<float>(log_remez_T9_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2656, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_T9_9<float>";
entry.accuracy_test<float>(log_remez_pade_T9_9<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2657, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_T9_9<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2658, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_T9_9_float_simd";
entry.accuracy_test<float>(log_remez_pade_T9_9_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2659, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez", "float", "scalar",  };
entry.subName = "log_remez_recip_T9_0<float>";
entry.accuracy_test<float>(log_remez_recip_T9_0<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2660, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez", "float2x4", "simd",  };
entry.subName = "log_remez_recip_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_recip_T9_0<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2661, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez", "float", "simd",  };
entry.subName = "log_remez_recip_T9_0_float_simd";
entry.accuracy_test<float>(log_remez_recip_T9_0_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2662, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez_pade", "float", "scalar",  };
entry.subName = "log_remez_pade_recip_T9_9<float>";
entry.accuracy_test<float>(log_remez_pade_recip_T9_9<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2663, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez_pade", "float2x4", "simd",  };
entry.subName = "log_remez_pade_recip_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_remez_pade_recip_T9_9<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2664, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "remez_pade", "float", "simd",  };
entry.subName = "log_remez_pade_recip_T9_9_float_simd";
entry.accuracy_test<float>(log_remez_pade_recip_T9_9_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2665, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "float", "scalar",  };
entry.subName = "log_fma_ec_T9_9<float>";
entry.accuracy_test<float>(log_fma_ec_T9_9<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2666, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "float2x4", "simd",  };
entry.subName = "log_fma_ec_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_fma_ec_T9_9<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2667, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "log", "float", "simd",  };
entry.subName = "log_fma_ec_T9_9_float_simd";
entry.accuracy_test<float>(log_fma_ec_T9_9_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2668, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "float", "scalar",  };
entry.subName = "log_fma_T9_9<float>";
entry.accuracy_test<float>(log_fma_T9_9<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2669, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "float2x4", "simd",  };
entry.subName = "log_fma_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_fma_T9_9<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2670, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "log", "float", "simd",  };
entry.subName = "log_fma_T9_9_float_simd";
entry.accuracy_test<float>(log_fma_T9_9_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2671, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "float", "scalar",  };
entry.subName = "log_ec_T9_9<float>";
entry.accuracy_test<float>(log_ec_T9_9<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2672, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "float2x4", "simd",  };
entry.subName = "log_ec_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_ec_T9_9<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2673, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "log", "float", "simd",  };
entry.subName = "log_ec_T9_9_float_simd";
entry.accuracy_test<float>(log_ec_T9_9_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2674, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "float", "scalar",  };
entry.subName = "log_T9_9<float>";
entry.accuracy_test<float>(log_T9_9<float>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2675, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "float2x4", "simd",  };
entry.subName = "log_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(log_T9_9<cr::simd::float2x4>, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2676, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "log", "float", "simd",  };
entry.subName = "log_T9_9_float_simd";
entry.accuracy_test<float>(log_T9_9_float_simd, std::logf, 0.08f, 12.5f, 0.08f, 12.5f);
entry.time<float>(msvc_bug_workaround_2677, 0.08f, 12.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "remez", "float", "scalar",  };
entry.subName = "exp_special_remez_fma_ec_T3_0<float>";
entry.accuracy_test<float>(exp_special_remez_fma_ec_T3_0<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2678, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "remez", "float2x4", "simd",  };
entry.subName = "exp_special_remez_fma_ec_T3_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_fma_ec_T3_0<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2679, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "remez", "float", "simd",  };
entry.subName = "exp_special_remez_fma_ec_T3_0_float_simd";
entry.accuracy_test<float>(exp_special_remez_fma_ec_T3_0_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2680, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "remez_pade", "float", "scalar",  };
entry.subName = "exp_special_remez_pade_fma_ec_T3_3<float>";
entry.accuracy_test<float>(exp_special_remez_pade_fma_ec_T3_3<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2681, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "remez_pade", "float2x4", "simd",  };
entry.subName = "exp_special_remez_pade_fma_ec_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_pade_fma_ec_T3_3<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2682, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "remez_pade", "float", "simd",  };
entry.subName = "exp_special_remez_pade_fma_ec_T3_3_float_simd";
entry.accuracy_test<float>(exp_special_remez_pade_fma_ec_T3_3_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2683, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "float", "scalar",  };
entry.subName = "exp_special_fma_ec_T3_3<float>";
entry.accuracy_test<float>(exp_special_fma_ec_T3_3<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2684, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "float2x4", "simd",  };
entry.subName = "exp_special_fma_ec_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_fma_ec_T3_3<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2685, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "float", "simd",  };
entry.subName = "exp_special_fma_ec_T3_3_float_simd";
entry.accuracy_test<float>(exp_special_fma_ec_T3_3_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2686, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "remez", "float", "scalar",  };
entry.subName = "exp_special_remez_fma_T3_0<float>";
entry.accuracy_test<float>(exp_special_remez_fma_T3_0<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2687, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "remez", "float2x4", "simd",  };
entry.subName = "exp_special_remez_fma_T3_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_fma_T3_0<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2688, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "remez", "float", "simd",  };
entry.subName = "exp_special_remez_fma_T3_0_float_simd";
entry.accuracy_test<float>(exp_special_remez_fma_T3_0_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2689, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "remez_pade", "float", "scalar",  };
entry.subName = "exp_special_remez_pade_fma_T3_3<float>";
entry.accuracy_test<float>(exp_special_remez_pade_fma_T3_3<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2690, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "remez_pade", "float2x4", "simd",  };
entry.subName = "exp_special_remez_pade_fma_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_pade_fma_T3_3<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2691, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "remez_pade", "float", "simd",  };
entry.subName = "exp_special_remez_pade_fma_T3_3_float_simd";
entry.accuracy_test<float>(exp_special_remez_pade_fma_T3_3_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2692, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "float", "scalar",  };
entry.subName = "exp_special_fma_T3_3<float>";
entry.accuracy_test<float>(exp_special_fma_T3_3<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2693, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "float2x4", "simd",  };
entry.subName = "exp_special_fma_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_fma_T3_3<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2694, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "float", "simd",  };
entry.subName = "exp_special_fma_T3_3_float_simd";
entry.accuracy_test<float>(exp_special_fma_T3_3_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2695, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "remez", "float", "scalar",  };
entry.subName = "exp_special_remez_ec_T3_0<float>";
entry.accuracy_test<float>(exp_special_remez_ec_T3_0<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2696, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "remez", "float2x4", "simd",  };
entry.subName = "exp_special_remez_ec_T3_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_ec_T3_0<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2697, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "remez", "float", "simd",  };
entry.subName = "exp_special_remez_ec_T3_0_float_simd";
entry.accuracy_test<float>(exp_special_remez_ec_T3_0_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2698, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "remez_pade", "float", "scalar",  };
entry.subName = "exp_special_remez_pade_ec_T3_3<float>";
entry.accuracy_test<float>(exp_special_remez_pade_ec_T3_3<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2699, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "remez_pade", "float2x4", "simd",  };
entry.subName = "exp_special_remez_pade_ec_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_pade_ec_T3_3<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2700, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "remez_pade", "float", "simd",  };
entry.subName = "exp_special_remez_pade_ec_T3_3_float_simd";
entry.accuracy_test<float>(exp_special_remez_pade_ec_T3_3_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2701, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "float", "scalar",  };
entry.subName = "exp_special_ec_T3_3<float>";
entry.accuracy_test<float>(exp_special_ec_T3_3<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2702, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "float2x4", "simd",  };
entry.subName = "exp_special_ec_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_ec_T3_3<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2703, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "float", "simd",  };
entry.subName = "exp_special_ec_T3_3_float_simd";
entry.accuracy_test<float>(exp_special_ec_T3_3_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2704, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "remez", "float", "scalar",  };
entry.subName = "exp_special_remez_T3_0<float>";
entry.accuracy_test<float>(exp_special_remez_T3_0<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2705, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "remez", "float2x4", "simd",  };
entry.subName = "exp_special_remez_T3_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_T3_0<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2706, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "remez", "float", "simd",  };
entry.subName = "exp_special_remez_T3_0_float_simd";
entry.accuracy_test<float>(exp_special_remez_T3_0_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2707, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "remez_pade", "float", "scalar",  };
entry.subName = "exp_special_remez_pade_T3_3<float>";
entry.accuracy_test<float>(exp_special_remez_pade_T3_3<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2708, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "remez_pade", "float2x4", "simd",  };
entry.subName = "exp_special_remez_pade_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_pade_T3_3<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2709, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "remez_pade", "float", "simd",  };
entry.subName = "exp_special_remez_pade_T3_3_float_simd";
entry.accuracy_test<float>(exp_special_remez_pade_T3_3_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2710, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "float", "scalar",  };
entry.subName = "exp_special_T3_3<float>";
entry.accuracy_test<float>(exp_special_T3_3<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2711, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "float2x4", "simd",  };
entry.subName = "exp_special_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_T3_3<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2712, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "float", "simd",  };
entry.subName = "exp_special_T3_3_float_simd";
entry.accuracy_test<float>(exp_special_T3_3_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2713, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "remez", "float", "scalar",  };
entry.subName = "exp_special_remez_fma_ec_T4_0<float>";
entry.accuracy_test<float>(exp_special_remez_fma_ec_T4_0<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2714, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "remez", "float2x4", "simd",  };
entry.subName = "exp_special_remez_fma_ec_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_fma_ec_T4_0<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2715, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "remez", "float", "simd",  };
entry.subName = "exp_special_remez_fma_ec_T4_0_float_simd";
entry.accuracy_test<float>(exp_special_remez_fma_ec_T4_0_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2716, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "remez_pade", "float", "scalar",  };
entry.subName = "exp_special_remez_pade_fma_ec_T4_4<float>";
entry.accuracy_test<float>(exp_special_remez_pade_fma_ec_T4_4<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2717, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "remez_pade", "float2x4", "simd",  };
entry.subName = "exp_special_remez_pade_fma_ec_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_pade_fma_ec_T4_4<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2718, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "remez_pade", "float", "simd",  };
entry.subName = "exp_special_remez_pade_fma_ec_T4_4_float_simd";
entry.accuracy_test<float>(exp_special_remez_pade_fma_ec_T4_4_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2719, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "float", "scalar",  };
entry.subName = "exp_special_fma_ec_T4_4<float>";
entry.accuracy_test<float>(exp_special_fma_ec_T4_4<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2720, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "float2x4", "simd",  };
entry.subName = "exp_special_fma_ec_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_fma_ec_T4_4<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2721, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "float", "simd",  };
entry.subName = "exp_special_fma_ec_T4_4_float_simd";
entry.accuracy_test<float>(exp_special_fma_ec_T4_4_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2722, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "remez", "float", "scalar",  };
entry.subName = "exp_special_remez_fma_T4_0<float>";
entry.accuracy_test<float>(exp_special_remez_fma_T4_0<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2723, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "remez", "float2x4", "simd",  };
entry.subName = "exp_special_remez_fma_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_fma_T4_0<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2724, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "remez", "float", "simd",  };
entry.subName = "exp_special_remez_fma_T4_0_float_simd";
entry.accuracy_test<float>(exp_special_remez_fma_T4_0_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2725, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "remez_pade", "float", "scalar",  };
entry.subName = "exp_special_remez_pade_fma_T4_4<float>";
entry.accuracy_test<float>(exp_special_remez_pade_fma_T4_4<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2726, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "remez_pade", "float2x4", "simd",  };
entry.subName = "exp_special_remez_pade_fma_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_pade_fma_T4_4<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2727, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "remez_pade", "float", "simd",  };
entry.subName = "exp_special_remez_pade_fma_T4_4_float_simd";
entry.accuracy_test<float>(exp_special_remez_pade_fma_T4_4_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2728, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "float", "scalar",  };
entry.subName = "exp_special_fma_T4_4<float>";
entry.accuracy_test<float>(exp_special_fma_T4_4<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2729, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "float2x4", "simd",  };
entry.subName = "exp_special_fma_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_fma_T4_4<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2730, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "float", "simd",  };
entry.subName = "exp_special_fma_T4_4_float_simd";
entry.accuracy_test<float>(exp_special_fma_T4_4_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2731, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "remez", "float", "scalar",  };
entry.subName = "exp_special_remez_ec_T4_0<float>";
entry.accuracy_test<float>(exp_special_remez_ec_T4_0<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2732, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "remez", "float2x4", "simd",  };
entry.subName = "exp_special_remez_ec_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_ec_T4_0<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2733, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "remez", "float", "simd",  };
entry.subName = "exp_special_remez_ec_T4_0_float_simd";
entry.accuracy_test<float>(exp_special_remez_ec_T4_0_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2734, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "remez_pade", "float", "scalar",  };
entry.subName = "exp_special_remez_pade_ec_T4_4<float>";
entry.accuracy_test<float>(exp_special_remez_pade_ec_T4_4<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2735, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "remez_pade", "float2x4", "simd",  };
entry.subName = "exp_special_remez_pade_ec_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_pade_ec_T4_4<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2736, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "remez_pade", "float", "simd",  };
entry.subName = "exp_special_remez_pade_ec_T4_4_float_simd";
entry.accuracy_test<float>(exp_special_remez_pade_ec_T4_4_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2737, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "float", "scalar",  };
entry.subName = "exp_special_ec_T4_4<float>";
entry.accuracy_test<float>(exp_special_ec_T4_4<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2738, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "float2x4", "simd",  };
entry.subName = "exp_special_ec_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_ec_T4_4<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2739, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "float", "simd",  };
entry.subName = "exp_special_ec_T4_4_float_simd";
entry.accuracy_test<float>(exp_special_ec_T4_4_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2740, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "remez", "float", "scalar",  };
entry.subName = "exp_special_remez_T4_0<float>";
entry.accuracy_test<float>(exp_special_remez_T4_0<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2741, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "remez", "float2x4", "simd",  };
entry.subName = "exp_special_remez_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_T4_0<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2742, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "remez", "float", "simd",  };
entry.subName = "exp_special_remez_T4_0_float_simd";
entry.accuracy_test<float>(exp_special_remez_T4_0_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2743, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "remez_pade", "float", "scalar",  };
entry.subName = "exp_special_remez_pade_T4_4<float>";
entry.accuracy_test<float>(exp_special_remez_pade_T4_4<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2744, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "remez_pade", "float2x4", "simd",  };
entry.subName = "exp_special_remez_pade_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_pade_T4_4<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2745, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "remez_pade", "float", "simd",  };
entry.subName = "exp_special_remez_pade_T4_4_float_simd";
entry.accuracy_test<float>(exp_special_remez_pade_T4_4_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2746, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "float", "scalar",  };
entry.subName = "exp_special_T4_4<float>";
entry.accuracy_test<float>(exp_special_T4_4<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2747, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "float2x4", "simd",  };
entry.subName = "exp_special_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_T4_4<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2748, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "float", "simd",  };
entry.subName = "exp_special_T4_4_float_simd";
entry.accuracy_test<float>(exp_special_T4_4_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2749, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "remez", "float", "scalar",  };
entry.subName = "exp_special_remez_fma_ec_T5_0<float>";
entry.accuracy_test<float>(exp_special_remez_fma_ec_T5_0<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2750, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "remez", "float2x4", "simd",  };
entry.subName = "exp_special_remez_fma_ec_T5_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_fma_ec_T5_0<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2751, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "remez", "float", "simd",  };
entry.subName = "exp_special_remez_fma_ec_T5_0_float_simd";
entry.accuracy_test<float>(exp_special_remez_fma_ec_T5_0_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2752, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "remez_pade", "float", "scalar",  };
entry.subName = "exp_special_remez_pade_fma_ec_T5_5<float>";
entry.accuracy_test<float>(exp_special_remez_pade_fma_ec_T5_5<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2753, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "remez_pade", "float2x4", "simd",  };
entry.subName = "exp_special_remez_pade_fma_ec_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_pade_fma_ec_T5_5<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2754, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "remez_pade", "float", "simd",  };
entry.subName = "exp_special_remez_pade_fma_ec_T5_5_float_simd";
entry.accuracy_test<float>(exp_special_remez_pade_fma_ec_T5_5_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2755, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "float", "scalar",  };
entry.subName = "exp_special_fma_ec_T5_5<float>";
entry.accuracy_test<float>(exp_special_fma_ec_T5_5<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2756, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "float2x4", "simd",  };
entry.subName = "exp_special_fma_ec_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_fma_ec_T5_5<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2757, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "float", "simd",  };
entry.subName = "exp_special_fma_ec_T5_5_float_simd";
entry.accuracy_test<float>(exp_special_fma_ec_T5_5_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2758, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "remez", "float", "scalar",  };
entry.subName = "exp_special_remez_fma_T5_0<float>";
entry.accuracy_test<float>(exp_special_remez_fma_T5_0<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2759, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "remez", "float2x4", "simd",  };
entry.subName = "exp_special_remez_fma_T5_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_fma_T5_0<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2760, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "remez", "float", "simd",  };
entry.subName = "exp_special_remez_fma_T5_0_float_simd";
entry.accuracy_test<float>(exp_special_remez_fma_T5_0_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2761, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "remez_pade", "float", "scalar",  };
entry.subName = "exp_special_remez_pade_fma_T5_5<float>";
entry.accuracy_test<float>(exp_special_remez_pade_fma_T5_5<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2762, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "remez_pade", "float2x4", "simd",  };
entry.subName = "exp_special_remez_pade_fma_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_pade_fma_T5_5<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2763, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "remez_pade", "float", "simd",  };
entry.subName = "exp_special_remez_pade_fma_T5_5_float_simd";
entry.accuracy_test<float>(exp_special_remez_pade_fma_T5_5_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2764, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "float", "scalar",  };
entry.subName = "exp_special_fma_T5_5<float>";
entry.accuracy_test<float>(exp_special_fma_T5_5<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2765, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "float2x4", "simd",  };
entry.subName = "exp_special_fma_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_fma_T5_5<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2766, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "float", "simd",  };
entry.subName = "exp_special_fma_T5_5_float_simd";
entry.accuracy_test<float>(exp_special_fma_T5_5_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2767, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "remez", "float", "scalar",  };
entry.subName = "exp_special_remez_ec_T5_0<float>";
entry.accuracy_test<float>(exp_special_remez_ec_T5_0<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2768, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "remez", "float2x4", "simd",  };
entry.subName = "exp_special_remez_ec_T5_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_ec_T5_0<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2769, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "remez", "float", "simd",  };
entry.subName = "exp_special_remez_ec_T5_0_float_simd";
entry.accuracy_test<float>(exp_special_remez_ec_T5_0_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2770, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "remez_pade", "float", "scalar",  };
entry.subName = "exp_special_remez_pade_ec_T5_5<float>";
entry.accuracy_test<float>(exp_special_remez_pade_ec_T5_5<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2771, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "remez_pade", "float2x4", "simd",  };
entry.subName = "exp_special_remez_pade_ec_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_pade_ec_T5_5<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2772, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "remez_pade", "float", "simd",  };
entry.subName = "exp_special_remez_pade_ec_T5_5_float_simd";
entry.accuracy_test<float>(exp_special_remez_pade_ec_T5_5_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2773, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "float", "scalar",  };
entry.subName = "exp_special_ec_T5_5<float>";
entry.accuracy_test<float>(exp_special_ec_T5_5<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2774, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "float2x4", "simd",  };
entry.subName = "exp_special_ec_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_ec_T5_5<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2775, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "float", "simd",  };
entry.subName = "exp_special_ec_T5_5_float_simd";
entry.accuracy_test<float>(exp_special_ec_T5_5_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2776, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "remez", "float", "scalar",  };
entry.subName = "exp_special_remez_T5_0<float>";
entry.accuracy_test<float>(exp_special_remez_T5_0<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2777, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "remez", "float2x4", "simd",  };
entry.subName = "exp_special_remez_T5_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_T5_0<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2778, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "remez", "float", "simd",  };
entry.subName = "exp_special_remez_T5_0_float_simd";
entry.accuracy_test<float>(exp_special_remez_T5_0_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2779, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "remez_pade", "float", "scalar",  };
entry.subName = "exp_special_remez_pade_T5_5<float>";
entry.accuracy_test<float>(exp_special_remez_pade_T5_5<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2780, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "remez_pade", "float2x4", "simd",  };
entry.subName = "exp_special_remez_pade_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_pade_T5_5<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2781, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "remez_pade", "float", "simd",  };
entry.subName = "exp_special_remez_pade_T5_5_float_simd";
entry.accuracy_test<float>(exp_special_remez_pade_T5_5_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2782, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "float", "scalar",  };
entry.subName = "exp_special_T5_5<float>";
entry.accuracy_test<float>(exp_special_T5_5<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2783, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "float2x4", "simd",  };
entry.subName = "exp_special_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_T5_5<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2784, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "float", "simd",  };
entry.subName = "exp_special_T5_5_float_simd";
entry.accuracy_test<float>(exp_special_T5_5_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2785, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "remez", "float", "scalar",  };
entry.subName = "exp_special_remez_fma_ec_T6_0<float>";
entry.accuracy_test<float>(exp_special_remez_fma_ec_T6_0<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2786, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "remez", "float2x4", "simd",  };
entry.subName = "exp_special_remez_fma_ec_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_fma_ec_T6_0<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2787, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "remez", "float", "simd",  };
entry.subName = "exp_special_remez_fma_ec_T6_0_float_simd";
entry.accuracy_test<float>(exp_special_remez_fma_ec_T6_0_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2788, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "remez_pade", "float", "scalar",  };
entry.subName = "exp_special_remez_pade_fma_ec_T6_6<float>";
entry.accuracy_test<float>(exp_special_remez_pade_fma_ec_T6_6<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2789, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "remez_pade", "float2x4", "simd",  };
entry.subName = "exp_special_remez_pade_fma_ec_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_pade_fma_ec_T6_6<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2790, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "remez_pade", "float", "simd",  };
entry.subName = "exp_special_remez_pade_fma_ec_T6_6_float_simd";
entry.accuracy_test<float>(exp_special_remez_pade_fma_ec_T6_6_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2791, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "float", "scalar",  };
entry.subName = "exp_special_fma_ec_T6_6<float>";
entry.accuracy_test<float>(exp_special_fma_ec_T6_6<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2792, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "float2x4", "simd",  };
entry.subName = "exp_special_fma_ec_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_fma_ec_T6_6<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2793, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "float", "simd",  };
entry.subName = "exp_special_fma_ec_T6_6_float_simd";
entry.accuracy_test<float>(exp_special_fma_ec_T6_6_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2794, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "remez", "float", "scalar",  };
entry.subName = "exp_special_remez_fma_T6_0<float>";
entry.accuracy_test<float>(exp_special_remez_fma_T6_0<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2795, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "remez", "float2x4", "simd",  };
entry.subName = "exp_special_remez_fma_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_fma_T6_0<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2796, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "remez", "float", "simd",  };
entry.subName = "exp_special_remez_fma_T6_0_float_simd";
entry.accuracy_test<float>(exp_special_remez_fma_T6_0_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2797, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "remez_pade", "float", "scalar",  };
entry.subName = "exp_special_remez_pade_fma_T6_6<float>";
entry.accuracy_test<float>(exp_special_remez_pade_fma_T6_6<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2798, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "remez_pade", "float2x4", "simd",  };
entry.subName = "exp_special_remez_pade_fma_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_pade_fma_T6_6<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2799, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "remez_pade", "float", "simd",  };
entry.subName = "exp_special_remez_pade_fma_T6_6_float_simd";
entry.accuracy_test<float>(exp_special_remez_pade_fma_T6_6_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2800, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "float", "scalar",  };
entry.subName = "exp_special_fma_T6_6<float>";
entry.accuracy_test<float>(exp_special_fma_T6_6<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2801, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "float2x4", "simd",  };
entry.subName = "exp_special_fma_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_fma_T6_6<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2802, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "float", "simd",  };
entry.subName = "exp_special_fma_T6_6_float_simd";
entry.accuracy_test<float>(exp_special_fma_T6_6_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2803, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "remez", "float", "scalar",  };
entry.subName = "exp_special_remez_ec_T6_0<float>";
entry.accuracy_test<float>(exp_special_remez_ec_T6_0<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2804, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "remez", "float2x4", "simd",  };
entry.subName = "exp_special_remez_ec_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_ec_T6_0<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2805, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "remez", "float", "simd",  };
entry.subName = "exp_special_remez_ec_T6_0_float_simd";
entry.accuracy_test<float>(exp_special_remez_ec_T6_0_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2806, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "remez_pade", "float", "scalar",  };
entry.subName = "exp_special_remez_pade_ec_T6_6<float>";
entry.accuracy_test<float>(exp_special_remez_pade_ec_T6_6<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2807, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "remez_pade", "float2x4", "simd",  };
entry.subName = "exp_special_remez_pade_ec_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_pade_ec_T6_6<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2808, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "remez_pade", "float", "simd",  };
entry.subName = "exp_special_remez_pade_ec_T6_6_float_simd";
entry.accuracy_test<float>(exp_special_remez_pade_ec_T6_6_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2809, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "float", "scalar",  };
entry.subName = "exp_special_ec_T6_6<float>";
entry.accuracy_test<float>(exp_special_ec_T6_6<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2810, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "float2x4", "simd",  };
entry.subName = "exp_special_ec_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_ec_T6_6<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2811, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "float", "simd",  };
entry.subName = "exp_special_ec_T6_6_float_simd";
entry.accuracy_test<float>(exp_special_ec_T6_6_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2812, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "remez", "float", "scalar",  };
entry.subName = "exp_special_remez_T6_0<float>";
entry.accuracy_test<float>(exp_special_remez_T6_0<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2813, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "remez", "float2x4", "simd",  };
entry.subName = "exp_special_remez_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_T6_0<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2814, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "remez", "float", "simd",  };
entry.subName = "exp_special_remez_T6_0_float_simd";
entry.accuracy_test<float>(exp_special_remez_T6_0_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2815, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "remez_pade", "float", "scalar",  };
entry.subName = "exp_special_remez_pade_T6_6<float>";
entry.accuracy_test<float>(exp_special_remez_pade_T6_6<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2816, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "remez_pade", "float2x4", "simd",  };
entry.subName = "exp_special_remez_pade_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_pade_T6_6<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2817, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "remez_pade", "float", "simd",  };
entry.subName = "exp_special_remez_pade_T6_6_float_simd";
entry.accuracy_test<float>(exp_special_remez_pade_T6_6_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2818, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "float", "scalar",  };
entry.subName = "exp_special_T6_6<float>";
entry.accuracy_test<float>(exp_special_T6_6<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2819, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "float2x4", "simd",  };
entry.subName = "exp_special_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_T6_6<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2820, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "float", "simd",  };
entry.subName = "exp_special_T6_6_float_simd";
entry.accuracy_test<float>(exp_special_T6_6_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2821, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "remez", "float", "scalar",  };
entry.subName = "exp_special_remez_fma_ec_T7_0<float>";
entry.accuracy_test<float>(exp_special_remez_fma_ec_T7_0<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2822, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "remez", "float2x4", "simd",  };
entry.subName = "exp_special_remez_fma_ec_T7_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_fma_ec_T7_0<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2823, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "remez", "float", "simd",  };
entry.subName = "exp_special_remez_fma_ec_T7_0_float_simd";
entry.accuracy_test<float>(exp_special_remez_fma_ec_T7_0_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2824, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "remez_pade", "float", "scalar",  };
entry.subName = "exp_special_remez_pade_fma_ec_T7_7<float>";
entry.accuracy_test<float>(exp_special_remez_pade_fma_ec_T7_7<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2825, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "remez_pade", "float2x4", "simd",  };
entry.subName = "exp_special_remez_pade_fma_ec_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_pade_fma_ec_T7_7<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2826, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "remez_pade", "float", "simd",  };
entry.subName = "exp_special_remez_pade_fma_ec_T7_7_float_simd";
entry.accuracy_test<float>(exp_special_remez_pade_fma_ec_T7_7_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2827, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "float", "scalar",  };
entry.subName = "exp_special_fma_ec_T7_7<float>";
entry.accuracy_test<float>(exp_special_fma_ec_T7_7<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2828, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "float2x4", "simd",  };
entry.subName = "exp_special_fma_ec_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_fma_ec_T7_7<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2829, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "float", "simd",  };
entry.subName = "exp_special_fma_ec_T7_7_float_simd";
entry.accuracy_test<float>(exp_special_fma_ec_T7_7_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2830, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "remez", "float", "scalar",  };
entry.subName = "exp_special_remez_fma_T7_0<float>";
entry.accuracy_test<float>(exp_special_remez_fma_T7_0<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2831, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "remez", "float2x4", "simd",  };
entry.subName = "exp_special_remez_fma_T7_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_fma_T7_0<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2832, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "remez", "float", "simd",  };
entry.subName = "exp_special_remez_fma_T7_0_float_simd";
entry.accuracy_test<float>(exp_special_remez_fma_T7_0_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2833, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "remez_pade", "float", "scalar",  };
entry.subName = "exp_special_remez_pade_fma_T7_7<float>";
entry.accuracy_test<float>(exp_special_remez_pade_fma_T7_7<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2834, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "remez_pade", "float2x4", "simd",  };
entry.subName = "exp_special_remez_pade_fma_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_pade_fma_T7_7<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2835, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "remez_pade", "float", "simd",  };
entry.subName = "exp_special_remez_pade_fma_T7_7_float_simd";
entry.accuracy_test<float>(exp_special_remez_pade_fma_T7_7_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2836, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "float", "scalar",  };
entry.subName = "exp_special_fma_T7_7<float>";
entry.accuracy_test<float>(exp_special_fma_T7_7<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2837, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "float2x4", "simd",  };
entry.subName = "exp_special_fma_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_fma_T7_7<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2838, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "float", "simd",  };
entry.subName = "exp_special_fma_T7_7_float_simd";
entry.accuracy_test<float>(exp_special_fma_T7_7_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2839, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "remez", "float", "scalar",  };
entry.subName = "exp_special_remez_ec_T7_0<float>";
entry.accuracy_test<float>(exp_special_remez_ec_T7_0<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2840, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "remez", "float2x4", "simd",  };
entry.subName = "exp_special_remez_ec_T7_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_ec_T7_0<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2841, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "remez", "float", "simd",  };
entry.subName = "exp_special_remez_ec_T7_0_float_simd";
entry.accuracy_test<float>(exp_special_remez_ec_T7_0_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2842, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "remez_pade", "float", "scalar",  };
entry.subName = "exp_special_remez_pade_ec_T7_7<float>";
entry.accuracy_test<float>(exp_special_remez_pade_ec_T7_7<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2843, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "remez_pade", "float2x4", "simd",  };
entry.subName = "exp_special_remez_pade_ec_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_pade_ec_T7_7<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2844, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "remez_pade", "float", "simd",  };
entry.subName = "exp_special_remez_pade_ec_T7_7_float_simd";
entry.accuracy_test<float>(exp_special_remez_pade_ec_T7_7_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2845, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "float", "scalar",  };
entry.subName = "exp_special_ec_T7_7<float>";
entry.accuracy_test<float>(exp_special_ec_T7_7<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2846, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "float2x4", "simd",  };
entry.subName = "exp_special_ec_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_ec_T7_7<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2847, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "float", "simd",  };
entry.subName = "exp_special_ec_T7_7_float_simd";
entry.accuracy_test<float>(exp_special_ec_T7_7_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2848, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "remez", "float", "scalar",  };
entry.subName = "exp_special_remez_T7_0<float>";
entry.accuracy_test<float>(exp_special_remez_T7_0<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2849, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "remez", "float2x4", "simd",  };
entry.subName = "exp_special_remez_T7_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_T7_0<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2850, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "remez", "float", "simd",  };
entry.subName = "exp_special_remez_T7_0_float_simd";
entry.accuracy_test<float>(exp_special_remez_T7_0_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2851, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "remez_pade", "float", "scalar",  };
entry.subName = "exp_special_remez_pade_T7_7<float>";
entry.accuracy_test<float>(exp_special_remez_pade_T7_7<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2852, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "remez_pade", "float2x4", "simd",  };
entry.subName = "exp_special_remez_pade_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_pade_T7_7<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2853, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "remez_pade", "float", "simd",  };
entry.subName = "exp_special_remez_pade_T7_7_float_simd";
entry.accuracy_test<float>(exp_special_remez_pade_T7_7_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2854, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "float", "scalar",  };
entry.subName = "exp_special_T7_7<float>";
entry.accuracy_test<float>(exp_special_T7_7<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2855, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "float2x4", "simd",  };
entry.subName = "exp_special_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_T7_7<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2856, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "float", "simd",  };
entry.subName = "exp_special_T7_7_float_simd";
entry.accuracy_test<float>(exp_special_T7_7_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2857, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "remez", "float", "scalar",  };
entry.subName = "exp_special_remez_fma_ec_T8_0<float>";
entry.accuracy_test<float>(exp_special_remez_fma_ec_T8_0<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2858, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "remez", "float2x4", "simd",  };
entry.subName = "exp_special_remez_fma_ec_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_fma_ec_T8_0<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2859, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "remez", "float", "simd",  };
entry.subName = "exp_special_remez_fma_ec_T8_0_float_simd";
entry.accuracy_test<float>(exp_special_remez_fma_ec_T8_0_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2860, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "remez_pade", "float", "scalar",  };
entry.subName = "exp_special_remez_pade_fma_ec_T8_8<float>";
entry.accuracy_test<float>(exp_special_remez_pade_fma_ec_T8_8<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2861, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "remez_pade", "float2x4", "simd",  };
entry.subName = "exp_special_remez_pade_fma_ec_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_pade_fma_ec_T8_8<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2862, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "remez_pade", "float", "simd",  };
entry.subName = "exp_special_remez_pade_fma_ec_T8_8_float_simd";
entry.accuracy_test<float>(exp_special_remez_pade_fma_ec_T8_8_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2863, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "float", "scalar",  };
entry.subName = "exp_special_fma_ec_T8_8<float>";
entry.accuracy_test<float>(exp_special_fma_ec_T8_8<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2864, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "float2x4", "simd",  };
entry.subName = "exp_special_fma_ec_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_fma_ec_T8_8<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2865, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "float", "simd",  };
entry.subName = "exp_special_fma_ec_T8_8_float_simd";
entry.accuracy_test<float>(exp_special_fma_ec_T8_8_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2866, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "remez", "float", "scalar",  };
entry.subName = "exp_special_remez_fma_T8_0<float>";
entry.accuracy_test<float>(exp_special_remez_fma_T8_0<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2867, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "remez", "float2x4", "simd",  };
entry.subName = "exp_special_remez_fma_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_fma_T8_0<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2868, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "remez", "float", "simd",  };
entry.subName = "exp_special_remez_fma_T8_0_float_simd";
entry.accuracy_test<float>(exp_special_remez_fma_T8_0_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2869, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "remez_pade", "float", "scalar",  };
entry.subName = "exp_special_remez_pade_fma_T8_8<float>";
entry.accuracy_test<float>(exp_special_remez_pade_fma_T8_8<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2870, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "remez_pade", "float2x4", "simd",  };
entry.subName = "exp_special_remez_pade_fma_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_pade_fma_T8_8<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2871, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "remez_pade", "float", "simd",  };
entry.subName = "exp_special_remez_pade_fma_T8_8_float_simd";
entry.accuracy_test<float>(exp_special_remez_pade_fma_T8_8_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2872, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "float", "scalar",  };
entry.subName = "exp_special_fma_T8_8<float>";
entry.accuracy_test<float>(exp_special_fma_T8_8<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2873, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "float2x4", "simd",  };
entry.subName = "exp_special_fma_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_fma_T8_8<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2874, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "float", "simd",  };
entry.subName = "exp_special_fma_T8_8_float_simd";
entry.accuracy_test<float>(exp_special_fma_T8_8_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2875, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "remez", "float", "scalar",  };
entry.subName = "exp_special_remez_ec_T8_0<float>";
entry.accuracy_test<float>(exp_special_remez_ec_T8_0<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2876, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "remez", "float2x4", "simd",  };
entry.subName = "exp_special_remez_ec_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_ec_T8_0<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2877, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "remez", "float", "simd",  };
entry.subName = "exp_special_remez_ec_T8_0_float_simd";
entry.accuracy_test<float>(exp_special_remez_ec_T8_0_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2878, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "remez_pade", "float", "scalar",  };
entry.subName = "exp_special_remez_pade_ec_T8_8<float>";
entry.accuracy_test<float>(exp_special_remez_pade_ec_T8_8<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2879, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "remez_pade", "float2x4", "simd",  };
entry.subName = "exp_special_remez_pade_ec_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_pade_ec_T8_8<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2880, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "remez_pade", "float", "simd",  };
entry.subName = "exp_special_remez_pade_ec_T8_8_float_simd";
entry.accuracy_test<float>(exp_special_remez_pade_ec_T8_8_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2881, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "float", "scalar",  };
entry.subName = "exp_special_ec_T8_8<float>";
entry.accuracy_test<float>(exp_special_ec_T8_8<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2882, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "float2x4", "simd",  };
entry.subName = "exp_special_ec_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_ec_T8_8<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2883, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "float", "simd",  };
entry.subName = "exp_special_ec_T8_8_float_simd";
entry.accuracy_test<float>(exp_special_ec_T8_8_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2884, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "remez", "float", "scalar",  };
entry.subName = "exp_special_remez_T8_0<float>";
entry.accuracy_test<float>(exp_special_remez_T8_0<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2885, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "remez", "float2x4", "simd",  };
entry.subName = "exp_special_remez_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_T8_0<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2886, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "remez", "float", "simd",  };
entry.subName = "exp_special_remez_T8_0_float_simd";
entry.accuracy_test<float>(exp_special_remez_T8_0_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2887, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "remez_pade", "float", "scalar",  };
entry.subName = "exp_special_remez_pade_T8_8<float>";
entry.accuracy_test<float>(exp_special_remez_pade_T8_8<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2888, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "remez_pade", "float2x4", "simd",  };
entry.subName = "exp_special_remez_pade_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_pade_T8_8<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2889, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "remez_pade", "float", "simd",  };
entry.subName = "exp_special_remez_pade_T8_8_float_simd";
entry.accuracy_test<float>(exp_special_remez_pade_T8_8_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2890, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "float", "scalar",  };
entry.subName = "exp_special_T8_8<float>";
entry.accuracy_test<float>(exp_special_T8_8<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2891, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "float2x4", "simd",  };
entry.subName = "exp_special_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_T8_8<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2892, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "float", "simd",  };
entry.subName = "exp_special_T8_8_float_simd";
entry.accuracy_test<float>(exp_special_T8_8_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2893, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "remez", "float", "scalar",  };
entry.subName = "exp_special_remez_fma_ec_T9_0<float>";
entry.accuracy_test<float>(exp_special_remez_fma_ec_T9_0<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2894, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "remez", "float2x4", "simd",  };
entry.subName = "exp_special_remez_fma_ec_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_fma_ec_T9_0<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2895, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "remez", "float", "simd",  };
entry.subName = "exp_special_remez_fma_ec_T9_0_float_simd";
entry.accuracy_test<float>(exp_special_remez_fma_ec_T9_0_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2896, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "remez_pade", "float", "scalar",  };
entry.subName = "exp_special_remez_pade_fma_ec_T9_9<float>";
entry.accuracy_test<float>(exp_special_remez_pade_fma_ec_T9_9<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2897, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "remez_pade", "float2x4", "simd",  };
entry.subName = "exp_special_remez_pade_fma_ec_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_pade_fma_ec_T9_9<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2898, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "remez_pade", "float", "simd",  };
entry.subName = "exp_special_remez_pade_fma_ec_T9_9_float_simd";
entry.accuracy_test<float>(exp_special_remez_pade_fma_ec_T9_9_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2899, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "float", "scalar",  };
entry.subName = "exp_special_fma_ec_T9_9<float>";
entry.accuracy_test<float>(exp_special_fma_ec_T9_9<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2900, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "float2x4", "simd",  };
entry.subName = "exp_special_fma_ec_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_fma_ec_T9_9<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2901, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "exp_special", "float", "simd",  };
entry.subName = "exp_special_fma_ec_T9_9_float_simd";
entry.accuracy_test<float>(exp_special_fma_ec_T9_9_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2902, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "remez", "float", "scalar",  };
entry.subName = "exp_special_remez_fma_T9_0<float>";
entry.accuracy_test<float>(exp_special_remez_fma_T9_0<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2903, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "remez", "float2x4", "simd",  };
entry.subName = "exp_special_remez_fma_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_fma_T9_0<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2904, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "remez", "float", "simd",  };
entry.subName = "exp_special_remez_fma_T9_0_float_simd";
entry.accuracy_test<float>(exp_special_remez_fma_T9_0_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2905, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "remez_pade", "float", "scalar",  };
entry.subName = "exp_special_remez_pade_fma_T9_9<float>";
entry.accuracy_test<float>(exp_special_remez_pade_fma_T9_9<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2906, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "remez_pade", "float2x4", "simd",  };
entry.subName = "exp_special_remez_pade_fma_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_pade_fma_T9_9<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2907, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "remez_pade", "float", "simd",  };
entry.subName = "exp_special_remez_pade_fma_T9_9_float_simd";
entry.accuracy_test<float>(exp_special_remez_pade_fma_T9_9_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2908, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "float", "scalar",  };
entry.subName = "exp_special_fma_T9_9<float>";
entry.accuracy_test<float>(exp_special_fma_T9_9<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2909, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "float2x4", "simd",  };
entry.subName = "exp_special_fma_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_fma_T9_9<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2910, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "exp_special", "float", "simd",  };
entry.subName = "exp_special_fma_T9_9_float_simd";
entry.accuracy_test<float>(exp_special_fma_T9_9_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2911, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "remez", "float", "scalar",  };
entry.subName = "exp_special_remez_ec_T9_0<float>";
entry.accuracy_test<float>(exp_special_remez_ec_T9_0<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2912, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "remez", "float2x4", "simd",  };
entry.subName = "exp_special_remez_ec_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_ec_T9_0<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2913, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "remez", "float", "simd",  };
entry.subName = "exp_special_remez_ec_T9_0_float_simd";
entry.accuracy_test<float>(exp_special_remez_ec_T9_0_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2914, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "remez_pade", "float", "scalar",  };
entry.subName = "exp_special_remez_pade_ec_T9_9<float>";
entry.accuracy_test<float>(exp_special_remez_pade_ec_T9_9<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2915, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "remez_pade", "float2x4", "simd",  };
entry.subName = "exp_special_remez_pade_ec_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_pade_ec_T9_9<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2916, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "remez_pade", "float", "simd",  };
entry.subName = "exp_special_remez_pade_ec_T9_9_float_simd";
entry.accuracy_test<float>(exp_special_remez_pade_ec_T9_9_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2917, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "float", "scalar",  };
entry.subName = "exp_special_ec_T9_9<float>";
entry.accuracy_test<float>(exp_special_ec_T9_9<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2918, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "float2x4", "simd",  };
entry.subName = "exp_special_ec_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_ec_T9_9<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2919, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "exp_special", "float", "simd",  };
entry.subName = "exp_special_ec_T9_9_float_simd";
entry.accuracy_test<float>(exp_special_ec_T9_9_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2920, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "remez", "float", "scalar",  };
entry.subName = "exp_special_remez_T9_0<float>";
entry.accuracy_test<float>(exp_special_remez_T9_0<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2921, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "remez", "float2x4", "simd",  };
entry.subName = "exp_special_remez_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_T9_0<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2922, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "remez", "float", "simd",  };
entry.subName = "exp_special_remez_T9_0_float_simd";
entry.accuracy_test<float>(exp_special_remez_T9_0_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2923, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "remez_pade", "float", "scalar",  };
entry.subName = "exp_special_remez_pade_T9_9<float>";
entry.accuracy_test<float>(exp_special_remez_pade_T9_9<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2924, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "remez_pade", "float2x4", "simd",  };
entry.subName = "exp_special_remez_pade_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_remez_pade_T9_9<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2925, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "remez_pade", "float", "simd",  };
entry.subName = "exp_special_remez_pade_T9_9_float_simd";
entry.accuracy_test<float>(exp_special_remez_pade_T9_9_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2926, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "float", "scalar",  };
entry.subName = "exp_special_T9_9<float>";
entry.accuracy_test<float>(exp_special_T9_9<float>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2927, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "float2x4", "simd",  };
entry.subName = "exp_special_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(exp_special_T9_9<cr::simd::float2x4>, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2928, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "exp_special", "float", "simd",  };
entry.subName = "exp_special_T9_9_float_simd";
entry.accuracy_test<float>(exp_special_T9_9_float_simd, [](float x) { return 8.175798915643707f * std::powf(1.0594630943592953f, x * 127.0f); }, -1.0f, 1.2f, -1.0f, 1.2f);
entry.time<float>(msvc_bug_workaround_2929, -1.0f, 1.2f);
}
{
auto& entry = testResult.make();
entry.tags = { "tan", "std::tanf", "float", "scalar", "reference",  };
entry.subName = "std::tanf";
entry.accuracy_test<float>(std::tanf, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2930, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_remez_abs_fma_ec_T3_0<float>";
entry.accuracy_test<float>(tan_remez_remez_abs_fma_ec_T3_0<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2931, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_remez_abs_fma_ec_T3_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_remez_abs_fma_ec_T3_0<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2932, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_remez_abs_fma_ec_T3_0_float_simd";
entry.accuracy_test<float>(tan_remez_remez_abs_fma_ec_T3_0_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2933, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_pade_abs_fma_ec_T3_3<float>";
entry.accuracy_test<float>(tan_remez_pade_abs_fma_ec_T3_3<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2934, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_pade_abs_fma_ec_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_pade_abs_fma_ec_T3_3<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2935, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_pade_abs_fma_ec_T3_3_float_simd";
entry.accuracy_test<float>(tan_remez_pade_abs_fma_ec_T3_3_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2936, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "float", "scalar",  };
entry.subName = "tan_fma_ec_T3_3<float>";
entry.accuracy_test<float>(tan_fma_ec_T3_3<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2937, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "float2x4", "simd",  };
entry.subName = "tan_fma_ec_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_fma_ec_T3_3<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2938, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "float", "simd",  };
entry.subName = "tan_fma_ec_T3_3_float_simd";
entry.accuracy_test<float>(tan_fma_ec_T3_3_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2939, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_remez_abs_fma_T3_0<float>";
entry.accuracy_test<float>(tan_remez_remez_abs_fma_T3_0<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2940, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_remez_abs_fma_T3_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_remez_abs_fma_T3_0<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2941, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_remez_abs_fma_T3_0_float_simd";
entry.accuracy_test<float>(tan_remez_remez_abs_fma_T3_0_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2942, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_pade_abs_fma_T3_3<float>";
entry.accuracy_test<float>(tan_remez_pade_abs_fma_T3_3<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2943, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_pade_abs_fma_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_pade_abs_fma_T3_3<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2944, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_pade_abs_fma_T3_3_float_simd";
entry.accuracy_test<float>(tan_remez_pade_abs_fma_T3_3_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2945, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "float", "scalar",  };
entry.subName = "tan_fma_T3_3<float>";
entry.accuracy_test<float>(tan_fma_T3_3<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2946, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "float2x4", "simd",  };
entry.subName = "tan_fma_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_fma_T3_3<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2947, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "float", "simd",  };
entry.subName = "tan_fma_T3_3_float_simd";
entry.accuracy_test<float>(tan_fma_T3_3_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2948, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_remez_abs_ec_T3_0<float>";
entry.accuracy_test<float>(tan_remez_remez_abs_ec_T3_0<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2949, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_remez_abs_ec_T3_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_remez_abs_ec_T3_0<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2950, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_remez_abs_ec_T3_0_float_simd";
entry.accuracy_test<float>(tan_remez_remez_abs_ec_T3_0_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2951, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_pade_abs_ec_T3_3<float>";
entry.accuracy_test<float>(tan_remez_pade_abs_ec_T3_3<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2952, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_pade_abs_ec_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_pade_abs_ec_T3_3<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2953, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_pade_abs_ec_T3_3_float_simd";
entry.accuracy_test<float>(tan_remez_pade_abs_ec_T3_3_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2954, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "float", "scalar",  };
entry.subName = "tan_ec_T3_3<float>";
entry.accuracy_test<float>(tan_ec_T3_3<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2955, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "float2x4", "simd",  };
entry.subName = "tan_ec_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_ec_T3_3<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2956, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "float", "simd",  };
entry.subName = "tan_ec_T3_3_float_simd";
entry.accuracy_test<float>(tan_ec_T3_3_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2957, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_remez_abs_T3_0<float>";
entry.accuracy_test<float>(tan_remez_remez_abs_T3_0<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2958, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_remez_abs_T3_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_remez_abs_T3_0<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2959, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_remez_abs_T3_0_float_simd";
entry.accuracy_test<float>(tan_remez_remez_abs_T3_0_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2960, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_pade_abs_T3_3<float>";
entry.accuracy_test<float>(tan_remez_pade_abs_T3_3<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2961, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_pade_abs_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_pade_abs_T3_3<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2962, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_pade_abs_T3_3_float_simd";
entry.accuracy_test<float>(tan_remez_pade_abs_T3_3_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2963, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "float", "scalar",  };
entry.subName = "tan_T3_3<float>";
entry.accuracy_test<float>(tan_T3_3<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2964, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "float2x4", "simd",  };
entry.subName = "tan_T3_3<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_T3_3<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2965, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "float", "simd",  };
entry.subName = "tan_T3_3_float_simd";
entry.accuracy_test<float>(tan_T3_3_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2966, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_remez_abs_fma_ec_T4_0<float>";
entry.accuracy_test<float>(tan_remez_remez_abs_fma_ec_T4_0<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2967, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_remez_abs_fma_ec_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_remez_abs_fma_ec_T4_0<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2968, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_remez_abs_fma_ec_T4_0_float_simd";
entry.accuracy_test<float>(tan_remez_remez_abs_fma_ec_T4_0_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2969, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_pade_abs_fma_ec_T4_4<float>";
entry.accuracy_test<float>(tan_remez_pade_abs_fma_ec_T4_4<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2970, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_pade_abs_fma_ec_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_pade_abs_fma_ec_T4_4<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2971, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_pade_abs_fma_ec_T4_4_float_simd";
entry.accuracy_test<float>(tan_remez_pade_abs_fma_ec_T4_4_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2972, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "float", "scalar",  };
entry.subName = "tan_fma_ec_T4_4<float>";
entry.accuracy_test<float>(tan_fma_ec_T4_4<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2973, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "float2x4", "simd",  };
entry.subName = "tan_fma_ec_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_fma_ec_T4_4<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2974, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "float", "simd",  };
entry.subName = "tan_fma_ec_T4_4_float_simd";
entry.accuracy_test<float>(tan_fma_ec_T4_4_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2975, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_remez_abs_fma_T4_0<float>";
entry.accuracy_test<float>(tan_remez_remez_abs_fma_T4_0<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2976, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_remez_abs_fma_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_remez_abs_fma_T4_0<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2977, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_remez_abs_fma_T4_0_float_simd";
entry.accuracy_test<float>(tan_remez_remez_abs_fma_T4_0_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2978, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_pade_abs_fma_T4_4<float>";
entry.accuracy_test<float>(tan_remez_pade_abs_fma_T4_4<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2979, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_pade_abs_fma_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_pade_abs_fma_T4_4<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2980, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_pade_abs_fma_T4_4_float_simd";
entry.accuracy_test<float>(tan_remez_pade_abs_fma_T4_4_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2981, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "float", "scalar",  };
entry.subName = "tan_fma_T4_4<float>";
entry.accuracy_test<float>(tan_fma_T4_4<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2982, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "float2x4", "simd",  };
entry.subName = "tan_fma_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_fma_T4_4<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2983, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "float", "simd",  };
entry.subName = "tan_fma_T4_4_float_simd";
entry.accuracy_test<float>(tan_fma_T4_4_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2984, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_remez_abs_ec_T4_0<float>";
entry.accuracy_test<float>(tan_remez_remez_abs_ec_T4_0<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2985, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_remez_abs_ec_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_remez_abs_ec_T4_0<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2986, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_remez_abs_ec_T4_0_float_simd";
entry.accuracy_test<float>(tan_remez_remez_abs_ec_T4_0_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2987, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_pade_abs_ec_T4_4<float>";
entry.accuracy_test<float>(tan_remez_pade_abs_ec_T4_4<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2988, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_pade_abs_ec_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_pade_abs_ec_T4_4<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2989, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_pade_abs_ec_T4_4_float_simd";
entry.accuracy_test<float>(tan_remez_pade_abs_ec_T4_4_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2990, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "float", "scalar",  };
entry.subName = "tan_ec_T4_4<float>";
entry.accuracy_test<float>(tan_ec_T4_4<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2991, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "float2x4", "simd",  };
entry.subName = "tan_ec_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_ec_T4_4<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2992, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "float", "simd",  };
entry.subName = "tan_ec_T4_4_float_simd";
entry.accuracy_test<float>(tan_ec_T4_4_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2993, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_remez_abs_T4_0<float>";
entry.accuracy_test<float>(tan_remez_remez_abs_T4_0<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2994, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_remez_abs_T4_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_remez_abs_T4_0<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2995, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_remez_abs_T4_0_float_simd";
entry.accuracy_test<float>(tan_remez_remez_abs_T4_0_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2996, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_pade_abs_T4_4<float>";
entry.accuracy_test<float>(tan_remez_pade_abs_T4_4<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2997, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_pade_abs_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_pade_abs_T4_4<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_2998, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_pade_abs_T4_4_float_simd";
entry.accuracy_test<float>(tan_remez_pade_abs_T4_4_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_2999, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "float", "scalar",  };
entry.subName = "tan_T4_4<float>";
entry.accuracy_test<float>(tan_T4_4<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3000, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "float2x4", "simd",  };
entry.subName = "tan_T4_4<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_T4_4<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3001, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "float", "simd",  };
entry.subName = "tan_T4_4_float_simd";
entry.accuracy_test<float>(tan_T4_4_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3002, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_remez_abs_fma_ec_T5_0<float>";
entry.accuracy_test<float>(tan_remez_remez_abs_fma_ec_T5_0<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3003, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_remez_abs_fma_ec_T5_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_remez_abs_fma_ec_T5_0<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3004, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_remez_abs_fma_ec_T5_0_float_simd";
entry.accuracy_test<float>(tan_remez_remez_abs_fma_ec_T5_0_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3005, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_pade_abs_fma_ec_T5_5<float>";
entry.accuracy_test<float>(tan_remez_pade_abs_fma_ec_T5_5<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3006, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_pade_abs_fma_ec_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_pade_abs_fma_ec_T5_5<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3007, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_pade_abs_fma_ec_T5_5_float_simd";
entry.accuracy_test<float>(tan_remez_pade_abs_fma_ec_T5_5_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3008, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "float", "scalar",  };
entry.subName = "tan_fma_ec_T5_5<float>";
entry.accuracy_test<float>(tan_fma_ec_T5_5<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3009, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "float2x4", "simd",  };
entry.subName = "tan_fma_ec_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_fma_ec_T5_5<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3010, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "float", "simd",  };
entry.subName = "tan_fma_ec_T5_5_float_simd";
entry.accuracy_test<float>(tan_fma_ec_T5_5_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3011, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_remez_abs_fma_T5_0<float>";
entry.accuracy_test<float>(tan_remez_remez_abs_fma_T5_0<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3012, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_remez_abs_fma_T5_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_remez_abs_fma_T5_0<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3013, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_remez_abs_fma_T5_0_float_simd";
entry.accuracy_test<float>(tan_remez_remez_abs_fma_T5_0_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3014, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_pade_abs_fma_T5_5<float>";
entry.accuracy_test<float>(tan_remez_pade_abs_fma_T5_5<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3015, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_pade_abs_fma_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_pade_abs_fma_T5_5<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3016, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_pade_abs_fma_T5_5_float_simd";
entry.accuracy_test<float>(tan_remez_pade_abs_fma_T5_5_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3017, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "float", "scalar",  };
entry.subName = "tan_fma_T5_5<float>";
entry.accuracy_test<float>(tan_fma_T5_5<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3018, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "float2x4", "simd",  };
entry.subName = "tan_fma_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_fma_T5_5<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3019, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "float", "simd",  };
entry.subName = "tan_fma_T5_5_float_simd";
entry.accuracy_test<float>(tan_fma_T5_5_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3020, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_remez_abs_ec_T5_0<float>";
entry.accuracy_test<float>(tan_remez_remez_abs_ec_T5_0<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3021, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_remez_abs_ec_T5_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_remez_abs_ec_T5_0<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3022, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_remez_abs_ec_T5_0_float_simd";
entry.accuracy_test<float>(tan_remez_remez_abs_ec_T5_0_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3023, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_pade_abs_ec_T5_5<float>";
entry.accuracy_test<float>(tan_remez_pade_abs_ec_T5_5<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3024, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_pade_abs_ec_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_pade_abs_ec_T5_5<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3025, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_pade_abs_ec_T5_5_float_simd";
entry.accuracy_test<float>(tan_remez_pade_abs_ec_T5_5_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3026, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "float", "scalar",  };
entry.subName = "tan_ec_T5_5<float>";
entry.accuracy_test<float>(tan_ec_T5_5<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3027, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "float2x4", "simd",  };
entry.subName = "tan_ec_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_ec_T5_5<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3028, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "float", "simd",  };
entry.subName = "tan_ec_T5_5_float_simd";
entry.accuracy_test<float>(tan_ec_T5_5_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3029, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_remez_abs_T5_0<float>";
entry.accuracy_test<float>(tan_remez_remez_abs_T5_0<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3030, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_remez_abs_T5_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_remez_abs_T5_0<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3031, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_remez_abs_T5_0_float_simd";
entry.accuracy_test<float>(tan_remez_remez_abs_T5_0_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3032, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_pade_abs_T5_5<float>";
entry.accuracy_test<float>(tan_remez_pade_abs_T5_5<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3033, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_pade_abs_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_pade_abs_T5_5<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3034, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_pade_abs_T5_5_float_simd";
entry.accuracy_test<float>(tan_remez_pade_abs_T5_5_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3035, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "float", "scalar",  };
entry.subName = "tan_T5_5<float>";
entry.accuracy_test<float>(tan_T5_5<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3036, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "float2x4", "simd",  };
entry.subName = "tan_T5_5<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_T5_5<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3037, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "float", "simd",  };
entry.subName = "tan_T5_5_float_simd";
entry.accuracy_test<float>(tan_T5_5_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3038, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_remez_abs_fma_ec_T6_0<float>";
entry.accuracy_test<float>(tan_remez_remez_abs_fma_ec_T6_0<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3039, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_remez_abs_fma_ec_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_remez_abs_fma_ec_T6_0<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3040, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_remez_abs_fma_ec_T6_0_float_simd";
entry.accuracy_test<float>(tan_remez_remez_abs_fma_ec_T6_0_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3041, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_pade_abs_fma_ec_T6_6<float>";
entry.accuracy_test<float>(tan_remez_pade_abs_fma_ec_T6_6<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3042, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_pade_abs_fma_ec_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_pade_abs_fma_ec_T6_6<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3043, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_pade_abs_fma_ec_T6_6_float_simd";
entry.accuracy_test<float>(tan_remez_pade_abs_fma_ec_T6_6_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3044, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "float", "scalar",  };
entry.subName = "tan_fma_ec_T6_6<float>";
entry.accuracy_test<float>(tan_fma_ec_T6_6<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3045, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "float2x4", "simd",  };
entry.subName = "tan_fma_ec_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_fma_ec_T6_6<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3046, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "float", "simd",  };
entry.subName = "tan_fma_ec_T6_6_float_simd";
entry.accuracy_test<float>(tan_fma_ec_T6_6_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3047, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_remez_abs_fma_T6_0<float>";
entry.accuracy_test<float>(tan_remez_remez_abs_fma_T6_0<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3048, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_remez_abs_fma_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_remez_abs_fma_T6_0<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3049, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_remez_abs_fma_T6_0_float_simd";
entry.accuracy_test<float>(tan_remez_remez_abs_fma_T6_0_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3050, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_pade_abs_fma_T6_6<float>";
entry.accuracy_test<float>(tan_remez_pade_abs_fma_T6_6<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3051, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_pade_abs_fma_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_pade_abs_fma_T6_6<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3052, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_pade_abs_fma_T6_6_float_simd";
entry.accuracy_test<float>(tan_remez_pade_abs_fma_T6_6_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3053, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "float", "scalar",  };
entry.subName = "tan_fma_T6_6<float>";
entry.accuracy_test<float>(tan_fma_T6_6<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3054, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "float2x4", "simd",  };
entry.subName = "tan_fma_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_fma_T6_6<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3055, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "float", "simd",  };
entry.subName = "tan_fma_T6_6_float_simd";
entry.accuracy_test<float>(tan_fma_T6_6_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3056, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_remez_abs_ec_T6_0<float>";
entry.accuracy_test<float>(tan_remez_remez_abs_ec_T6_0<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3057, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_remez_abs_ec_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_remez_abs_ec_T6_0<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3058, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_remez_abs_ec_T6_0_float_simd";
entry.accuracy_test<float>(tan_remez_remez_abs_ec_T6_0_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3059, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_pade_abs_ec_T6_6<float>";
entry.accuracy_test<float>(tan_remez_pade_abs_ec_T6_6<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3060, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_pade_abs_ec_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_pade_abs_ec_T6_6<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3061, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_pade_abs_ec_T6_6_float_simd";
entry.accuracy_test<float>(tan_remez_pade_abs_ec_T6_6_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3062, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "float", "scalar",  };
entry.subName = "tan_ec_T6_6<float>";
entry.accuracy_test<float>(tan_ec_T6_6<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3063, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "float2x4", "simd",  };
entry.subName = "tan_ec_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_ec_T6_6<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3064, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "float", "simd",  };
entry.subName = "tan_ec_T6_6_float_simd";
entry.accuracy_test<float>(tan_ec_T6_6_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3065, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_remez_abs_T6_0<float>";
entry.accuracy_test<float>(tan_remez_remez_abs_T6_0<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3066, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_remez_abs_T6_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_remez_abs_T6_0<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3067, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_remez_abs_T6_0_float_simd";
entry.accuracy_test<float>(tan_remez_remez_abs_T6_0_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3068, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_pade_abs_T6_6<float>";
entry.accuracy_test<float>(tan_remez_pade_abs_T6_6<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3069, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_pade_abs_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_pade_abs_T6_6<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3070, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_pade_abs_T6_6_float_simd";
entry.accuracy_test<float>(tan_remez_pade_abs_T6_6_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3071, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "float", "scalar",  };
entry.subName = "tan_T6_6<float>";
entry.accuracy_test<float>(tan_T6_6<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3072, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "float2x4", "simd",  };
entry.subName = "tan_T6_6<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_T6_6<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3073, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "float", "simd",  };
entry.subName = "tan_T6_6_float_simd";
entry.accuracy_test<float>(tan_T6_6_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3074, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_remez_abs_fma_ec_T7_0<float>";
entry.accuracy_test<float>(tan_remez_remez_abs_fma_ec_T7_0<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3075, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_remez_abs_fma_ec_T7_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_remez_abs_fma_ec_T7_0<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3076, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_remez_abs_fma_ec_T7_0_float_simd";
entry.accuracy_test<float>(tan_remez_remez_abs_fma_ec_T7_0_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3077, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_pade_abs_fma_ec_T7_7<float>";
entry.accuracy_test<float>(tan_remez_pade_abs_fma_ec_T7_7<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3078, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_pade_abs_fma_ec_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_pade_abs_fma_ec_T7_7<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3079, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_pade_abs_fma_ec_T7_7_float_simd";
entry.accuracy_test<float>(tan_remez_pade_abs_fma_ec_T7_7_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3080, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "float", "scalar",  };
entry.subName = "tan_fma_ec_T7_7<float>";
entry.accuracy_test<float>(tan_fma_ec_T7_7<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3081, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "float2x4", "simd",  };
entry.subName = "tan_fma_ec_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_fma_ec_T7_7<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3082, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "float", "simd",  };
entry.subName = "tan_fma_ec_T7_7_float_simd";
entry.accuracy_test<float>(tan_fma_ec_T7_7_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3083, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_remez_abs_fma_T7_0<float>";
entry.accuracy_test<float>(tan_remez_remez_abs_fma_T7_0<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3084, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_remez_abs_fma_T7_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_remez_abs_fma_T7_0<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3085, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_remez_abs_fma_T7_0_float_simd";
entry.accuracy_test<float>(tan_remez_remez_abs_fma_T7_0_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3086, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_pade_abs_fma_T7_7<float>";
entry.accuracy_test<float>(tan_remez_pade_abs_fma_T7_7<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3087, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_pade_abs_fma_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_pade_abs_fma_T7_7<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3088, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_pade_abs_fma_T7_7_float_simd";
entry.accuracy_test<float>(tan_remez_pade_abs_fma_T7_7_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3089, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "float", "scalar",  };
entry.subName = "tan_fma_T7_7<float>";
entry.accuracy_test<float>(tan_fma_T7_7<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3090, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "float2x4", "simd",  };
entry.subName = "tan_fma_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_fma_T7_7<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3091, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "float", "simd",  };
entry.subName = "tan_fma_T7_7_float_simd";
entry.accuracy_test<float>(tan_fma_T7_7_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3092, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_remez_abs_ec_T7_0<float>";
entry.accuracy_test<float>(tan_remez_remez_abs_ec_T7_0<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3093, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_remez_abs_ec_T7_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_remez_abs_ec_T7_0<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3094, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_remez_abs_ec_T7_0_float_simd";
entry.accuracy_test<float>(tan_remez_remez_abs_ec_T7_0_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3095, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_pade_abs_ec_T7_7<float>";
entry.accuracy_test<float>(tan_remez_pade_abs_ec_T7_7<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3096, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_pade_abs_ec_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_pade_abs_ec_T7_7<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3097, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_pade_abs_ec_T7_7_float_simd";
entry.accuracy_test<float>(tan_remez_pade_abs_ec_T7_7_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3098, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "float", "scalar",  };
entry.subName = "tan_ec_T7_7<float>";
entry.accuracy_test<float>(tan_ec_T7_7<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3099, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "float2x4", "simd",  };
entry.subName = "tan_ec_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_ec_T7_7<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3100, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "float", "simd",  };
entry.subName = "tan_ec_T7_7_float_simd";
entry.accuracy_test<float>(tan_ec_T7_7_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3101, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_remez_abs_T7_0<float>";
entry.accuracy_test<float>(tan_remez_remez_abs_T7_0<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3102, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_remez_abs_T7_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_remez_abs_T7_0<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3103, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_remez_abs_T7_0_float_simd";
entry.accuracy_test<float>(tan_remez_remez_abs_T7_0_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3104, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_pade_abs_T7_7<float>";
entry.accuracy_test<float>(tan_remez_pade_abs_T7_7<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3105, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_pade_abs_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_pade_abs_T7_7<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3106, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_pade_abs_T7_7_float_simd";
entry.accuracy_test<float>(tan_remez_pade_abs_T7_7_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3107, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "float", "scalar",  };
entry.subName = "tan_T7_7<float>";
entry.accuracy_test<float>(tan_T7_7<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3108, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "float2x4", "simd",  };
entry.subName = "tan_T7_7<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_T7_7<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3109, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "float", "simd",  };
entry.subName = "tan_T7_7_float_simd";
entry.accuracy_test<float>(tan_T7_7_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3110, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_remez_abs_fma_ec_T8_0<float>";
entry.accuracy_test<float>(tan_remez_remez_abs_fma_ec_T8_0<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3111, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_remez_abs_fma_ec_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_remez_abs_fma_ec_T8_0<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3112, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_remez_abs_fma_ec_T8_0_float_simd";
entry.accuracy_test<float>(tan_remez_remez_abs_fma_ec_T8_0_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3113, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_pade_abs_fma_ec_T8_8<float>";
entry.accuracy_test<float>(tan_remez_pade_abs_fma_ec_T8_8<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3114, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_pade_abs_fma_ec_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_pade_abs_fma_ec_T8_8<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3115, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_pade_abs_fma_ec_T8_8_float_simd";
entry.accuracy_test<float>(tan_remez_pade_abs_fma_ec_T8_8_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3116, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "float", "scalar",  };
entry.subName = "tan_fma_ec_T8_8<float>";
entry.accuracy_test<float>(tan_fma_ec_T8_8<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3117, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "float2x4", "simd",  };
entry.subName = "tan_fma_ec_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_fma_ec_T8_8<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3118, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "float", "simd",  };
entry.subName = "tan_fma_ec_T8_8_float_simd";
entry.accuracy_test<float>(tan_fma_ec_T8_8_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3119, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_remez_abs_fma_T8_0<float>";
entry.accuracy_test<float>(tan_remez_remez_abs_fma_T8_0<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3120, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_remez_abs_fma_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_remez_abs_fma_T8_0<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3121, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_remez_abs_fma_T8_0_float_simd";
entry.accuracy_test<float>(tan_remez_remez_abs_fma_T8_0_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3122, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_pade_abs_fma_T8_8<float>";
entry.accuracy_test<float>(tan_remez_pade_abs_fma_T8_8<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3123, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_pade_abs_fma_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_pade_abs_fma_T8_8<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3124, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_pade_abs_fma_T8_8_float_simd";
entry.accuracy_test<float>(tan_remez_pade_abs_fma_T8_8_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3125, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "float", "scalar",  };
entry.subName = "tan_fma_T8_8<float>";
entry.accuracy_test<float>(tan_fma_T8_8<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3126, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "float2x4", "simd",  };
entry.subName = "tan_fma_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_fma_T8_8<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3127, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "float", "simd",  };
entry.subName = "tan_fma_T8_8_float_simd";
entry.accuracy_test<float>(tan_fma_T8_8_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3128, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_remez_abs_ec_T8_0<float>";
entry.accuracy_test<float>(tan_remez_remez_abs_ec_T8_0<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3129, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_remez_abs_ec_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_remez_abs_ec_T8_0<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3130, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_remez_abs_ec_T8_0_float_simd";
entry.accuracy_test<float>(tan_remez_remez_abs_ec_T8_0_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3131, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_pade_abs_ec_T8_8<float>";
entry.accuracy_test<float>(tan_remez_pade_abs_ec_T8_8<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3132, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_pade_abs_ec_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_pade_abs_ec_T8_8<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3133, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_pade_abs_ec_T8_8_float_simd";
entry.accuracy_test<float>(tan_remez_pade_abs_ec_T8_8_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3134, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "float", "scalar",  };
entry.subName = "tan_ec_T8_8<float>";
entry.accuracy_test<float>(tan_ec_T8_8<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3135, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "float2x4", "simd",  };
entry.subName = "tan_ec_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_ec_T8_8<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3136, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "float", "simd",  };
entry.subName = "tan_ec_T8_8_float_simd";
entry.accuracy_test<float>(tan_ec_T8_8_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3137, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_remez_abs_T8_0<float>";
entry.accuracy_test<float>(tan_remez_remez_abs_T8_0<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3138, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_remez_abs_T8_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_remez_abs_T8_0<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3139, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_remez_abs_T8_0_float_simd";
entry.accuracy_test<float>(tan_remez_remez_abs_T8_0_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3140, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_pade_abs_T8_8<float>";
entry.accuracy_test<float>(tan_remez_pade_abs_T8_8<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3141, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_pade_abs_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_pade_abs_T8_8<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3142, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_pade_abs_T8_8_float_simd";
entry.accuracy_test<float>(tan_remez_pade_abs_T8_8_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3143, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "float", "scalar",  };
entry.subName = "tan_T8_8<float>";
entry.accuracy_test<float>(tan_T8_8<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3144, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "float2x4", "simd",  };
entry.subName = "tan_T8_8<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_T8_8<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3145, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "float", "simd",  };
entry.subName = "tan_T8_8_float_simd";
entry.accuracy_test<float>(tan_T8_8_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3146, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_remez_abs_fma_ec_T9_0<float>";
entry.accuracy_test<float>(tan_remez_remez_abs_fma_ec_T9_0<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3147, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_remez_abs_fma_ec_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_remez_abs_fma_ec_T9_0<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3148, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_remez_abs_fma_ec_T9_0_float_simd";
entry.accuracy_test<float>(tan_remez_remez_abs_fma_ec_T9_0_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3149, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_pade_abs_fma_ec_T9_9<float>";
entry.accuracy_test<float>(tan_remez_pade_abs_fma_ec_T9_9<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3150, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_pade_abs_fma_ec_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_pade_abs_fma_ec_T9_9<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3151, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_pade_abs_fma_ec_T9_9_float_simd";
entry.accuracy_test<float>(tan_remez_pade_abs_fma_ec_T9_9_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3152, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "float", "scalar",  };
entry.subName = "tan_fma_ec_T9_9<float>";
entry.accuracy_test<float>(tan_fma_ec_T9_9<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3153, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "float2x4", "simd",  };
entry.subName = "tan_fma_ec_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_fma_ec_T9_9<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3154, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "ends corrected", "float", "simd",  };
entry.subName = "tan_fma_ec_T9_9_float_simd";
entry.accuracy_test<float>(tan_fma_ec_T9_9_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3155, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_remez_abs_fma_T9_0<float>";
entry.accuracy_test<float>(tan_remez_remez_abs_fma_T9_0<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3156, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_remez_abs_fma_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_remez_abs_fma_T9_0<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3157, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_remez_abs_fma_T9_0_float_simd";
entry.accuracy_test<float>(tan_remez_remez_abs_fma_T9_0_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3158, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_pade_abs_fma_T9_9<float>";
entry.accuracy_test<float>(tan_remez_pade_abs_fma_T9_9<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3159, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_pade_abs_fma_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_pade_abs_fma_T9_9<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3160, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_pade_abs_fma_T9_9_float_simd";
entry.accuracy_test<float>(tan_remez_pade_abs_fma_T9_9_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3161, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "float", "scalar",  };
entry.subName = "tan_fma_T9_9<float>";
entry.accuracy_test<float>(tan_fma_T9_9<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3162, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "float2x4", "simd",  };
entry.subName = "tan_fma_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_fma_T9_9<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3163, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "fma", "float", "simd",  };
entry.subName = "tan_fma_T9_9_float_simd";
entry.accuracy_test<float>(tan_fma_T9_9_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3164, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_remez_abs_ec_T9_0<float>";
entry.accuracy_test<float>(tan_remez_remez_abs_ec_T9_0<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3165, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_remez_abs_ec_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_remez_abs_ec_T9_0<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3166, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_remez_abs_ec_T9_0_float_simd";
entry.accuracy_test<float>(tan_remez_remez_abs_ec_T9_0_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3167, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_pade_abs_ec_T9_9<float>";
entry.accuracy_test<float>(tan_remez_pade_abs_ec_T9_9<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3168, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_pade_abs_ec_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_pade_abs_ec_T9_9<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3169, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_pade_abs_ec_T9_9_float_simd";
entry.accuracy_test<float>(tan_remez_pade_abs_ec_T9_9_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3170, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "float", "scalar",  };
entry.subName = "tan_ec_T9_9<float>";
entry.accuracy_test<float>(tan_ec_T9_9<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3171, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "float2x4", "simd",  };
entry.subName = "tan_ec_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_ec_T9_9<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3172, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "ends corrected", "float", "simd",  };
entry.subName = "tan_ec_T9_9_float_simd";
entry.accuracy_test<float>(tan_ec_T9_9_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3173, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_remez_abs_T9_0<float>";
entry.accuracy_test<float>(tan_remez_remez_abs_T9_0<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3174, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_remez_abs_T9_0<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_remez_abs_T9_0<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3175, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_remez_abs_T9_0_float_simd";
entry.accuracy_test<float>(tan_remez_remez_abs_T9_0_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3176, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "tan", "remez_pade", "float", "scalar",  };
entry.subName = "tan_remez_pade_abs_T9_9<float>";
entry.accuracy_test<float>(tan_remez_pade_abs_T9_9<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3177, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "tan", "remez_pade", "float2x4", "simd",  };
entry.subName = "tan_remez_pade_abs_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_remez_pade_abs_T9_9<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3178, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "tan", "remez_pade", "float", "simd",  };
entry.subName = "tan_remez_pade_abs_T9_9_float_simd";
entry.accuracy_test<float>(tan_remez_pade_abs_T9_9_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3179, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "float", "scalar",  };
entry.subName = "tan_T9_9<float>";
entry.accuracy_test<float>(tan_T9_9<float>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3180, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "float2x4", "simd",  };
entry.subName = "tan_T9_9<cr::simd::float2x4>";
entry.accuracy_test<cr::simd::float2x4>(tan_T9_9<cr::simd::float2x4>, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<cr::simd::float2x4>(msvc_bug_workaround_3181, -1.5f, 1.5f);
}
{
auto& entry = testResult.make();
entry.tags = { "float", "simd",  };
entry.subName = "tan_T9_9_float_simd";
entry.accuracy_test<float>(tan_T9_9_float_simd, std::tanf, -1.5f, 1.5f, -1.5f, 1.5f);
entry.time<float>(msvc_bug_workaround_3182, -1.5f, 1.5f);
}
}
